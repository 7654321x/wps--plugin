[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "docxtool-local-recognition-agent"
version = "0.2.0"
requires-python = ">=3.8,<3.11"
dependencies = ["docxtool==1.3"]

[tool.setuptools.packages.find]
where = ["src"]
