# WPS 插件交接文档

更新时间：2026-08-07
适用仓库：`D:\PycharmProjects\wps`

## 2026-08-07 v1.5.6 恢复正式预览批注

### 当前结论

- “预览排版”后台识别成功却没有 WPS 批注的根因已确认：产品配置仍为 `threadedPreviewMode="diagnostic"`，日志明确记录 `pipeline.diagnostic.complete` 和“诊断识别完成（未写入）”。
- RecognitionPlan、格式命令生成、PreviewPlan、`host.apply_preview_batch` 和 WPS 批注服务均已存在；本轮不重写批注链，只把产品默认模式切换为 `enabled`。

### 实际改动

- `apps/classified-offline/ui/local-runtime-config.js` 改为 `threadedPreviewEnabled=true`、`threadedPreviewMode="enabled"`。
- 同步测试断言、P054 三态规则和项目 Agent 的临时后台进程/授权回收规则。
- 产品版本由 1.5.5 升至 1.5.6。

### 自动验证

- `npm test`：96/96 PASS。
- `npm run verify:all`：66/66 Python 回归、TypeScript、Node、构建与加载项检查 PASS。
- `npm run verify:local-direct`：真实已安装 Broker smoke PASS；32 个段落、31 个识别 blocks、31 个 binding blocks。
- v1.5.6 调试包与 3889 实际响应均确认 `threadedPreviewEnabled=true`、`threadedPreviewMode="enabled"`。
- `main.py start --once` 在重启旧 3889 资源服务时返回 `WPSJS_MANAGED_PROCESS_STOP_FAILED`：旧服务由另一权限会话启动，当前会话未越权停止；静态配置已更新并由现有服务正确返回，但资源服务进程缓存的 build_id 仍为上一构建。

### 真实 WPS 验收状态

- `NOT RUN`：当前 WPS WebView 仍加载 v1.5.5 diagnostic 配置；必须重新加载 v1.5.6 后点击“预览排版”，看到 `pipeline.preview.complete` 且批注数大于 0，再保存并检查 DOCX/OOXML。

### 未解决问题与下一步严格顺序

1. 用户正常保存并完全退出当前 WPS；从启动旧 3889 服务的同一权限会话结束该服务，或在该会话重新运行 `main.py start --once`。
2. 重新打开文档并点击“预览排版”，确认日志从 `pipeline.diagnostic.complete` 改为 `pipeline.preview.complete`，批注数大于 0。
3. 真实批注写入后保存文档并检查 OOXML；未完成前不写完整 PASS。

### 生成文件与 Git

- 生成文件：`apps/classified-offline/ui/build-info.js`（构建脚本生成）。
- 产品提交：`299fd18`（`release: v1.5.6 恢复正式预览批注`）。
- GitHub 推送：已通过 `git push origin HEAD:main` 推送，远程 `main` 已更新到 `299fd18`；本条交接回写将作为后续 docs 提交同步。

## 2026-08-07 v1.5.5 Broker 切换语义与首页日志去重

### 当前结论

- 安装到 AppData 的 Broker/识别程序仍必须与 runtime 清单 SHA-256 完全一致；该完整性门禁没有放松。
- 身份可信的旧 Broker 与当前安装构建不一致时改为正常版本切换，只记录 INFO；旧 PID 残留状态和新状态暂缺不再产生 readiness 警告。
- `index.html` 响应仍执行严格字节校验；资源探针不再自行记录成功，也不再冒充真实 WPS 首页请求。
- 本轮采用最小收口，没有新增协议版本、能力字段、端口或跨进程单写入器。

### 实际改动

- `main.py` 增加旧构建切换分类与 `broker.switch.start/completed` 事件；启动等待以 `broker_instance_id` 排除旧状态，并沿用 PID、创建时间和 PyInstaller 父子进程身份校验，无法写入新实例状态时最终返回 `LOCAL_JOB_BROKER_READY_TIMEOUT`。
- `probe_debug_server()` 改为只返回报告；资源服务启动或复用边界调用一次首页校验日志。
- 启动器资源请求增加 `X-Docxtool-Probe: 1`；资源服务仅对真实 WPS 请求记录 `requested/served`。
- 产品版本由 1.5.4 升至 1.5.5，并补充 Broker 与资源服务针对性回归测试。

### 自动验证

- Broker/资源服务针对性 Python 回归：33 项 PASS。
- `npm test`：96/96 PASS。
- `npm run verify:all`：66/66 Python 回归、TypeScript、Node、构建与加载项检查 PASS。
- `npm run verify:local-direct`：真实已安装 Broker smoke PASS；32 个段落、31 个识别 blocks、31 个 binding blocks。
- `main.py start --once`：PASS；复用 Broker PID 37940，随机 loopback Control Server 端口 57729，首页校验成功只记录一次，当前会话无 Broker 哈希 WARN。
- `git diff --check`：待提交前最终执行。

### 真实 WPS 验收状态

- `NOT RUN`：当前尚未通过真实 WPS 宿主重新加载并点击功能区完成验收；自动资源探针成功不能替代真实 WPS 加载证据。

### 未解决问题与下一步严格顺序

1. 运行发布固定门禁并执行一次 `main.py start --once`，确认日志无 Broker 哈希 WARN、首页成功校验只出现一次。
2. 后续若实施跨进程唯一文件写入者，必须作为独立架构版本处理，不与本次修复混合。
3. 真实 WPS 重新加载后再核对首页 requested/served、Ribbon 和 Host Runtime 事件；未执行前不得写 PASS。

### 生成文件与 Git

- 版本：v1.5.5。
- 生成文件：`apps/classified-offline/ui/build-info.js`（由构建脚本生成）。
- 产品提交：`094acaa`（`release: v1.5.5 修复 Broker 切换与首页日志去重`）。
- GitHub 推送：已通过 `git push origin HEAD:main` 推送；远程 `main` 已更新到 `094acaa`。
- 本条交接回写将作为后续 docs 提交同步。

## 2026-08-06 文档目录收口、Agent 提交与原始日志发布规则（历史记录；v1.5.0 规则见文末最新条目）

### 当前结论

- 所有权威规则正文已迁入 `docs/`：`docs/AGENTS.md`、`docs/问题清单Agent.md`、`docs/交接文档.md`。根目录 `AGENTS.md` 仅保留极短的自动发现入口，明确要求下次任务先读取 `docs/AGENTS.md`。
- 除根入口 `AGENTS.md` 和 GitHub 仓库入口 `README.md` 外，普通 Markdown 已集中到 `docs/`；`agent/` 只保存审阅证据等非 Markdown Agent 资料。
- 历史规则曾允许发布根目录原始 `wps-plugin-debug.log`；v1.5.0 已将运行日志统一为本机 `wps-plugin.log`，两者均不再进入 GitHub。

### 实际改动

- 移动 `AGENTS.md`、`问题清单Agent.md`、`交接文档.md` 的权威正文到 `docs/`，并在根目录重建 325 字节加载入口。
- 更新 `docs/AGENTS.md` 中的所有日常读取、问题清单、交接和发布路径为 `docs/`。
- 发布规则改为：禁带扫描通过后用 `git add --all` 暂存全部可版本化文件，并强制包含 `agent/`。
- 历史 `.gitignore` 曾放开根目录 `wps-plugin-debug.log`；v1.5.0 改为忽略 `wps-plugin.log`、`wps-plugin-debug.log` 和 `.runtime/logs/`，不生成轮转日志。
- v1.5.0 运行日志只作本机诊断，不作为发布文件；敏感信息仍必须在统一格式化器边界脱敏。

### 自动验证

- 根入口和 `docs/` 三份权威文件均存在，入口路径正确：PASS。
- 普通 Markdown 已集中到 `docs/`；根目录仅保留入口 `AGENTS.md` 与 GitHub `README.md`：PASS。
- `wps-plugin-debug.log`：17,636 行可解析 JSON、340 行原始非 JSON 行；敏感信息模式扫描 0 命中，已从 Git 忽略规则中放开：PASS。
- `git diff --check`：PASS（仅 Windows 换行提示）。
- 代码测试/真实 WPS 验收：未运行，本轮未修改 WPS 业务逻辑。

### 真实 WPS 验收状态

- 不变：`REAL_WPS_E2E_PARTIAL`；目录和发布流程调整不构成真实 WPS 验收。

### 未解决问题与下一步严格顺序

1. 下次任务先读取 `docs/AGENTS.md`、`docs/问题清单Agent.md` 和本文件，再开始修改。
2. 发布前确认 `wps-plugin.log`、`wps-plugin-debug.log` 和其他运行日志均未进入暂存区。
3. 禁带扫描通过后使用 `git add --all`，审阅暂存清单，确保 `agent/`、`docs/` 和所有其他可版本化文件一并提交。
4. 按 `docs/AGENTS.md` 的发布门禁、提交消息与远程核对流程推送 GitHub。

### 生成/整理文件路径与 Git

- `docs/AGENTS.md`
- `docs/问题清单Agent.md`
- `docs/交接文档.md`
- `agent/reviews/wps-plugin-v1.3.1-review.patch`
- `wps-plugin-debug.log`（原始日志例外；已原样提交）
- 内容提交：`0f1f331`（`docs: organize project records and include diagnostics`）。
- GitHub 推送：`git push origin HEAD:main` 成功；远程 `main` 已更新到 `0f1f331`。本条交接回写将作为后续 docs 提交同步。

## 2026-08-06 目录整理与 Agent 资料归档（本轮）

### 当前结论

- 已按职责整理仓库根目录；Agent 审阅资料集中到 `agent/`，项目参考文档集中到 `docs/`，用户文件和本机产物归档到被忽略的 `local_recycle/` 分类目录。
- 根目录 `AGENTS.md`、`问题清单Agent.md`、`交接文档.md` 必须保留；根目录识别验收源文件和 `wps-plugin-debug.log` 被脚本固定引用，本轮没有移动。
- 未修改 WPS 业务代码、版本号、运行时配置或真实 WPS 行为。

### 实际改动

- 新增 `docs/AGENT_WORKSPACE.md`，说明 Agent 资料边界和根目录权威规则文件；普通 Markdown 统一集中在 `docs/`。
- 移动项目文档：`DEVELOPMENT_PLAN.md` → `docs/DEVELOPMENT_PLAN.md`；`公文格式规范.md` → `docs/公文格式规范.md`。
- 移动审阅证据：`wps-plugin-v1.3.1-review.patch` → `agent/reviews/wps-plugin-v1.3.1-review.patch`。
- 归档用户 DOCX 到 `local_recycle/user-documents/`；归档 wheel 到 `local_recycle/runtime-artifacts/`。这些目录被 Git 忽略。
- `AGENTS.md`、`问题清单Agent.md` 已阅读；本轮未发现新的可复发问题，不新增问题清单条目。

### 自动验证

- 移动后目标文件存在、源路径已清理：PASS。
- `acceptance-real-doc.ps1` 所需的根目录 `005班子对照检查材料.docx`：保留并未改变。
- 日志脚本所需的根目录 `wps-plugin-debug.log`：保留并未改变。
- 代码/真实 WPS 测试：未运行（本轮仅目录整理）。

### 真实 WPS 验收状态

- 不变：`REAL_WPS_E2E_PARTIAL`；本轮没有新增或虚构真实 WPS PASS。

### 未解决问题与下一步严格顺序

1. 继续使用现有脚本固定路径运行真实文档验收，不要移动根目录源 DOCX 或诊断日志。
2. 后续如需把诊断日志也归档，必须先修改并验证 `main.py`、E2E 脚本和日志工具的统一路径合同，再单独发布。
3. 下次提交前按 `AGENTS.md` 的显式文件清单和 GitHub 发布门禁执行；本轮不自动 commit/push。

### 生成/整理文件路径与 Git

- `docs/AGENT_WORKSPACE.md`
- `agent/reviews/wps-plugin-v1.3.1-review.patch`
- `docs/DEVELOPMENT_PLAN.md`
- `docs/公文格式规范.md`
- `local_recycle/user-documents/`
- `local_recycle/runtime-artifacts/`
- 本轮未 commit、未 push；工作树包含本次整理的可审阅变更。

## 2026-08-06 AGENTS.md 固化 format_document 正式执行链（本轮）

### 当前结论

- 用户提供"一键排版"新执行链流程（保存 → Worker 分批快照 → 识别提交 → 四重校验 → 核验快照 → 本地命令生成 → 分批执行 → 执行后检查 → 自动保存），并确认识别提交**按部署形态二选一**：本机直连 `LocalFileQueueTransport`（Job Broker 文件队列）/ 远程受控 `RemoteHttpsTransport`（Control Server）。
- 按用户要求本轮**只改 `AGENTS.md`**，不动 README；未改任何代码、构建产物或产品版本。

### 实际改动

- `AGENTS.md`：
  - 第 1 条 Control Server 链路补充承载 `RemoteHttpsTransport`，识别结果必须经 Schema/版本/能力/Hash 四重校验后才能进入命令生成。
  - 第 2 条本地识别链路补充承载 `LocalFileQueueTransport`（无端口 Job Broker 文件队列领取任务，禁止 Worker/Host 直接 `ShellExecute`）。
  - 新增"生产执行链的识别提交按部署形态二选一"段：生产链同一时刻只启用其一，均不得混入 9528 旧链路。
  - Worker 边界段补充：识别快照由主线程按批采集纯数据交给 Worker；Worker 分批调用 `WpsCommandExecutor`，每个待写目标执行前局部 Hash 核验；Worker 不得访问宿主对象。
  - 新增 `## 正式执行链（format_document 一键排版）` 小节：固化 16 步流程，并绑定 P039（文档变化判定基线）、P041（禁止批次间全文 `activeRevision()`）、P002（保存后 OOXML 复查）。

### 自动验证

- `git diff --check`：PASS（仅 CRLF 提示，无 whitespace 错误）。
- 纯文档规则变更（AGENTS.md 第 41 条：流程规则文档变更不改变产品版本），不触发构建/发布；未跑代码门禁。

### 真实 WPS 验收状态

- 不变：`REAL_WPS_E2E_PARTIAL`，format_document 未真实验收，不得写 PASS。

### 未解决问题

- 新执行链对应代码（transport 选择、RecognitionResult 校验、Worker 分批执行器）尚未实现，属后续 T12–T17 / v1.4.0 C8–C16 范围。
- README 中 format_document 状态描述按用户要求本轮不改，等真实验收通过后再更新。
- 工作区同时包含 v1.4.0 Control Server 未提交改动（control-server/、packages/control-client/、schemas/、main.py、package.json 1.4.0 等）与本轮 AGENTS.md 改动；两者均未 commit/push。

### Commit / Push

- 未 commit、未 push。最近提交仍为 `a205e54 docs: record v1.3.2 GitHub publish`（8/6 15:38）；分支 `codex/1.0`。

## 2026-08-06 v1.4.0 WPS Control Server 基础控制面（本轮最新）

### 当前结论

- 旧“禁止新增本机业务端口”规则已按用户明确授权移除。生产控制面现在允许独立 WPS Control Server 使用 `127.0.0.1` 随机高位 HTTP 端口；固定 `9528`、旧 `local-agent`、旧 `command-service` 仍不回接生产链。
- 已完成 C0–C4、C6、C7 基础：版本化 JSON Schema/TypeScript/Python 合同、Bearer token、endpoint manifest、PID/创建时间/instance/版本/合同/心跳校验、单 active + 单 queued Job Store、取消/超时、FormattingPlannerPort、ControlTransport，以及 Dedicated Worker 的可选控制面路径。
- C5 Runtime RecognitionPort 尚未接入。服务器如实报告 `recognition.available=false`，`controlServerEnabled=false`；没有自动切换现有 Broker 识别、预览或正式排版，避免未验收情况下改变真实 WPS 行为。

### 实际改动

- 新增 `control-server/`：只绑定 `127.0.0.1` 随机端口，提供 `/v1/health`、`/v1/capabilities`、`/v1/jobs`、job status/result/cancel；请求不接受 EXE、命令行、脚本、模块或代码字段，日志不记录正文、路径或 token。
- endpoint manifest 位于 `%LOCALAPPDATA%\DocxToolWps\control\endpoint.json`；`main.py` 可启动/复用/status/stop，并在复用时联合核对 manifest、PID、创建时间、instance、版本、合同、heartbeat 与 Bearer 鉴权响应。
- 新增 `packages/control-client` 与 Control schemas；Worker 仅在显式 `control_endpoint` 配置时经 `LocalHttpControlTransport` 提交/轮询/取消，未配置时保持已有受检 Broker 路径。
- 根、classified-offline 包版本同步为 `1.4.0`；当前构建 ID：`20260806092552-2af010b834e4`。
- 更新 `AGENTS.md`、`README.md`、`docs/DEVELOPMENT_PLAN.md`、架构/安全/合同文档及问题清单 P056–P059。

### 自动验证

- `control-server/tests`：6 PASS（随机 loopback manifest、鉴权/Host、立即 202、幂等、单队列、取消、危险字段拒绝）。
- `npm test`：101 PASS；包含 Control Client、Control schemas 和 Worker ControlTransport 路径测试，证明不调用 Host recognition RPC。
- `npm run verify:all`：`VERIFY_ALL_PASS`；Python 全量 40 PASS、Control Server Ruff PASS、classified/online build 和 add-in verify PASS。
- `npm run verify:local-direct`：`VERIFY_LOCAL_DIRECT_PASS`；脱敏 fixture 32 段、31 blocks、31 bindings，旧 9528 未监听。
- `npm run build:classified`：PASS，构建 ID 如上；`git diff --check`：PASS。
- `main.ensure_control_server()` 与 `python main.py control-status`：PASS；实际随机端口为 `55950`，实例/PID 已通过本机 health 校验（不记录 token）。
- Control Server Ruff：PASS。`main.py` 的 15 个既有 E701/E702 紧凑写法仍存在，未为无关格式问题重排；本轮新增的 Control Server 文件无 Ruff 告警。

### 真实 WPS 验收状态

- C14 diagnostic 20/200/1000：WAITING（C5 Runtime RecognitionPort 尚未接入）。
- C15 preview 20/200/1000：WAITING；默认不会写批注。
- C16 format 20/200/1000、取消、文档切换、回滚、最终保存/OOXML：WAITING；本轮没有修改 WPS API Executor、没有虚构 PASS。

### 未解决问题与下一步严格顺序

1. C5：把现有受校验 `docxtool-recognize.exe`/Broker 输出安全映射为冻结 `RecognitionResult` 的 RecognitionPort，先写 shadow JSON 等价测试，不引入第二识别算法或任意 EXE。
2. C6/C7 联调：用真实 FormattingPlannerPort 对照现有 `LocalFormatCommandGenerator`，验证 Control Server result 与本地命令集等价。
3. C8–C13：补全状态机、snapshot A/B、HostBridge 局部核验、preview/format batch 和 20/200/1000 fake-WPS 门禁。
4. C14→C15→C16：严格按 diagnostic、preview、format 顺序完成真实 WPS 20/200/1000 与保存后 OOXML 验收；任一前置失败不得进入下一阶段。

### 生成文件与 Git

- 运行时 manifest：`%LOCALAPPDATA%\DocxToolWps\control\endpoint.json`（用户级 token 文件，忽略且不提交）。
- 运行时日志：`D:\PycharmProjects\wps\.runtime\logs\control-server.log`（忽略且不提交）。
- 发布提交：`419039f068d067b3fb53cf24bff729f93f991295`（`release: v1.4.0 WPS Control Server foundation`）。
- GitHub 推送：`git push origin HEAD:main` 成功；已核对本地 HEAD 与 `refs/heads/main` 均为 `419039f068d067b3fb53cf24bff729f93f991295`。本交接发布记录将作为单独 docs 提交同步。

## 2026-08-06 Control Plane Looper C0 盘点（已解除）

### 当前结论

- 用户已明确授权恢复本机服务器端口，并移除“禁止新增本机业务端口”的旧规则。Control Server 允许使用随机高位 loopback HTTP 端口；固定 `9528`、旧 `local-agent` 和旧 `command-service` 仍不回接生产链。
- C0 阻断已解除。当前状态：`C1_IN_PROGRESS`；本轮后续只增加控制面合同和编排边界，不修改识别算法、公文排版规则或 WPS API 业务规则。

### C0 实际事实表

| 项目 | 当前实现 | 结论 |
|---|---|---|
| 生产识别 | `LocalProcessRecognitionTransport` 写 UUID 任务目录 | 通过无端口文件队列提交 |
| Broker | `local-runtime/job_broker.py` / `docxtool-job-broker.exe` | 负责 claim、lease、recognizer 启动和 result/error |
| Worker | `apps/classified-offline/src/pipeline-worker.ts` | 负责快照、Hash、识别轮询、命令生成、预览/格式批次、取消 |
| HostBridge | `packages/wps-adapter/src/host-bridge.ts` | 只执行受限 WPS RPC；没有生产 HTTP 等待 |
| 格式计划 | Worker 内 `LocalFormatCommandGenerator` | 未拆成独立 Control Server 端口 |
| 旧 HTTP 客户端 | `launcher-client`、`command-service-client`、`HttpLocalRecognitionTransport` | 仅遗留/测试路径，不能作为新生产链恢复 |
| 网络端口 | 新 Control Server 仅监听 `127.0.0.1` 随机高位端口；3889/9528 保留为旧静态资源/诊断链 | 不固定使用 9528，不监听 `0.0.0.0` |
| 预览模式 | `diagnostic`，`threadedPreviewEnabled=false` | 真实 WPS 验收仍未完成 |

### 下一步严格顺序

1. 增加版本化 Control Contracts、随机 loopback endpoint manifest 和认证边界。
2. 增加单 active/单 queued Job Store、取消/超时和 RecognitionPort/FormattingPlannerPort；适配器继续复用现有受校验 runtime。
3. 增加 Dedicated Worker 的 `ControlTransport`，先以 shadow/diagnostic 方式接入；真实 WPS 20/200/1000、预览、格式、取消、文档切换和保存后 OOXML 继续保持 WAITING。

### Git

- 本轮已更新 C0 解除记录，代码改动尚未 commit/push；产品版本在完成控制面协议改动后按规则递增次版本号。

## 2026-08-06 v1.3.2 Broker 与真实预览收口（本轮最新）

### 当前结论

- 审阅基线：`1be9b8f`；v1.3.2 release HEAD：`c81e3371dd25c1ad0a430a0ab3f83fe0049be7c8`；当前分支：`codex/1.0`。
- 包版本和 Broker 版本：`1.3.2`；根 `package.json` 与 `apps/classified-offline/package.json` 保持同步。
- v1.3.2 构建 ID：`20260806073518-27919a7a6d8d`（由构建脚本生成）；`threadedPreviewMode=diagnostic`，`threadedPreviewEnabled=false`。
- 新 Broker 已安装并健康运行：PID `29540`（PyInstaller 实际 worker PID），`broker_instance_id=57438f22-3823-4e19-a87e-27d96ad7ef0f`，进程创建时间 `2026-08-06T07:34:21.659832Z`；queue contract：`1`。
- Broker EXE SHA-256：`ebaad8ceda2135020018a2ade5f95e2254a59a4d1f1c6039abf4647a1c9bcee5`；recognizer SHA-256：`45a9418b26e800d8e8533eda625423da266bfce48a7da8ce5055606a20ef79cd`。
- 最终状态：`READY_FOR_REAL_WPS_VALIDATION`。A1–A7 已完成；A8–A15 尚未完成，不能写 `REAL_WPS_PREVIEW_PASS`、`THREAD_DECOUPLING_COMPLETE` 或 `T12_READY`。

### 实际改动

- Broker status 增加 instance、进程创建时间、Broker/runtime 版本与双 SHA-256、path hash、queue contract 校验；`main.py` stop/restart 只接受精确路径、命令行、PID 创建时间同时匹配的受管 Broker。
- 文件队列使用 `claim.lock` 的 `O_CREAT|O_EXCL` 独占 claim，增加 15 秒 lease、失主过期恢复 `recovery.json`、取消前/启动前/运行中的稳定错误码，以及有效 result 优先于 error/cancel 的终态判断。
- 修复 Worker 先消费并清理 result 后 Broker reap 的竞态：任务目录消失不再伪造 `LOCAL_RECOGNITION_RESULT_MISSING`；客户端清理同时包含 `claim.lock`。
- 修复 v1.3.2 安装时 PyInstaller one-file 父/子 Broker 的锁文件处理：只依据受信 current/status 的路径 hash、PID 创建时间和同一启动树停止旧 Broker，不扩大到其他同名进程。
- 增加 `disabled / diagnostic / enabled` 三态预览模式。diagnostic 只运行 Worker snapshot 与 Broker 识别，不写批注、不写格式；正式 Preview Batch 仍受 enabled 门禁保护。
- 同步 runtime manifest/current/status 校验、Host/recognition client 合同字段、错误码、Broker 回归测试和 Looper 状态。

### 自动验证

- `npm test`：96/96 PASS。
- `.venv\\Scripts\\python.exe -m pytest local-runtime\\tests -q`：14 PASS。
- `npm run verify:all`：`VERIFY_ALL_PASS`；Python 全量 34 PASS、classified/online build 和 add-in verify PASS，门禁范围内 Ruff PASS。
- `npm run verify:local-direct`：`VERIFY_LOCAL_DIRECT_PASS`；脱敏 fixture 32 段、31 blocks、31 bindings；9528 未监听。
- `npm run build:local-runtime`：`LOCAL_RUNTIME_BUILD_PASS`；`npm run install:local-runtime`：`LOCAL_RUNTIME_INSTALL_PASS`。
- `git diff --check`：PASS。针对 `local-runtime` 的 Ruff 检查 PASS；`main.py` 仍有 15 个既有 E701/E702 紧凑写法告警，未为本轮无关格式问题重排旧代码。
- Broker 双实例 claim、lease/recovery、存活 claim、Popen 后取消、Worker 清理终态目录和 diagnostic mode 均有自动回归。

### 真实 WPS 验收状态

- 当前 WPS 仍有既有进程运行，日志最近加载的是旧 build `20260806030041-212ee48af0a1`；尚未加载本轮 `20260806073518-27919a7a6d8d`。
- 尚无新 build 的 diagnostic 20/200/1000 识别证据，尚无 enabled 正式预览 20/200/1000、取消、文档切换或保存后 OOXML 证据。
- 因此以下项目全部保持 `WAITING`：20/200/1000 blocks/binding、Host RPC/claim/launch/recognition 指标、WPS Responding、批注数量、`comments.xml`、正文 hash、段落/表格结构和用户批注保留。
- 本轮未强制关闭 WPS、未操作用户文档、未把自动 smoke 结果冒充真实 WPS 验收。

### 未解决问题

- 必须由用户保存当前文档并完全关闭全部 WPS 窗口，再重新打开脱敏 20/200/1000 文档，使当前 build 进入宿主后执行 diagnostic 验收。
- 真实 WPS 尚未证明正式 enabled 预览和保存后 OOXML 持久化；T12–T17 继续禁止进入。
- `main.py` 的 15 个既有 Ruff E701/E702 仅为旧紧凑写法，未影响 `verify:all` 范围内门禁；若要清零需另开纯格式修复。

### 下一步严格顺序

1. 用户保存并完全退出全部 WPS 进程；重新打开当前本地 debug 加载项和脱敏 20 段文档。
2. 确认 Host 日志的 build 为 `20260806073518-27919a7a6d8d`，只执行 diagnostic 20；确认零批注、零格式写入和 result/error 合同。
3. 依次执行 diagnostic 200、diagnostic 1000，记录 snapshot/queue/claim/launch/recognition 指标；任一失败即保持 diagnostic 并停止后续正式预览。
4. 仅当 diagnostic 20/200/1000 全部真实通过时，将本地验收配置切为 enabled，依次执行正式预览 20、200、1000。
5. 继续验收 queued/claimed/running/Preview Batch 取消和文档 A→B 切换，确认不重复启动、不跨文档写入。
6. 通过 WPS 保存测试副本，检查 `comments.xml`、批注角色/锚点、正文 SHA-256、段落顺序、表格结构和原有批注；完成后才更新 Looper 为最终 PASS/READY_FOR_T12。

### 生成文件路径

- Looper：`D:\PycharmProjects\wps\.runtime\looper-v131-broker-real-preview.md`（忽略，不提交）。
- 构建清单与 EXE：`D:\PycharmProjects\wps\dist\local-runtime\win-x64\runtime-manifest.json`、`docxtool-recognize.exe`、`docxtool-job-broker.exe`（忽略，不提交）。
- 已安装 runtime：`C:\Users\94575\AppData\Roaming\Docxtool\runtime\docxtool-4.0\`；status/current/jobs 在 `%APPDATA%\\Docxtool\\`（运行时文件，不提交）。
- 插件构建信息：`D:\PycharmProjects\wps\apps\classified-offline\ui\build-info.js`。
- 规则和交接：`D:\PycharmProjects\wps\问题清单Agent.md`、`D:\PycharmProjects\wps\交接文档.md`。

### Commit / Push

- release commit：`c81e3371dd25c1ad0a430a0ab3f83fe0049be7c8`（`release: v1.3.2 WPS plugin patch release`）。
- release push：`PASS`，已执行 `git push origin HEAD:main`，远程 `main` 已接受 `c81e3371dd25c1ad0a430a0ab3f83fe0049be7c8`；本次交接同步作为随后文档提交推送。
- `.runtime`、日志、EXE、用户 DOCX 和测试工作副本不纳入提交。

## 2026-08-05 根目录全链路诊断日志（最新）

### 背景

- 本轮针对“点击预览排版仍然没有反应”补充可落盘诊断，只增加可观测性。
- 未修改识别分类规则、公文格式规则、FormattingCommandSet schema、Preview/Format 业务语义或 WPS adapter 正常执行顺序。
- ACTIVE_EDITION：`classified-offline`。

### 日志文件

- `<repo>/wps-plugin-debug.log`
- 格式：UTF-8 JSON Lines，一行一个事件。
- 运行时唯一写入者：9528 local-agent；main、Ribbon、Host 和 taskpane 均只通过带 session header 的 loopback POST 发送。

### 轮转

- 单文件 5 MB，保留 `wps-plugin-debug.log.1` 至 `.5`。
- `.gitignore` 已忽略 `wps-plugin-debug.log*`。

### 覆盖链路

- main 与 build/runtime/profile 脚本加载。
- Ribbon 脚本、`OnAddinLoad`、按钮点击、Host dispatch 和任务窗格 fallback。
- Host runtime 安装等待、router dispatch、正式应用用例和预览结果状态。
- taskpane DOM/按钮绑定、请求持久化、Host 状态变化、渲染和 30 秒超时。
- recognition、command service 请求状态与字节数。
- WPS 预览批注清理、逐项写入、读回及失败异常。

### 脱敏规则

- 客户端和 local-agent 双重脱敏。
- 不记录 DOCX 正文、段落原文、完整本地路径、session token、Authorization、Cookie、完整请求体/响应体或 Base64 文档。
- 允许记录 build ID、脱敏文档身份哈希、扩展名、段落/命令/批注数量、HTTP 状态、耗时、稳定错误码和已脱敏 stack。

### 查看命令

```powershell
npm run logs:wps:path
npm run logs:wps:summary
npm run logs:wps:tail
npm run logs:wps:clear
```

### 自动验证

- `npm run typecheck`：PASS。
- `npm test`：66 项 PASS。
- local-agent tests：11 项 PASS。
- `verify-all.ps1`：PASS；合计 Python 34 项 PASS、Ruff PASS、classified/online build PASS、add-in verify PASS。
- 最新 classified Build ID：`20260804183339-bb0fa4405a70`。
- E2E prepare：PASS；3889、9528、统一 command service 和 debug registration 均为 PASS。
- 根日志已生成；prepare 后安全检查为有效 JSONL、0 个 ERROR/FATAL，未发现 session token、用户目录或仓库绝对路径。

### 真实 WPS 日志状态

- `READY_FOR_USER_WPS_LOG_CAPTURE`。
- prepare 后仍检测到既有 WPS 进程；为保护可能未保存的用户文档，本轮没有强制结束这些进程。
- 既有 WebView 已上报 `main_script_loaded`、`ribbon_onload`，并在 module 标签修复后由缓存的旧 Host bundle 上报 `host_module_loaded`、`host_router_installed`；旧 bundle 没有新 logger，故根日志仍未接管宿主事件。现已同时增加 build 版本参数，最新 build 尚待 WPS 重启加载，因此当前不能写 `ROOT_DIAGNOSTIC_LOG_CAPTURED` 或 `REAL_WPS_PREVIEW_PASS`。

### 最后一个有效事件

- 初始 prepare 快照最后事件：`local_agent.request.completed`。
- 根日志已包含 `launcher.prepare.start` 至 `launcher.prepare.ready`；E2E 旁路状态包含 `main_script_loaded`、`ribbon_onload`、`host_module_loaded`、`host_router_installed`，但当前运行的是缓存旧 bundle，根日志尚未接管 main/Ribbon 早期队列。

### 初步断点

- 当前不是 recognition wheel 或 command-service 失败。先修复了 Vite ESM 产物用 classic script 加载的问题；随后又确认固定子资源 URL会命中旧 bundle。main 现以 nonce 加载 build-info，其余 profile/runtime/Ribbon/Host 使用 build id 参数，任务窗格按 `host_build` 动态导入 workflow。

### 下一步严格顺序

1. 用户先保存正在编辑的文档，然后完全关闭全部 WPS 窗口。
2. 重新打开一个已保存的本地 `.docx`，打开任务窗格，核对 build 短号接近 `20260804183339-bb0...`。
3. 只点击一次“预览排版”，不要连续点击；其余服务和日志操作无需用户处理。
4. 下一轮先读取根日志最后事件定位断点；若预览成功，再保存 DOCX 并完成 OOXML/批注读回检查，之后才能标真实 PASS。

### 实际改动

- 新增共享诊断类型、双重脱敏和有界 loopback logger。
- 新增 local-agent JSONL writer、鉴权接口、状态接口、轮转与请求日志。
- main、Ribbon、Host、taskpane、recognition、command service 和 WPS 批注链路均已接入。
- 新增日志查看脚本、package 命令、Python/Node 回归和 production dist 强制检查。
- 修复 Vite ESM 产物的加载标签：Host runtime 和 taskpane workflow 使用 `type="module"`，Ribbon 继续保持 WPS 所需的全局 classic callback。
- main 和 taskpane 子资源均按 bootstrap nonce/build id 版本化，避免 WPS WebView 继续复用旧 Host/Ribbon/workflow bundle。
- `问题清单Agent.md` 新增 P022、P023 防复发规则；本地 Looper 状态记录在 `.runtime/looper-root-diagnostic-log.md`。

### 未解决问题

- 尚未由重新启动后的真实 WPS 产生按钮点击日志。
- 尚未完成真实预览批注写入/读回和保存后 DOCX/OOXML 检查。

### 生成文件路径

- 根目录日志：`<repo>/wps-plugin-debug.log`（忽略，不提交）。
- 本地 Looper 状态：`<repo>/.runtime/looper-root-diagnostic-log.md`（忽略，不提交）。
- 交接文档：`<repo>/交接文档.md`。

### Commit / Push

- 本轮快速发布已完成，具体提交哈希以仓库当前 `HEAD` 为准。
- 本轮发布包含源码、测试、文档和脱敏 fixture；真实 DOCX、`.idea`、`.reasonix`、`.runtime` 和根日志均未纳入版本控制。

## 2026-08-05 Ribbon“预览排版”无效命令排查与 production 链路修复（最新）

### 当前结论

- 用户反馈：WPS Ribbon 中的“预览排版”按钮点击后无效。经排查，直接原因是当前运行的 WPS 进程（8/4 8:53 与 10:18 启动）加载的是早于最新 build（`20260804174334-…`，8/5 01:43 构建）的旧插件页面，HostCommandRouter 未安装，`window.DocxtoolHostDispatch` 不存在，按钮命令无人消费。属于 P004 旧宿主缓存旧构建场景。
- 同时发现并修复 production 链路两个独立缺口：`main.production.js` 引用的 `js/ribbon-production.js` 在构建产物中不存在（vite 输出为 `js/ribbon.js`），按 dist 发布时 ribbon 回调 404、`OnAction` 未定义，WPS 对每个按钮报“无效命令”；且 `main.production.js` 未加载 `ui/e2e-session.js`（prepare 生成的 `DocxtoolRuntimeConfig` 注入脚本），`vite.config.js` 也未拷贝该文件到 dist，导致 host-runtime `runtimeConfig()` 抛 `PRODUCTION_COMPOSITION_NOT_READY`，router 永不安装。
- 本轮未修改识别 wheel、识别规则、格式业务规则或 command-service 业务逻辑。

### 实际改动

- `apps/classified-offline/main.production.js`：新增 `<script src='ui/e2e-session.js'></script>`；ribbon 引用从 `js/ribbon-production.js` 改为 `js/ribbon.js`（与 vite 构建输出一致）。
- `apps/classified-offline/vite.config.js`：copyFile 新增 `ui/e2e-session.js → dist/ui/e2e-session.js`。
- `tests/wps-phase-one.test.mjs`：新增测试“production entry loads the runtime config and the ribbon file that the build actually emits”，断言 production 入口加载 `js/ribbon.js` 与 `ui/e2e-session.js`、不再引用 `js/ribbon-production.js`、vite 拷贝 e2e-session.js。
- `scripts/verify-addin.mjs`：classified 分支新增 `ui/build-info.js`、`ui/e2e-session.js`、`ui/default-format-profile.js` 生产产物检查。
- `问题清单Agent.md`：新增 P021，记录 production 入口 runtime config 注入缺失与 ribbon 文件名错位的症状、根因、禁止做法与验证门槛。

### 自动验证

- `npm run typecheck`：通过。
- `npm test`：通过，59 项 PASS（新增 1 项）。
- `npm run build:classified`：通过，新 Build ID `20260804174334-7374fe454efc`。
- dist 产物检查：`dist/main.js` 依次加载 build-info / default-format-profile / e2e-session / js/ribbon.js / host-runtime；`dist/ui/e2e-session.js` 存在且含当前 session token；`dist/js/ribbon.js` 为 production ribbon（含 `DocxtoolHostDispatch`）。
- `npm run verify:addin -- classified-offline`：通过，`ADDIN_VERIFY_PASS`。
- `pwsh -NoProfile -File scripts/verify-all.ps1`：通过，`VERIFY_ALL_PASS`。
- 9528 统一服务健康：`/v1/health` ok、`/v1/version` 返回 `docxtool 4.0`；3889 静态服务 200。

### 真实 WPS 验收状态

- 当前 `.runtime/e2e/current.json` 仍为 `REAL_WPS_E2E_PARTIAL`：只有 `main_script_loaded`、`host_module_loaded`、`ribbon_onload`，无 `host_router_installed`；该 session 早于最新 build，来自旧 WPS 进程，不能写为 PASS。
- 需要用户关闭全部 WPS 窗口后重新打开本地 `.docx`，让旧 WebView 加载最新 build；重新打开后任务窗格 build 短号应接近 `20260804174334-7374…`，点击“预览排版”应立即显示“命令已发送，等待 WPS 主上下文处理…”，且 `current.json` 应出现 `host_router_installed`。

### 未解决问题

1. 真实 WPS 中尚未确认新版 build 出现 `host_router_installed`，也未完成预览批注生成、保存后 DOCX/OOXML 批注检查、正文 SHA-256 与段落顺序检查。
2. 用户当前 WPS 进程早于最新构建，必须完整重启 WPS 宿主（关闭全部窗口再打开）才能加载新 build；禁止只重启服务。
3. `standard-online` 仍是旧骨架（Ribbon 使用浏览器事件、production composition 仍使用 Mock），本轮 ACTIVE_EDITION 为 `classified-offline`，未改联网版。

### 下一步严格顺序

1. 保持 3889/9528 常驻，不重新 prepare、不轮换 token。
2. 用户关闭全部 WPS 窗口。
3. 重新打开本地 `.docx` 文档。
4. 打开任务窗格，核对 build 短号接近 `20260804174334-7374…`。
5. 点击“功能检测”，确认中文状态。
6. 点击“预览排版”，确认即时等待状态与后续批注。
7. 保存后做 OOXML 检查；未完成保存后检查前不得写 PASS。

### 生成文件路径

- 本地 Looper 记录：`D:\PycharmProjects\wps\.runtime\looper-wps-host-interaction.md`
- 交接文档：`D:\PycharmProjects\wps\交接文档.md`
- 问题清单：`D:\PycharmProjects\wps\问题清单Agent.md`

### Commit / Push

- 本轮未 commit、未 push。
- 上一轮 GitHub 最新提交仍为 `ef467b8 feat: bind WPS host recognition workflow`。
- 工作区包含本轮源码/测试/脚本改动；`ui/e2e-session.js`（含 token）与真实 DOCX、`.runtime`、`.idea` 仍不纳入提交。

## 2026-08-05 WPS 预览排版交互 Looper 收口（最新）

### 当前结论

- ACTIVE_EDITION：`classified-offline`。证据来自涉密版任务窗格、3889 静态加载项、9528 统一服务和 `.runtime/e2e/current.json`。
- 本轮没有修改识别 wheel、识别规则、格式业务规则或 command-service 业务逻辑。
- 预览排版无反应的主因仍归类为宿主交互链路问题：主上下文/Ribbon/任务窗格桥接/旧 WebView，而不是识别算法失败。
- `classified-offline` 已具备 `HostCommandRouter`、`DocxtoolHostDispatch`、`tryInstall()` 重试安装、任务窗格即时等待状态和中文错误映射。
- 本轮只补齐一个交互缺口：Ribbon fallback 创建任务窗格失败时不再静默吞错。

### 实际改动

- `apps/classified-offline/js/ribbon-production.js`：`CreateTaskPane()` 返回空值时写入 `TASKPANE_CREATE_FAILED`，不再继续访问 `pane.ID`；成功时显式返回 `true`。
- `apps/classified-offline/js/ribbon.js`：开发 Ribbon 同步上述 fallback 保护。
- `apps/classified-offline/src/error-messages.ts`：`DOCUMENT_MUST_BE_SAVED` 用户提示统一为“本地 .docx 文件”。
- `tests/wps-phase-one.test.mjs`：新增/补强生产 Ribbon 直连 Host 路由、禁止浏览器事件、任务窗格即时等待状态、任务窗格创建失败中文错误的回归测试。
- `问题清单Agent.md`：补充 P020，记录 `CreateTaskPane()` 空返回导致按钮无反馈的防复发规则。
- `.runtime/looper-wps-host-interaction.md`：本地 Looper 执行记录，仅本机使用，不提交。

### 自动验证

- `npm run typecheck`：通过。
- `npm test`：通过，58 项 PASS。
- `node --test --test-name-pattern="host|router|taskpane|ribbon" tests/wps-phase-one.test.mjs`：通过，16 项 PASS。
- `npm run build:classified`：通过。
- `npm run verify:addin -- classified-offline`：通过。
- `pwsh -NoProfile -File scripts/verify-all.ps1`：通过，`VERIFY_ALL_PASS`。
- classified-offline 最新 Build ID：`20260804172956-f6a3242b5e86`。
- dist 产物检查：`main.js` 引用 `host-runtime.js`；production Ribbon 包含 `DocxtoolHostDispatch`；不包含 `CustomEvent/dispatchEvent`；生产 dist 未发现 Mock adapter。

### 真实 WPS 验收状态

- 当前 3889 静态资源、9528 统一入口和 debug 注册均为 PASS。
- 当前 `.runtime/e2e/current.json` 仍为 `REAL_WPS_E2E_PARTIAL`，不能写为真实 WPS PASS。
- 需要用户关闭全部 WPS 窗口并重新打开本地 `.docx`，让旧 WebView 重新加载最新 build。
- 重新加载后，任务窗格构建短号应接近 `20260804172956-f6a...`；点击“预览排版”后应立即显示“命令已发送，等待 WPS 主上下文处理…”。

### 未解决问题

1. 真实 WPS 中尚未确认新版 build 已出现 `host_router_installed`。
2. 尚未完成真实 WPS 预览批注生成、保存后 DOCX/OOXML 批注检查、正文 SHA-256 和段落顺序检查。
3. `standard-online` 仍是旧骨架：Ribbon 使用浏览器事件、生产 composition 仍使用 Mock。因本轮 ACTIVE_EDITION 为 `classified-offline`，未改联网版生产链路。

### 下一步严格顺序

1. 不再重复 prepare，保持 3889/9528 常驻。
2. 用户关闭全部 WPS 窗口。
3. 重新打开本地 `.docx` 文档。
4. 打开任务窗格，核对 build 短号。
5. 点击“功能检测”，确认中文状态。
6. 点击“预览排版”，确认即时等待状态和后续批注。
7. 保存文档后再做 OOXML 检查；未保存检查前不得写 PASS。

### 生成文件路径

- 本地 Looper 记录：`D:\PycharmProjects\wps\.runtime\looper-wps-host-interaction.md`
- 交接文档：`D:\PycharmProjects\wps\交接文档.md`
- 问题清单：`D:\PycharmProjects\wps\问题清单Agent.md`

### Commit / Push

- 本轮未 commit、未 push。
- 上一轮 GitHub 最新提交仍为 `ef467b8 feat: bind WPS host recognition workflow`。
- 当前工作区包含本轮源码/测试/文档改动，以及验证脚本重新生成的脱敏 fixture DOCX；真实 DOCX、`.idea` 和本地工作副本仍未纳入提交。

## 2026-08-05 错误中文化与 `DOCUMENT_MUST_BE_SAVED` 排查（最新）

### 当前结论

- 用户截图中的 `DOCUMENT_MUST_BE_SAVED` 不是识别 wheel 或本地服务故障。
- 根因是当前 WPS 活动文档尚未保存为本地 `.docx` 文件；WPS 插件需要取得本地 DOCX 路径后，才能交给本机 9528 服务做识别和绑定。
- 这个错误码是正确的内部稳定码，但不能直接裸露给用户；界面已改为显示中文原因和处理动作，同时保留“错误码：DOCUMENT_MUST_BE_SAVED”用于排查。

### 实际改动

- `apps/classified-offline/src/error-messages.ts`：新增统一中文错误映射，提供 `errorMessage()` 和 `errorText()`。
- `apps/classified-offline/src/taskpane-workflow.ts`：任务窗格“执行状态”“问题与复核”改为显示中文说明。
- `apps/classified-offline/src/host-runtime.ts`：主上下文写入失败状态时同步写入中文进度。
- `apps/classified-offline/src/health-check.ts`：功能检测报告显示中文失败原因，并保留原始错误码。
- `apps/classified-offline/js/ribbon.js`、`apps/classified-offline/js/ribbon-production.js`：兜底 Ribbon 错误提示改为中文。
- `tests/wps-phase-one.test.mjs`：新增 `DOCUMENT_MUST_BE_SAVED` 中文化和功能检测中文报告断言。
- `问题清单Agent.md`：新增 P019，规定稳定错误码不得裸露给用户。

### 自动验证

- `npm test`：通过，55 项 PASS。
- `npm run build:classified`：通过，Build ID `20260804162507-2c08c59b4a41`。
- `.venv\Scripts\python.exe -m pytest local-agent\tests\test_local_agent.py command-service\tests\test_command_service.py -q`：通过，29 项 PASS。

### 真实 WPS 状态

- 截图中的旧界面仍显示英文码，说明当前 WPS WebView 可能尚未加载最新构建，或该错误发生在构建更新前。
- 本轮代码已修复显示文案；若 WPS 仍显示旧英文错误，需要关闭全部 WPS 窗口后重新打开，让加载项重新读取最新构建。
- 重试前必须先在 WPS 中把当前文档保存为本地 `.docx` 文件；未保存文档、云端临时文档或非 DOCX 文档都会继续被拒绝。
- 本地服务已重新准备：3889 静态资源 PASS，9528 统一入口 PASS，debug 注册 PASS；session 已生成但不记录完整 ID。
- `/v1/version` 与 `/v1/handshake` 已确认：`docxtool 4.0`、`source-locator-v2`、`host-text-v1`，握手成功。

### 下一步

1. 重启/确认 3889 静态服务和 9528 统一本地服务。
2. 用户关闭并重新打开 WPS，确保加载最新插件页面。
3. 在 WPS 中先执行“另存为/保存”为本地 `.docx`。
4. 再点击“预览排版”或“一键排版”。
5. 若仍失败，界面应显示中文原因和“错误码：...”，后续按中文原因继续定位。

### Commit / Push

- 本轮未 commit、未 push。
- 工作区仍有大量既有改动和真实 DOCX，本轮不做清理、不做 reset。

## 2026-08-05 预览排版按钮无反应修复（最新）

### 当前结论

- 用户反馈“预览排版没有反应”时，服务检查显示 3889 静态资源、9528 统一入口和 debug 注册均为 PASS。
- `.runtime/e2e/current.json` 只记录到 `main_script_loaded`，没有 `host_router_installed`；这说明 WPS 主上下文没有成功安装 HostCommandRouter。
- 根因是 WPS 主脚本加载早于 `Application` 对象注入；旧代码只尝试安装一次，遇到 `WPS_APPLICATION_UNAVAILABLE` 后不再重试。任务窗格按钮只写入请求，没人消费，所以界面看起来没有反应。

### 实际改动

- `apps/classified-offline/src/host-runtime.ts`：
  - 增加 `tryInstall()` 定时重试，每 250 ms 检查 `Application` 和 BuildInfo。
  - 等待二者就绪后再安装 HostCommandRouter。
  - Node 测试环境中的重试定时器使用 `unref`，避免测试进程不退出。
- `apps/classified-offline/src/taskpane-workflow.ts`：
  - 点击按钮后立即显示“命令已发送，等待 WPS 主上下文处理…”。
  - 若请求仍在 `PluginStorage` 或主上下文正在处理，不再被旧状态轮询覆盖回“就绪”。
- `tests/wps-phase-one.test.mjs`：
  - 增加源码断言，确保 host runtime 保留重试安装和 `host_router_installed` 上报。
- `问题清单Agent.md`：
  - 新增 P020，记录任务窗格按钮无反应的触发场景、根因、处理方式和验证门槛。

### 自动验证

- `npm test`：通过，55 项 PASS。
- `.venv\Scripts\python.exe -m pytest local-agent\tests\test_local_agent.py command-service\tests\test_command_service.py -q`：通过，29 项 PASS。
- `scripts\verify-all.ps1`：通过，`VERIFY_ALL_PASS`。
- classified-offline Build ID：`20260804170637-14ff8696d914`。
- `scripts\e2e-classified.ps1 prepare`：静态资源 PASS、local-agent PASS、command-service PASS、debug registration PASS；session 已生成但不记录完整 ID。

### 真实 WPS 状态

- 当前已打开的 WPS 窗口仍可能停留在旧 WebView；prepare 后状态只上报到 `main_script_loaded`，还没有看到新版 `host_router_installed`。
- 因此必须关闭全部 WPS 窗口后重新打开文档，才能让 WPS 加载最新主脚本和重试安装逻辑。
- 重新打开后，点击“预览排版”应至少立即显示“命令已发送，等待 WPS 主上下文处理…”。若文档未保存，则应显示中文的“当前文档尚未保存”错误。

### 下一步

1. 用户关闭全部 WPS 窗口。
2. 重新打开本地 `.docx` 文档。
3. 打开任务窗格，确认构建短号接近 `20260804170637-14ff...`。
4. 点击“预览排版”。
5. 若仍无反应，先读取 `.runtime/e2e/current.json`，必须确认是否出现 `host_router_installed`；未出现则继续查 WPS 主上下文加载。

### Commit / Push

- 本轮未 commit、未 push。
- 没有修改识别 wheel、命令服务业务逻辑或真实 DOCX。

### 后续发布规则

- 用户已要求后续尽量使用快速发布。
- 默认只推送当前仓库 `D:\PycharmProjects\wps`。
- 快速发布仍固定排除真实 DOCX、`.idea`、`.runtime`、Token、日志和本地缓存。
- 默认执行一次必要门禁、一次敏感信息扫描和一次 `git push`，不再反复展开提交范围细节；除非发现风险文件或测试失败。

## 2026-07-31 wheel 恢复与本级 venv 重装（最新）

### 当前结论

- 已按用户要求恢复 wheel 使用链路，并改为固定使用 WPS 本级虚拟环境：`D:\PycharmProjects\docxtool\wps\.venv`。
- 上级 `D:\PycharmProjects\docxtool` 目录的源码、Git 状态和已有 wheel 文件未被修改；本轮只读取了上级 `dist\docxtool-1.4-py3-none-any.whl` 作为安装源。
- `docxtool 1.4` 已安装到 WPS 本级 `.venv`，导入路径为 `D:\PycharmProjects\docxtool\wps\.venv\lib\site-packages\docxtool`。
- 9528 统一服务已复用原 session token 重启，当前监听进程 PID 为 3248；3889 静态服务保持常驻，9529 不应恢复。

### 实际改动

- `local-agent\pyproject.toml`：依赖从 `docxtool==1.3` 修正为 `docxtool==1.4`。
- `scripts\verify-all.ps1`：Python 改为 `wps\.venv\Scripts\python.exe`，并移除上级 `..\src` 的 `PYTHONPATH` 注入。
- `scripts\e2e-classified.ps1`：Python 改为 `wps\.venv\Scripts\python.exe`，9528 启动时仅注入本仓库 `local-agent\src` 与 `command-service\src`。
- `scripts\acceptance-real-doc.ps1`：DOCX 只读检查改用 WPS 本级 `.venv`。
- `问题清单Agent.md`：新增 P015，防止 WPS 本级 wheel 被上级虚拟环境或上级源码路径绕过。

### 自动验证

- `wps\.venv` 中已安装：`docxtool==1.4`、`pytest==8.3.5`、`ruff==0.11.0`、`build==1.2.2.post1` 及 wheel 依赖。
- Python 导入检查 PASS：`docxtool` 来自 `wps\.venv\lib\site-packages`。
- 9528 真实识别接口 PASS：`005班子对照检查材料.docx` 返回 79 个 block，其中 78 个真实段落 locator verified，1 个 table block 按规则未验证/跳过。
- `scripts\verify-all.ps1` PASS：TypeScript typecheck、51 项 Node/协议测试、Python 测试、Ruff、classified/online 构建、addin verify 和安全扫描均通过。

### 真实 WPS 验收状态

- 本轮只恢复 wheel 与本级运行环境，并验证本机 9528 识别接口；没有重新执行真实 WPS 点击、保存后批注 OOXML 或一键排版阻断验收。
- 因此真实 WPS 保存后验收状态保持未通过/待复验，不能写为 PASS。

### 未解决问题

1. 真实 WPS 中重新执行“预览排版”，用 WPS 文档对象保存后检查 `comments.xml`。
2. 真实 WPS 中重新执行“一键排版”，确认稳定返回 `MIXED_PARAGRAPH_REQUIRES_SPLIT` 且零格式写入。
3. 后续若要提交 GitHub，只处理本目录 `D:\PycharmProjects\docxtool\wps` 对应仓库，不再提交上级 `docxtool` 仓库。

### 下一步严格顺序

1. 保持 3889/9528 常驻，不重新 prepare、不轮换 token。
2. 用户在 WPS 中加载真实文档后，先点“预览排版”。
3. 由自动检查链读取保存后 OOXML，确认批注数量、角色颜色、正文 SHA-256、段落数量和顺序。
4. 再执行“一键排版”阻断验收，确认混合段落阻断和零写入。
5. 每轮结束继续更新 `问题清单Agent.md` 和本交接文档。

### 生成文件路径

- 本级虚拟环境：`D:\PycharmProjects\docxtool\wps\.venv`
- 已安装 wheel 来源（只读）：`D:\PycharmProjects\docxtool\dist\docxtool-1.4-py3-none-any.whl`
- 交接文档：`D:\PycharmProjects\docxtool\wps\交接文档.md`
- 问题清单：`D:\PycharmProjects\docxtool\wps\问题清单Agent.md`

### Commit / Push

- 本轮未 commit、未 push。
- 当前改动仅在 WPS 仓库内；真实 DOCX、`.runtime`、`.idea` 和构建产物仍不提交。

## 2026-07-31 GitHub 双仓库发布（最新）

### 当前结论

- 用户已明确要求把所有代码提交到 GitHub。本轮发布范围是两个独立仓库：WPS 插件 `7654321x/wps--plugin`，以及 wheel/SDK 根项目 `7654321x/docxtool`。
- 真实公文、识别预览工作副本、正式阻断工作副本、`.runtime`、session token、IDE `.idea` 和构建产物均明确排除；只提交源码、合同、测试、文档、部署配置和已经验证为脱敏内容的自动验收 fixtures。
- 真实 WPS 保存后 OOXML 验收仍未完成，不因本次代码发布改写为 PASS。

### 实际改动

- WPS 仓库：提交物理段落 locator、RecognitionResult 1.1、严格 WPS 适配器、子 Range 批注、表格跳过、混合段落阻断、跨文档 tracker 清理、统一 9528 服务、健康检查、E2E 与 OOXML 检查、任务窗格提示和问题清单。
- 根项目：提交 docxtool 1.4 识别引擎、SDK、物理段落锚点、认证与安全加固、部署入口、前端配置、批量测试工具、依赖锁、文档和完整回归测试。
- 发布审计新增 P014，防止 PowerShell 连续外部命令用最后一个成功退出码掩盖前序测试失败。

### 自动验证

- WPS：`npm run verify:all` PASS；Node/协议共 51 项 PASS；Python 服务测试 PASS；TypeScript、classified/online 构建和加载项检查 PASS。
- 根项目：项目 Python 3.8 `.venv` 全量 pytest PASS；Node 11 项 PASS；Ruff PASS；sdist/wheel 构建 PASS。
- 两个仓库 `git diff --cached --check` PASS；高风险密钥扫描未发现私钥、GitHub token、OpenAI key、AWS key 或真实 WPS session token。根项目命中的 `ADMIN_TOKEN`/`PROXY_SECRET` 均为 `.env.example`、文档和测试中的明确占位符。
- 发布完成后发现常驻端口已停止，已按 P009 复用现有 token 恢复服务：3889 PID 29496、9528 PID 33092，9529 未监听；prepare 产生的 build-info 和 fixture 时间戳改动已恢复到已提交版本，工作树保持干净。

### 真实 WPS 验收状态

- 即时子 Range 批注曾在真实 WPS 中显示，但首次保存后 OOXML 没有批注，因此保存持久化仍为 FAIL/待复验。
- 跨文档旧 tracker 的 `DOCUMENT_CHANGED` 已修复并自动测试通过；真实新版的 `MIXED_PARAGRAPH_REQUIRES_SPLIT` 阻断仍待恢复验收。
- 本次 GitHub 发布不包含任何真实文档或验收副本。

### 未解决问题

1. 重新执行真实 WPS 预览并通过 `Document.Activate() → Save()` 保存，关闭后检查 78 条批注、19 个混合物理段落和 0 个表格单元格批注。
2. 真实复验一键排版返回 `MIXED_PARAGRAPH_REQUIRES_SPLIT`，并证明 command-service、transaction、executor 零写入。
3. 单独处理 WPS 宿主缓存旧 Ribbon 中“打开任务窗格”的问题；仓库与 3889 当前 XML 已只有三个按钮。

### 下一步严格顺序

1. 完成两个仓库暂存文件的最终敏感信息审计。
2. 先创建 WPS 与根项目代码提交，再分别推送到各自 GitHub 远端。
3. 回写实际 commit hash 和 push 状态，并再提交一次 WPS 交接文档。
4. 保持 3889/9528 常驻，不重新 prepare、不轮换 token。
5. 下一轮从保存后 OOXML 真实复验继续，不重复已通过的协议与定位实现。

### 生成文件路径

- WPS 交接文档：`D:\PycharmProjects\docxtool\wps\交接文档.md`
- WPS 问题清单：`D:\PycharmProjects\docxtool\wps\问题清单Agent.md`
- 预览验收副本（未提交）：`D:\PycharmProjects\docxtool\wps\tests\005班子对照检查材料-定位修复验收工作副本.docx`
- 正式阻断副本（未提交）：`D:\PycharmProjects\docxtool\wps\tests\005班子对照检查材料-正式排版阻断工作副本.docx`

### Commit / Push

- WPS 代码提交：`6016154c0c0e1a73c410ffbb5639441fdc820fc9`（`feat: harden WPS recognition and formatting workflow`）。
- 根项目代码提交：`872221980aeb143d21fa6986d8c46296463756be`；为保留远端已经发布的 1.1—1.4 历史，最终使用双亲合并提交 `13fbc17b52dc9dd3458c581cc34ee5cb0b361e3a`。
- GitHub push：PASS。`7654321x/wps--plugin` 远端 `main` 已包含产品代码提交 `6016154c0c0e1a73c410ffbb5639441fdc820fc9` 及其后的交接状态文档提交；`7654321x/docxtool` 远端 `main` 已指向 `13fbc17b52dc9dd3458c581cc34ee5cb0b361e3a`。
- 交接状态回写提交不包含产品代码或真实文档。

## 2026-07-30 物理定位修复实施与真实 WPS 暂停点（最新）

### 当前结论

- wheel/SDK 物理段落锚点、UTF-16 子范围、RecognitionResult 1.1、严格 WPS 定位、表格跳过、混合段落子 Range 批注和正式执行前阻断均已实现。
- WPS 端已经删除 dense/non-empty/裸 `source_paragraph_index` fallback。无法证明位置的 block 进入 `unresolved_blocks`，不生成猜测目标。
- 本机真实 `005班子对照检查材料.docx` 识别服务结果：79 个总 block、78 个段落 block、78 个 locator verified、0 个 unverified、19 个混合物理段落。
- 真实 WPS 第一次预览即时成功，界面可见不同角色颜色和子 Range 批注，任务窗格显示 19 个混合物理段落；但首次保存后 OOXML 中没有批注，因此该轮保存持久化明确为 **FAIL**，不能写成真实 PASS。
- 随后发现两个独立问题：任务窗格获得焦点时 `Ctrl+S` 未保存 WPS 文档；切换到第二个文档后旧预览 tracker 导致 `DOCUMENT_CHANGED`。后者已经修复并构建，前者已确定后续必须使用 WPS 文档对象 `Activate → Save`。
- 用户要求暂停。当前 WPS 打开的是 `005班子对照检查材料-定位修复验收工作副本.docx`；新版构建已加载，文档已通过 WPS 对象保存为 `Saved=true`，但还没有重新点击预览。任务窗格仍显示上一次的 `DOCUMENT_MUST_BE_SAVED`，这是保存前的旧状态。

### 实际改动

- 根 wheel：`src/docxtool/document/importer.py`、`src/docxtool/sdk/models.py`、`recognition.py`、`cli.py` 增加物理段落索引、完整物理文本哈希、物理出现序号、UTF-16 起止偏移、locator verified 和可选本机 `recognized_text`。
- WPS 合同：RecognitionResult 升级为 1.1；增加严格 locator、`mixed_structure`、`formatting_disposition`、脱敏 `unresolved_blocks`；CommandRequest 仍为 1.0，FormattingCommandSet 仍为 1.1。
- WPS 适配器：只接受“完整物理段落哈希 → physical occurrence → UTF-16 子范围 → 子范围哈希 → 可选 recognized_text”全部一致的目标；表格段落不参与候选。
- 预览：按 `target_id` 匹配命令并对混合角色建立独立子 Range 批注；任务窗格显示混合物理段落数量和中文阻断说明。
- 正式用例：unresolved、mixed/review-only 在 command-service、transaction、executor 之前阻断。
- 宿主切换：`composition-root.ts` 暴露唯一 `previewTracker`；`HostCommandRouter.reconcileActiveDocument()` 切换文档时只清 tracker，不删除旧文档批注。
- 测试新增：locator 缺失不猜测、重复物理段落 occurrence、表格无批注、预览 unresolved/mixed 摘要、命令服务 1.1 locator 强校验、跨文档 tracker 清理。

### 自动验证

- 父项目目标测试：`test_sdk.py + test_processing_flags.py + test_importer_heading_flow.py` 共 18 项 PASS；相关 Ruff PASS。
- WPS 在 tracker 修复前执行 `npm run verify:all`：协议/Node 共 50 项 PASS、Python 服务共 28 项 PASS、类型检查、双构建、加载项检查均 PASS。
- tracker 修复后执行：TypeScript build PASS、`wps-phase-one.test.mjs` 49 项 PASS、classified build PASS、`git diff --check` PASS。
- tracker 修复后的完整 `npm run verify:all` 尚未重新执行，下一步必须补跑。
- 9528 统一服务已使用原 token 重启并加载新 wheel，当前 PID 2960；3889 静态服务 PID 4276；9529 未监听。
- 最新 classified build id：`20260730161533-8a0432a16629`。

### 真实 WPS 验收状态

- WPS 启动：PASS。
- 真实文档工作副本打开：PASS。
- 新 locator 的 WPS 即时子 Range 批注：第一次运行界面可见，但保存后检查 FAIL，故整体仍为 NOT PASS。
- 保存后正文检查（第一次运行）：正文 SHA-256 前后均为 `f704d41938be94bc3f3f1cdceaa9a4f73bd1efd9a953b14ebd83f8a0e19a6af0`，95 个 OOXML 段落、段落顺序和 1 个表格保持一致；批注数量为 0，未达到门槛。
- 正式一键排版阻断：第一次因跨文档旧 tracker 返回 `DOCUMENT_CHANGED`；代码已修复，真实新版复验尚未运行。预期正确结果必须是 `MIXED_PARAGRAPH_REQUIRES_SPLIT`，且格式和正文零写入。
- 用户最终视觉验收：未开始。

### 未解决问题

1. 必须在当前已保存的预览工作副本上重新点击“预览排版”，等待完成后用 WPS 对象 `Activate → Save`，确认 `Saved=true`，再正常关闭并检查 OOXML。
2. 保存后必须验证 78 条 DocxTool 批注、19 个多批注物理段落、0 个表格单元格批注、角色作者计数、全部子 Range 文本哈希匹配 wheel、正文哈希和段落顺序不变。
3. 打开正式阻断副本，等待宿主 reconcile 清掉旧 tracker，再点击“一键排版”；必须得到 `MIXED_PARAGRAPH_REQUIRES_SPLIT`，不能再出现 `DOCUMENT_CHANGED`。
4. WPS 当前仍显示旧缓存 Ribbon 中的“打开任务窗格”，而仓库和 3889 的 `ribbon.xml` 已只有三个按钮。此项需在不影响主验收的前提下单独查 WPS Ribbon 缓存；不能误改当前正确 XML。
5. 完成上述真实复验后再评估剩余 wheel 分类错误；定位未完整 PASS 前不调整分类器。

### 下一步严格顺序

1. 不重新 prepare、不轮换 token；确认 3889/9528 继续健康。
2. 在当前 WPS 文档重新点击“预览排版”，等待任务窗格显示 78 条批注和 19 个混合物理段落。
3. 通过 WPS 文档对象执行 `Activate → Save`，确认 `Saved=true`，正常关闭该文档。
4. 只读检查保存后的 DOCX/OOXML；若无 `comments.xml` 或任一子 Range 不匹配，先查保存链，不能标 PASS。
5. 打开 `005班子对照检查材料-正式排版阻断工作副本.docx`，等待文档切换 reconcile，再调用 Ribbon 相同正式入口。
6. 验证稳定错误码为 `MIXED_PARAGRAPH_REQUIRES_SPLIT`，command-service/transaction/executor 零调用，保存后正文和格式未变化。
7. 补跑 `npm run verify:all`、父项目相关测试、Ruff 和两个仓库 `git diff --check`。
8. 更新本交接文档与问题清单；仅在全部保存后检查通过时，才把真实 WPS 能力标为 PASS。

### 生成文件路径

- 原始真实文档（只读，不得覆盖）：`D:\PycharmProjects\docxtool\wps\005班子对照检查材料.docx`
- 预览验收工作副本：`D:\PycharmProjects\docxtool\wps\tests\005班子对照检查材料-定位修复验收工作副本.docx`
- 正式阻断工作副本：`D:\PycharmProjects\docxtool\wps\tests\005班子对照检查材料-正式排版阻断工作副本.docx`
- 定位诊断目录：`D:\PycharmProjects\docxtool\wps\.runtime\diagnosis\005-preview`
- 本交接文档：`D:\PycharmProjects\docxtool\wps\交接文档.md`

### Commit / Push

- 本轮未 commit、未 push。
- 两个仓库均有大量既有用户改动和未跟踪文件；禁止 reset、restore、clean，后续继续按文件范围审计。

## 2026-07-30 物理锚点与子 Range 修复计划（最新）

### 当前结论

- 已按用户指定顺序形成实施计划；本轮仅规划，不修改产品代码和测试 DOCX。
- 修复主线固定为：wheel/SDK 物理锚点 → 删除裸序号 fallback → 无证据复核/表格跳过 → 混合段落子 Range 批注 → 正式排版前阻断 → 再评估 wheel 分类。

### 计划中的合同决定

- 不再复用语义含混的逻辑 `source_paragraph_index`。新增版本化 `SourceRangeLocator`：主文档 story、物理段落完整文本 SHA-256、物理段落出现序号、逻辑块文本 SHA-256、UTF-16 块内起止偏移、前后块哈希和可选本机明文。
- 偏移单位固定为 UTF-16 code unit，范围不包含 Word/WPS 段落结束标记；Python 与 TypeScript 使用同一换行规范化算法并共享 fixtures。
- 本机 `recognized_text` 只用于 WPS Range 定位和可视批注，完成定位后不进入 CommandRequest、command-service、日志、报告或 PluginStorage。
- 表格 block 在没有正式表格合同前不产生普通段落目标；WPS 表格单元格不得使用相邻 wheel block 或裸序号猜测角色。
- 一个物理段落含多个角色时，每个逻辑块可生成子 Range 预览批注，但该物理段落标记 `review_only / mixed_structure`；正式一键排版在事务开始前返回 `MIXED_PARAGRAPH_REQUIRES_SPLIT`，不得任选一个角色覆盖整段。

### 下一步严格顺序

1. 在根 wheel 的 importer 保留原始物理段落身份和每个拆分逻辑块的 UTF-16 起止偏移；SDK 模型和本机识别响应输出新 locator。
2. 冻结 Python/TypeScript locator 合同、Schema 与共享脱敏 fixtures；旧 wheel 无 locator 时只允许复核，不得兼容裸序号。
3. WPS adapter 实现完整物理哈希 → 子范围偏移 → 子范围哈希的三重校验，并删除 dense/non-empty/裸 index fallback。
4. preview comment service 支持子 Range；无法验证 locator 时输出“需要复核”，表格单元格跳过。
5. application use case 在任何写入和事务开始前检测 mixed structure，返回稳定阻断码和需要拆段的物理段落数量。
6. 使用脱敏同结构 fixture 自动验证手工换行、内联标题+正文、重复文本、空段、49 个表格段落和尾部附件混合结构。
7. 在真实 WPS 中执行预览、保存和 OOXML 检查：子 Range 批注锚点正确、表格单元格无伪角色、用户批注保留、正文 SHA-256 不变；再验证一键排版在混合段落前零写入阻断。
8. 只有全部定位证据 PASS 后，才生成 wheel 分类误差清单并调整真正的分类规则。

### 预计修改位置

- 根 wheel：`src/docxtool/document/importer.py`、`src/docxtool/sdk/models.py`、`src/docxtool/sdk/recognition.py` 及对应 tests。
- WPS 合同：`packages/contracts/src/index.ts`、`schemas/recognition-result.schema.json`、共享 fixtures、command-service validation。
- WPS 定位与预览：`packages/recognition-client/src/index.ts`、`packages/wps-adapter/src/official-host.ts`、`packages/wps-adapter/src/preview-comments.ts`。
- 应用流程：`packages/application/src/format-document-usecase.ts`、真实 WPS E2E 与 OOXML 检查脚本。

### 验收状态与 Commit / Push

- 当前仅计划完成；真实 WPS 修复验收尚未开始，状态不得写 PASS。
- 本轮未 commit、未 push。

## 2026-07-30 `005` 识别错位归因审计（最新）

### 后续设计决定：允许本机 wheel 返回定位文字

- 用户确认可考虑让本机 wheel 返回“某段文字属于某角色”。该方向能直接解决逻辑块哈希无法与整段 WPS 文本匹配的问题，但文字本身不能唯一解决重复文本、表格单元格和同一物理段落内多角色定位。
- 推荐最小返回字段为：`recognized_text`、原始物理段落稳定锚点、`start_offset`、`end_offset`、`occurrence_index`、前后块哈希和识别角色。
- 明文只允许在 `local-agent → 当前 WPS 宿主` 的本机内存链路中短暂传递；不得进入 command-service、FormattingCommandSet、日志、报告、PluginStorage 或任何网络服务。WPS 完成 Range 定位后仍向命令服务发送现有脱敏哈希合同。
- SDK 对外默认继续 text-free；本机离线入口可显式启用 `include_text`，避免联网版或第三方调用意外获得正文。
- 实施时必须同时移除裸序号 fallback；仅增加文字字段而保留旧 fallback，仍会在重复文字和匹配失败时产生错误批注。

### 当前结论

- 用户看到的“大量识别错误”主要不是 wheel 分类规则错误，而是 wheel 逻辑块与 WPS 物理段落的定位合同错误。
- 保存后的 DOCX 包含 46 个正文物理段落和 49 个表格单元格段落，共 95 个 OOXML 段落，其中 50 个非空；预览恰好生成 50 条批注，说明当前实现对每个非空 WPS 段落都尝试了分类。
- wheel structural importer 把手工换行、内联“标题。正文”和附件尾部拆成 79 个逻辑块，并把整张表表示为一个 `__table__` 结构块。19 个正文物理段落对应多个 wheel 逻辑块，其中 18 个物理段落包含多个不同角色。
- 当前 `source_paragraph_index` 是扁平逻辑块序号。适配器只有 33 个块能按完整段落哈希对齐；其余块使用裸序号 fallback，错误地对齐到 WPS 物理段落或表格单元格。
- 典型证据：wheel 正确把第一个物理段落拆为主标题、标题续行和职务姓名，但 WPS 把“主标题续行”批注落到第二个物理段落的“一级标题+导语”；wheel 正确识别“（六）……”为二级标题、“5.测试”为三级标题，WPS 却把整个物理段落标为正文；表格题注 wheel 已识别为 `__object_caption__`，保存的批注仍是正文。
- wheel 对可独立定位的普通段落、一级标题、二级标题、三级标题、四级标题、落款和附件角色总体符合文档语义。当前错位证据污染了 UI 结果，不能先去修改 wheel 分类规则。

### 实际改动

- 产品代码与用户 DOCX 均未修改。
- `问题清单Agent.md` 新增 P011，固定禁止逻辑序号到物理段落序号的 fallback，并规定物理锚点、子范围偏移、表格跳过和混合结构复核要求。
- 本交接文档记录诊断证据和下一步顺序。

### 自动验证

- 使用文档结构检查器提取 50 条保存后批注；全部批注均可在 OOXML 中找到。
- 完整渲染并人工检查 8 页 PNG，确认文档包含大量手工换行、内联标题/正文、表格和附件混合结构，未发现渲染失败。
- 本地 wheel 重新识别：79 个逻辑 block；原文件损坏关系 `Target="../NULL"` 由 wheel 仅在任务临时副本中修复，原件未改。
- 结构计数：正文物理段落 46、表格段落 49、总段落 95、非空段落 50；正文物理段落中 19 个承载多个逻辑块、18 个承载多个角色。
- 当前文件 SHA-256：`15CDAB427DB4941D03284AA4DF16B252A5AA0AA4E9739E7692D0315D01E1F90D`。

### 真实 WPS 验收状态

- 当前保存后的真实 WPS 批注已经证明定位错误，因此本轮识别预览为 FAIL，不能作为 wheel 分类质量 PASS。
- 尚未实施修复，也未重新生成新的真实 WPS 预览副本。

### 未解决问题与下一步严格顺序

1. 在 wheel/SDK 合同中区分 `block_index`、原始物理段落锚点和物理段落内字符偏移。
2. 移除 `LocalWheelRecognitionProvider` 的裸 `source_paragraph_index` fallback；无法证明锚点时返回复核/跳过，不得猜测。
3. 为一个物理段落内的多个逻辑块实现子 Range 预览；正式段落格式在未实现安全拆段前标记为混合结构并拒绝整段覆盖。
4. 表格单元格在正式表格合同完成前不参与普通段落识别预览。
5. 增加 `005` 的脱敏同结构 fixture 与 46/49/79/95 粒度回归测试，再运行真实 WPS 预览和保存后 OOXML 检查。
6. 完成上述定位修复后，再单独评估剩余的真正 wheel 分类误差；现在不应先调 wheel 规则。

### 生成文件路径

- 被审计工作副本：`D:\PycharmProjects\docxtool\wps\tests\005班子对照检查材料-识别预览工作副本.docx`
- 本地诊断批注 JSON：`D:\PycharmProjects\docxtool\wps\.runtime\diagnosis\005-preview\comments.json`
- 内部渲染检查目录：`D:\PycharmProjects\docxtool\wps\.runtime\diagnosis\005-preview\render`
- 交接文档：`D:\PycharmProjects\docxtool\wps\交接文档.md`

### Commit / Push

- 本轮未 commit、未 push。

## 2026-07-30 预览 400 修复与本机服务单端口收敛（最新）

### 当前结论

- 截图中的 `COMMAND_SERVICE_400` 已定位：真实 wheel 结果包含带段落索引的 `__object_caption__`，旧 wheel→协议适配器没有把它转换为冻结协议支持的 `caption`，命令服务因此以 `INVALID_SCHEMA / unknown recognized type` 拒绝请求。
- 唯一合同边界现已增加 `__object_caption__ → caption`；无段落索引的 `__table__` 仍不进入段落合同，没有在 executor、UI 或命令服务中增加旧字段 fallback。
- 本机 WPS 业务链已从 9528+9529 收敛为单端口 9528：`/v1/recognize` 与 `/v1/commands` 共用一个常驻进程；命令路由直接复用正式 command-service 核心，没有另写命令生成器。
- 旧 9529 已停止监听；当前 9528 统一服务 PID 为 6596，静态加载项 3889 继续常驻。

### 实际改动

- `packages/recognition-client/src/index.ts`：新增 wheel 对象题注到冻结 `caption` 角色的映射。
- `packages/command-service-client/src/index.ts`：解析命令服务错误 JSON，优先显示稳定错误码，不再把所有 400 折叠成 `COMMAND_SERVICE_400`；非 JSON 响应仍有安全回退码。
- `local-agent/src/docxtool_local_agent/app.py`：统一提供正式识别和正式命令接口；命令请求仍执行冻结协议校验和 profile 命令构建。
- `local-agent/src/docxtool_local_agent/e2e.py`：只读链路直接调用同一正式命令核心，避免单线程服务自调用死锁；同步对象题注映射。
- `scripts/e2e-classified.ps1`、运行时配置、状态与验收脚本：WPS 的两个客户端端点均指向 9528，不再启动 9529 服务进程。
- `AGENTS.md`、`问题清单Agent.md`、WPS/诊断/实机文档：固定单业务端口规则，并新增 P010 防复发项。

### 自动验证

- `npm test`：44 项 PASS；新增对象题注适配和稳定 400 错误码测试。
- Python command-service + local-agent：26 项 PASS；新增同端口 `/v1/commands` 路由测试。
- `npm run verify:all`：PASS，包括 typecheck、Node/Python、Ruff、安全扫描、classified/online 构建和 add-in verify。
- 统一服务只读 E2E：PASS，125 条正式命令；没有发生服务自调用死锁。
- 真实 `005` wheel 脱敏计划合同验证：79 个 block、78 个段落合同、309 条正式命令、0 warning，统一 9528 接口返回 PASS。
- 端口检查：9528 正常监听；9529 无监听；`e2e:classified:status` 的静态资源、本地统一服务、命令接口与 debug 注册均为 PASS。

### 真实 WPS 验收状态

- 本轮尚未在重新加载后的真实 WPS WebView 中再次点击“预览排版”，因此真实 `005` 批注预览保持待用户验收，不能写为 PASS。
- 当前已打开的旧 WebView 仍可能缓存旧构建和 9529 配置；必须关闭 WPS 后重新打开一次工作副本，才能加载本轮单端口构建。token 没有轮换。
- 保存后的 DOCX/OOXML 尚无本轮新证据；此前脱敏 fixture 的真实 WPS E2E 结论不受影响。

### 未解决问题与下一步严格顺序

1. 用户关闭并重新打开 WPS 一次，打开 `tests\005班子对照检查材料-识别预览工作副本.docx`。
2. 用户只点击一次“预览排版”，检查对象题注和各角色批注是否出现；无需再启动任何服务。
3. 保存工作副本后自动读取 DOCX/OOXML，确认 DocxTool 批注、角色作者和正文完整性，再决定真实预览是否 PASS。
4. 若仍失败，界面现在会显示真实稳定错误码，按该码修复唯一合同边界，不恢复 9529，也不增加 WPS 端分类器。

### 生成文件路径

- 真实文档工作副本：`D:\PycharmProjects\docxtool\wps\tests\005班子对照检查材料-识别预览工作副本.docx`
- 脱敏 wheel 计划：`D:\PycharmProjects\docxtool\wps\.runtime\e2e\latest-real-recognition.json`
- 常驻服务配置：`D:\PycharmProjects\docxtool\wps\apps\classified-offline\ui\e2e-session.js`
- 交接文档：`D:\PycharmProjects\docxtool\wps\交接文档.md`

### Commit / Push

- 本轮未 commit、未 push。

## 2026-07-30 识别批注精简收口（最新）

### 2026-07-30 真实文档错位与角色颜色修复

- 真实文件只读审计：`2021-2026区政协工作总结(2).docx` 共 23 个 Word 段落，其中 22 个非空段落；wheel 原始结果为 `title=1`、`title_cont=1`、`heading1=7`、`body=13`。
- 根因确认：wheel 原始结果把标题后的长段落识别为 `body`，并未识别成标题续行；WPS 端因空段导致裸序号整体错位，才出现正文/一级标题互换。
- `LocalWheelRecognitionProvider` 已优先使用 wheel 的 `text_sha256` 哈希锚点做单调映射；无哈希旧 wheel 才使用非空段落序号兼容。
- 预览批注按角色设置官方 `Comment.Author`：主标题、续行、一至四级标题、正文及特殊类型由 WPS 分配不同颜色；没有修改正文高亮、字体颜色或底纹。
- Node/TypeScript：42 项 PASS；Python 26 项、Ruff、涉密版/在线版构建与加载项检查 PASS。
- 真实 WPS 脱敏会话：`ca16c58209a445c0bd3a57703c0acaec`，总体 `CLASSIFIED_THREE_ACTIONS_PASS`；保存后确认 7 个不同 `DocxTool·` 角色作者，预览字段完整、用户批注保留、最终清理成功。
- 真实文档原件未修改；工作副本 SHA-256 与原件一致：`D:\PycharmProjects\docxtool\wps\tests\2021-2026区政协工作总结-识别预览工作副本.docx`。
- WPS 已打开该工作副本。真实文档的颜色和分类视觉验收等待用户点击一次“预览排版”，不得在验收前写成真实文档 PASS。
- Commit / Push：未执行。

### 2026-07-30 `005班子对照检查材料.docx` 无法识别诊断

- 没有文件名白名单，也不限制只能识别某份文档；要求仅为已保存、本机存在、扩展名为 `.docx`。
- 原文件 ZIP 结构完整，SHA-256：`C1F69E83290FFD9F19E1CAE3BDD082C91020F1AE6F9BFA744C08FF09C9533F01`。
- wheel 直接识别 PASS：79 个 block、5 个 review item。
- 文件包含损坏关系 `Target="../NULL"`；wheel 会自动修复任务临时副本，原文件不变，因此这不是当前阻塞。
- 实际失败为服务重启后旧 WPS WebView 继续使用旧 token，返回 `UNAUTHORIZED`。已完整重启 WPS 并打开工作副本。
- 常驻服务状态：静态资源 PASS、local-agent PASS、command-service PASS、debug registration PASS；当前 session `ccedbb4436d945c99c706c26251a8b9b`。
- 工作副本：`D:\PycharmProjects\docxtool\wps\tests\005班子对照检查材料-识别预览工作副本.docx`，初始 SHA-256 与原件一致。
- 用户要求本地服务长期开启；除非异常或构建重载，不再停止或重复 prepare。
- `UNAUTHORIZED` 二次复发根因已修复：旧脚本每次生成新 token，却在服务健康时不重启服务。现已使用 `.runtime/e2e/session-token.txt` 持久化并做鉴权探测。
- 连续两次 prepare 自动验证：`TokenStable=True`，local-agent、command-service、wpsjs 三个 PID 均保持不变。
- 当前常驻 session：`b81372cf55e04cef89a82ddb6197b062`；静态资源、local-agent、command-service、注册均 PASS，WPS host 已上报。
- WPS 已完整重启并重新打开 `tests\005班子对照检查材料-识别预览工作副本.docx`，等待用户再次点击“预览排版”做真实文档视觉确认。

---

### 当前结论

识别预览批注已去除 session、文档身份、段落索引、anchor、Range 和内部识别代码。可见内容由有序字段数组生成，压缩为三行；每行项目之间一个半角空格。主标题使用“二号”，固定行距显示“28 磅”，段前/段后显示“行”。真实 WPS 自动验收最终状态为 `CLASSIFIED_THREE_ACTIONS_PASS`。

### 实际改动

- `packages/wps-adapter/src/preview-comments.ts`：字段数组、中文字号、中文单位、三行紧凑展示；所有权改为预览前用户批注稳定指纹与新增批注联合判断。
- `packages/application/src/format-document-usecase.ts`：批注保存导致的运行节点重组不再误报正文已变化；正文哈希、段落数、段落顺序、节数和文档身份仍严格校验。
- `apps/classified-offline/src/host-runtime.ts`、`scripts/e2e-classified.ps1`：预览和最终保存稳定后再生成/检查证据，预览 0 条不再假 PASS。
- `scripts/inspect-one-click-ooxml.py`：识别无内部 marker 的精简批注，并检查 15 个必要字段完整。
- 新增 `问题清单Agent.md` 并写入 `AGENTS.md` 强制规则，沉淀内部信息泄露、WPS 保存时序、旧宿主缓存、批注 Range/日期重算、单位混用等防复发项。

### 自动验证

- TypeScript/Node：41 项 PASS。
- Python：26 项 PASS；Ruff PASS。
- 涉密版与在线版构建、加载项清单检查：PASS。
- 真实 WPS 会话：`3963a138579d429a943f337fcfae7f79`。
- 功能检测、预览排版、一键排版、事务回滚、保存后格式：全部 PASS。
- 正文 SHA-256、段落数量、段落顺序：全部不变。
- 预览 DocxTool 批注存在，用户批注保留；最终 DocxTool 批注为 0，用户批注仍保留。
- 保存后批注字段检查：28/28 条 DocxTool 批注均包含全部 15 个必要字段；无孤立批注定义。

### 真实 WPS 验收状态

自动真实宿主验收：PASS。用户只需按需查看脱敏预览副本的视觉密度；不需要重复执行开发测试流程。

### 未解决问题

- WPS 会自动折行；三行字段组在窄批注栏中仍会产生视觉换行，这是宿主布局，不是额外字段。
- 当前未改用连字符分隔；空格在真实 WPS 中显示正常。若后续视觉验收要求，可将组内连接符统一改为 ` - `。

### 下一步严格顺序

1. 进入真实文档识别阶段：先对真实 DOCX 创建工作副本，原件只读保留。
2. WPS 正式入口读取已保存工作副本，经 loopback local-agent 调用 `docxtool.sdk.recognize_docx(..., recognition_mode="legacy")`。
3. 第一轮只生成识别结果和预览批注，不执行一键排版；由用户检查主标题、一至四级标题、正文、称呼、附件和落款识别。
4. 记录误识别段落类型及 wheel 原始类型，修复应落在 wheel 或唯一角色适配边界，不能在 WPS 端另写识别器。
5. 识别收口后才对工作副本执行一键排版，并更新本交接文档。

### 生成文件路径

- 原始脱敏副本：`D:\PycharmProjects\docxtool\wps\.runtime\reports\3963a138579d429a943f337fcfae7f79\01-original-test-copy.docx`
- 预览批注副本：`D:\PycharmProjects\docxtool\wps\.runtime\reports\3963a138579d429a943f337fcfae7f79\02-preview-comments.docx`
- 最终排版副本：`D:\PycharmProjects\docxtool\wps\.runtime\reports\3963a138579d429a943f337fcfae7f79\03-final-format.docx`
- 脱敏报告：`D:\PycharmProjects\docxtool\wps\.runtime\reports\3963a138579d429a943f337fcfae7f79\one-click-e2e.json`

### Commit / Push

未执行 commit，未执行 push。

---

## 1. 当前结论

WPS 一键排版的正式执行链已经存在并可在真实 WPS 中写入；此前的“通过”结论不可靠，因为它只检查了写入、回滚和正文不变，未检查保存后的具体格式值。

本轮已补齐保存后 DOCX 的严格检查，并发现、修复了若干识别与自动化入口问题。**当前真实 WPS 最终验收仍为失败，不能宣称完成。**

最新失败会话：`ffa0e3ba793d4a4cb89a78f6f0363ac0`  
稳定错误码：`DOCUMENT_MUST_BE_SAVED`

这意味着隐藏 E2E 入口已经被调用，但其开始时 WPS 当前活动文档未处于可读取的已保存状态。必须先在代码中确认活动文档就是当前会话的测试副本，并由受控测试流程保存该副本，再执行正式用例；不要要求用户手工点击“保存”。

## 2. 已完成的内容

- 冻结 `FormattingCommandSet 1.1`，并使用唯一的旧命令适配边界。
- 正式写入链统一为 `FormatDocumentUseCase → WpsApiDocumentExecutor`；任务窗格没有直接写入 `Font`、`ParagraphFormat` 或 `PageSetup`。
- 真实 WPS 写入、读回、事务回滚和脱敏 fixture 已实现。
- 生成了无用户正文的多节测试 DOCX，包含主标题、一至四级标题、正文、混排、重复/空段、纵横分节和页面设置场景。
- 新增 `scripts/verify-one-click-format.py`：保存后以 `python-docx` 与 OOXML 读取字体、字号、粗体、对齐、字符缩进、固定行距和大纲级别；检查结果不含正文。
- 新增 `tests/fixtures/one-click-format-expectations.json`：定义 fixture 各段的目标 profile。
- E2E 验收脚本已把“保存后格式检查”纳入 PASS 条件；任一属性不符会返回 `SAVED_DOCX_FORMAT_MISMATCH`。
- 修复 WPS 启动器：自动选择实际安装的最新 WPS，而不是旧版后台宿主。
- 修复识别适配：wheel 的 `title`、`title_cont`、`sign_org`、`sign_date` 等旧角色映射为 WPS 合同角色；补齐分节后被 wheel 遗漏的末段为正文。
- 修复命令服务：`title_continuation` 使用与 `main_title` 相同的默认 profile。
- 自动格式测试函数现为 `window.DocxtoolAutomaticFormatTest()`；可由隐藏 E2E 任务窗格直接调用，页面按钮仅为人工备用入口，不应再用鼠标脚本点击。

## 3. 已验证结果

### 自动测试

- Python：`local-agent` 与 `command-service` 测试通过（25 项）。
- TypeScript 构建通过。
- Node 测试通过（最近一次 24 项）。
- 基线无格式 DOCX 会被保存后格式检查器正确判为 `FAIL`，因此检查器不会再把未写入的文档误报为成功。

### 真实 WPS 观察

- 真实 WPS 中曾成功完成一次正式写入、读回、回滚和保存，界面可见主标题、标题层级和正文格式变化。
- 保存后检查暴露：主标题续行没有 profile 映射，且 wheel 漏掉分节后的最后一段；相应代码已修复。
- 修复后启动新会话时，隐藏 E2E 入口报告 `DOCUMENT_MUST_BE_SAVED`。这份新工作副本尚未进入正式写入阶段，保存后检查文件仍是基线失败结果，不能作为验收副本。

## 4. 当前运行状态

- 本地静态资源、local-agent、command-service：已由 `npm run e2e:classified:prepare` 启动。
- WPS 加载项：已注册；当前电脑上 WPS 仍在运行，存在多个测试工作副本标签。
- 当前工作副本：
  `D:\PycharmProjects\docxtool\wps\tests\e2e-work\test-working-copy-ffa0e3ba793d4a4cb89a78f6f0363ac0.docx`
- 当前会话文件：
  `D:\PycharmProjects\docxtool\wps\.runtime\e2e\current.json`

## 5. 下一步严格顺序

1. 在隐藏 E2E 入口执行前，读取 `Application.ActiveDocument` 并确认其与当前会话工作副本一致；若测试副本未保存，直接调用其 `Save()`，随后重新读取 `Saved` 状态。此逻辑只能作用于已由本地会话 guard 认证的 fixture。
2. 用全新的脱敏测试副本运行 `npm run e2e:classified:auto`，不得点击 Ribbon 或任务窗格按钮。
3. 验证会话同时出现 `rollback: PASS` 与 `one_click_format: PASS`。
4. 运行 `scripts/verify-one-click-format.py`；必须为 `PASS`。若失败，按段落索引、角色和属性名继续定位，禁止仅凭 WPS 未报错判定成功。
5. 复制并保留三份输出：写入前副本、回滚副本、最终排版副本；生成脱敏 JSON/Markdown 报告。
6. 只有以上均通过后，再请用户对最终排版副本做视觉确认。

## 6. 重要约束

- 不要 commit、push、reset、clean 或覆盖用户已有改动。
- 不要读取、上传或测试用户真实 DOCX；只使用脱敏 fixture。
- WPS JSAPI 只能在加载项宿主中调用；外部脚本只负责启动、会话、保存后 DOCX 检查和报告。
- 静默 E2E 必须调用 `DocxtoolAutomaticFormatTest → FormatDocumentUseCase`，不得新建另一套 WPS 格式写入器。
- 段前、段后使用“行”；固定行距使用“磅”。
- 页面网格保持 `line_only`：22 行目标，禁止强制 28 字符网格造成横向拉伸。

## 7. 每次任务结束的维护规则

每次任务结束前，必须更新本文件，至少写清：本轮目标、实际改动、自动验证、真实 WPS 结果、当前阻塞、下一步命令/操作、生成文件路径，以及是否执行过 commit/push。

---

## 8. 2026-07-30：批注式预览与静默入口（本轮）

### 实际改动

- 新增 `WpsPreviewCommentService` 与 `CommentPreviewCapabilityProvider`；只使用本机 `wps-jsapi 1.0.5` 已声明的 `Document.Comments`、`Comments.Add`、`Comment.Delete`、`Comment.Reference/Range`、`Range.Start/End`。
- 新增 `PreviewMutationTracker`、`ClearFormattingPreviewUseCase`；预览批注包含 `[DOCXTOOL_PREVIEW]`、session、段落索引、角色、样式和短 anchor，不含正文、路径或令牌。
- `PreviewDocumentUseCase` 会生成命令计划并添加预览批注；`FormatDocumentUseCase` 在正式写入前只删除本 session 的 Docxtool 批注并校验正文 hash/段落数量。
- 正常任务窗格已移除开发诊断和逐项写入测试按钮，只保留“仅识别、在文中显示批注、清除 Docxtool 批注、一键排版”。
- `DocxtoolAutomaticFormatTest()` 是唯一测试函数；隐藏任务窗格入口直接调用该函数，不依赖鼠标点击。
- 静默入口新增活动文档 guard：确认 `ActiveDocument.FullName` 是当前 E2E fixture；仅对 guard 通过的 fixture 自动 `Save()`，失败返回 `E2E_ACTIVE_DOCUMENT_MISMATCH`、`E2E_DOCUMENT_NOT_READY` 或 `E2E_DOCUMENT_SAVE_FAILED`。

### 自动验证

- `npm run typecheck`：通过。
- Node 测试：25 项通过；新增 Range 批注、不删除用户批注、隐藏入口和识别覆盖测试。
- 本机 `wps-jsapi` 声明确认：批注集合、添加、删除、Range 起止位置存在；`Comment.Content` 不在声明中，未作为依赖。
- WPS 官方站点未检索到可用于桌面批注成员的更细粒度公开页，因此实际 RuntimeProbe 仍是成员可用性的最终依据。

### 真实 WPS 结果与阻塞

- `npm run e2e:classified:auto` 已启动服务、生成新 session 和工作副本，但隐藏入口没有回调；会话 `f5d03a115cc14a6cb037ea4fedb44808` 尚未执行。
- 原因已定位为当前 WPS 进程保留旧加载项上下文；官方 `wpsjs debug` 能重新注册开发加载项并启动新宿主，但不能安全关闭用户仍在使用的 WPS 文档来强制所有进程重载。
- 本轮没有以鼠标点击替代静默 E2E，也没有把这次未回调误报为成功。

### 下一步

1. 在不关闭用户文档的前提下，验证 WPS 是否支持对已加载插件执行 reload/activate 的官方 API；若无，自动启动独立 WPS 宿主并将 fixture 交给该宿主。
2. 真实 RuntimeProbe 执行批注“Add → 读回 → Delete → 删除后读回”，据结果标记 `PASS` 或 `COMMENT_PREVIEW_UNSUPPORTED`。
3. 扩展 E2E 产物为 `01-before`、`02-preview-comments`、`03-comments-cleared`、`04-rollback-restored`、`05-after-one-click-format`，并增加保存后评论/用户批注检查器。
4. 只有真实 WPS 读回、保存后 DOCX 检查、回滚和五份产物均通过后，才允许更新最终验收状态。

---

## 9. 2026-07-30：真实文档识别预览与一键排版验收准备

### 本轮目标与实际改动

- Ribbon 已移除“运行能力检测”，只保留“仅识别、预览排版、一键排版、打开任务窗格、关于”。
- 修正 Ribbon 正式动作：现在由任务窗格调用同一个 production composition 中的 `RecognizeDocumentUseCase`、`PreviewDocumentUseCase`、`FormatDocumentUseCase`；不再把用户的“一键排版”误接到脱敏 E2E 驱动。
- 正式任务窗格收敛为“识别结果、排版预览、执行状态、问题与复核”四区；本机内存中仅显示段落序号、类别、置信度和计划格式，不显示或写入任何真实正文到报告。
- 预览批注显示计划字体、字号、粗体、对齐、字符缩进、段前后行数、固定行距和段前分页；批注含 session、文档标识和 anchor 标记。
- 预览清理增加 session、版本、文档标识和 anchor 的组合校验；用户批注采用范围/元数据安全指纹核验，绝不使用 `DeleteAllComments`。
- 文档状态比对增加正文 hash、段落顺序、段落数、节数和格式 revision。预览导致的未保存状态不再单独误判为用户修改；正文或格式变化会返回 `DOCUMENT_CHANGED`。
- 新增构建身份生成：`window.DocxtoolBuildInfo`、任务窗格版本/短 build ID、`build_id`、时间和 asset hash。
- 新增 `npm run acceptance:real-doc:prepare|status|report`。准备命令创建隔离工作副本、构建、重新注册开发加载项、启动本机服务、检查健康状态，并生成不含正文/完整路径的报告。

### 自动验证

- `npm run typecheck`：通过。
- `npm test`：27 项通过。
- `npm run build:classified`、`npm run build:online`、`npm run verify:addin -- classified-offline`：通过。
- 真实验收准备服务：静态资源、local-agent、command-service、debug 注册、build 一致性、工作副本均为 `PASS/BUILD_READY`。
- 真实样本文档包含 WPS 特有的 `NULL` 关系项，`python-docx` 无法安全装载；准备程序已改用只读 OOXML 元数据统计（不读取正文），这不影响 WPS 打开工作副本。

### 真实 WPS 验收状态

- Session：`8fb31f5ebe7a4c2aab3c14e358383dc8`
- Build：`20260730093155-7340dd212881`
- 工作副本：`.runtime/manual-acceptance/8fb31f5ebe7a4c2aab3c14e358383dc8/01-original-copy.docx`
- 脱敏报告：同目录的 `acceptance-report.md` 与 `acceptance-report.json`。
- 原件未修改、未上传、未保存；当前没有把真实 WPS 写入或视觉效果错误报为已通过。

### 下一步（仅用户最终视觉验收）

1. 关闭旧 WPS 窗口后重新打开 WPS，并打开上述工作副本。
2. 在任务窗格核对 build 短号为 `20260730093155-7340dd212881`；若不同，停止并按 `ADDIN_CONTEXT_STALE` 处理。
3. 依次点击 Ribbon 的“仅识别”“预览排版”“一键排版”，查看批注和最终页面效果；由用户决定是否保存工作副本。

### Git

- 本轮未执行 commit、push、reset、clean、restore 或 checkout。

---

## 10. 2026-07-30：任务窗格生命周期与 Ribbon 主上下文修复

### 根因

- Ribbon 原来只把 `recognize/preview/apply` 写入 `PluginStorage`，实际 composition 和动作消费器却位于任务窗格 window。任务窗格未打开或关闭后，没有页面消费动作，因此按钮表现为“无反应”。
- “打开任务窗格”原逻辑每次都强制 `Visible=true`，没有 hide/toggle；窗格内部也没有通过宿主 API 关闭的入口。
- `wpsjs debug` 未固定端口时，注册地址曾漂移到 `3897`，验收脚本却只检查 `3891`，造成旧 build/旧主上下文难以识别。现在已统一固定为 `http://127.0.0.1:3891/`。

### 架构修复

- 新增 `src/host-runtime.ts`，在 WPS 加载项主上下文安装唯一 `HostCommandRouter`、`TaskPaneManager`、`HostResultStore` 和 production composition singleton。
- Ribbon 通过固定注册表直接调用 `HostCommandRouter`；不访问任务窗格 DOM，也不依赖任务窗格 window。
- 任务窗格改为 `PluginStorage` 受控桥接方案：只发送固定 Schema 命令并轮询脱敏 `HostResultStore`，不再创建 composition 或直接调用 use case。
- `TaskPaneManager` 使用官方 `CreateTaskPane/GetTaskPane/Visible/Delete/Navigate/Width` 能力，支持单实例 create/show/hide/toggle/reopen/dispose。
- 关闭与重开不会丢失识别、预览、进度或错误；活动文档切换会按哈希隔离并清空旧文档视图。
- 每次 callback 记录 callback 名、build ID、host context、起止时间、状态和稳定错误码；错误会进入“问题与复核”。
- 正式和开发构建都生成 `host-runtime.js`、`taskpane-workflow.js`；生产任务窗格不包含开发测试按钮。

### Ribbon 映射

- `recognize` → `recognize_document` → `RecognizeDocumentUseCase` → 识别结果。
- `preview` → `preview_document` → `PreviewDocumentUseCase` → 排版预览。
- `apply` → `format_document` → `FormatDocumentUseCase` → 执行状态。
- `taskpane` → `toggle_taskpane` → TaskPaneManager show/hide。
- `about` → `show_about` → 问题与复核。

### 自动验证

- `npm run verify:all`：通过。
- TypeScript typecheck：通过；Node：34 项通过。
- Python：26 项通过；Ruff：通过；profile sync：通过。
- classified/online 两个 edition 构建通过；addin verify 与安全扫描通过。
- TaskPaneManager 单实例、开关/重开、关闭按钮、Ribbon 无窗格调用、状态持久化、文档切换隔离、未知命令拒绝、build stale 和 CSS 边界均有自动测试。

### 真实 WPS 状态

- 已两次自动启动脱敏 WPS fixture；服务和 debug 注册通过，但正在运行的 WPS 进程复用了旧加载项主上下文，没有回调新 HostCommandRouter，因此真实宿主状态保持 `REAL_WPS_E2E_NOT_RUN`，没有误报 PASS。
- 动态注册地址漂移问题已修复；当前正式注册地址固定为 `127.0.0.1:3891`。
- 最终人工验收 Session：`0eb2fa233bf2448e84f19a77fe842a1c`；Build：`20260730105839-da31edd34646`。
- 根目录验收副本：`D:\PycharmProjects\docxtool\wps\真实文档验收工作副本.docx`。

### 用户最小验收

1. 关闭所有旧 WPS 窗口并重新打开验收副本。
2. 核对任务窗格 build 为 `20260730100044-7b727de2a33c`。
3. 点击一次“打开任务窗格”确认出现，再点一次确认收起。
4. 保持窗格关闭，点击“仅识别”，确认窗格自动出现并显示结果。
5. 点击窗格顶部“关闭”，确认可关闭并能再次打开。
6. 点击“预览排版”，确认批注和预览结果。
7. 点击“一键排版”，确认执行状态可见。

### 工作区

- 未修改 Web 服务行为、DOCX Web 排版引擎或 recognition wheel。
- 未修改真实原文档；只更新本地验收工作副本。
- 未执行 commit、push、reset、clean、restore 或 checkout。

---

## 11. 2026-07-30：`xkonglong/gw` 参考仓库审计

- 已浅克隆到 `.runtime/reference/gw`，commit `13de962ea6b7f6486dabd2197f6378bede06ee3d`；该目录由 `.runtime/` 忽略，不进入当前仓库。
- 该仓库公开内容主要是 30 个 VBA 脚本、README 和操作指南，不含其 WPS JS 加载项、Ribbon 或任务窗格实现，因此不能用于解决当前 TaskPane 生命周期问题。
- 可借鉴的设计思路：按表格 Range 边界把正文切成安全片段、逐节保留纵横方向设置、标题层级模式与计数器重置、落款/日期/附件的识别样例、WPS 兼容页码字段、功能按单一职责拆分。
- 不应直接复制：宏会执行 `Fields.Unlink`、`ConvertNumbersToText`、`ClearFormatting`、正文替换和大量删除；依赖 Selection、固定颜色和硬编码尺寸，没有事务、读回、回滚或测试。
- 它的页边距、1.5 倍行距、13 磅段前后、宋体正文等参数与本项目冻结规范不一致，只能作为识别与测试样例，不能作为默认 Profile。
- 仓库没有明确 LICENSE 文件；只借鉴公开思路，不复制代码。
- 本次未修改产品代码，未执行参考宏，未 commit/push。

### 任务窗格 toggle 实机补丁

- 实机发现 WPS 设置 `TaskPane.Visible=false` 后不是同步读回；旧实现立即读到 `true`，误判为 `TASKPANE_HIDE_FAILED`，随后错误处理又调用 `show()`，导致窗格刚关闭就重新打开。
- 已取消关闭后的同步 `Visible` 读回，并规定 `close_taskpane/toggle_taskpane` 失败时绝不自动重开窗格。
- 查阅 WPS 官方文档后确认：收起窗格的正式方式是 `Application.GetTaskPane(numberId)` 取得创建时的窗格对象，再设置 `TaskPane.Visible=false`；`Delete()`用于删除对象，不应作为普通关闭方式。
- Ribbon 和任务窗格内部的关闭按钮现在直接调用上述官方生命周期 API，不再依赖 HostCommandRouter 是否已经加载，因此旧主上下文下也能执行关闭。
- 正式和开发任务窗格顶部都增加了始终可见的“关闭任务窗格”按钮，使用 sticky 工具栏，只影响窗格 WebView。
- Ribbon 顶部功能区现在提供独立的“打开任务窗格”和“关闭任务窗格”按钮；关闭按钮紧挨打开按钮，不再要求重复点击同一个按钮。
- TypeScript、34 项 Node 测试、classified build 和 add-in verify 再次通过；需要关闭全部旧 WPS 窗口后加载上述新 Build 才能验证。

---

## 12. 2026-07-30：GitHub 最新代码同步

### 本轮目标与结果

- 已将当前产品代码、协议、正式执行链、任务窗格生命周期修复、批注预览、自动验证脚本、项目文档和脱敏 fixtures 提交到 GitHub。
- 代码提交：`d40b1df`（`feat: unify WPS formatting and taskpane workflow`）。
- 推送目标：`ssh://git@ssh.github.com:443/7654321x/wps--plugin.git` 的 `main` 分支。
- 推送结果：成功，远程由 `3e9ed1f` 前进到 `d40b1df`。

### 提交前自动验证

- `npm run verify:all`：通过。
- TypeScript typecheck：通过。
- Node 测试：35 项通过。
- Python 测试：26 项通过。
- classified/online 构建、加载项校验和安全检查：通过。
- 本轮没有重新执行真实 WPS 宿主写入，因此真实 WPS 验收状态保持上一节记录，不新增 PASS 结论。

### 隐私与本地文件

- 未提交 `005班子对照检查材料.docx`、`005班子对照检查材料_正确格式.docx`、`真实文档验收工作副本.docx` 和 WPS 临时锁文件。
- 上述文件仍只保留在本机工作区；GitHub 中仅包含脱敏测试 fixture。

### 下一步

1. 后续开发以远程 `main` 的 `d40b1df` 为新基线。
2. 继续真实 WPS 验收前，先确认加载的是最新 build，再执行识别、批注预览和正式一键排版。
3. 保存后仍必须通过 DOCX/OOXML 严格检查，不能以 API 未抛异常替代真实 PASS。

---

## 13. 2026-07-31：GitHub 提交范围确认

- 用户明确指定：本项目后续只推送 `D:\PycharmProjects\docxtool\wps` 这个独立 Git 仓库。
- 用户说“提交 GitHub”或“推送所有文件”时，仅检查、提交并推送该目录对应的 `7654321x/wps--plugin` 仓库。
- 不再提交或推送上级目录 `D:\PycharmProjects\docxtool` 的 Git 仓库。
- 仍自动排除真实 DOCX、验收工作副本、密钥、Token、Cookie、`.env`、`.runtime`、日志、数据库、构建产物和 IDE 配置。
- 本轮仅更新交接规则，未修改产品代码，未执行 commit 或 push；真实 WPS 验收状态不变。

---

## 14. 2026-07-31：wheel 1.7 端到端绑定交接

### 当前结论

- 自动化状态：`READY_FOR_USER_WPS_VALIDATION`。
- 真实 WPS 客户端、真实 `Range` 写入和视觉效果尚未由 Agent 执行；不得将本节的自动化结果写为真实 WPS 通过。
- 根仓库已构建新的 DocxTool `1.7` wheel；WPS 本地 agent 只通过该 wheel 的公开 SDK 识别和绑定，不调用上级源码目录。

### 实际改动

- docxtool 识别 SDK：支持物理段落锚点、同段多逻辑片段、片段级有效格式、East Asia/ASCII 字体、格式覆盖率和 host binding。
- WPS local agent：`/v1/recognize` 必须接收 HostSnapshot，并返回 wheel plan 与 host binding；新增不读取用户文档的 `/v1/handshake`，用于功能检测执行最小识别和绑定握手。
- WPS 协议：RecognitionResult 和 FormattingCommandSet 均升级为 `1.2`；每个可执行目标带 host 段落、raw UTF-16 范围和物理段落哈希。
- WPS adapter：正式 Range 定位先校验物理段哈希、子范围和 Range.Text 读回；不再使用逻辑段序号、首次文本匹配或裸索引 fallback。review 仅预览，unresolved、表格和不完整/混合片段组不进入一键排版。
- E2E 只读链路：由安全 HostSnapshot 调用 `bind_recognition_plan()`；仅将哈希、角色、版本和已确认 host anchors 发送给命令服务，正文不进入命令请求。
- `host-text-v1`：新增 TypeScript 金标测试与 `scripts/sync-host-text-golden.ps1`；同步副本为 `docs/HOST_TEXT_V1_GOLDEN.json`，源文件为 `..\\docs\\HOST_TEXT_V1_GOLDEN.json`。
- Ribbon：涉密离线版顶部仅保留 `预览排版`、`一键排版`、`功能检测`，统一通过 HostCommandRouter。

### 自动验证

- `pwsh -NoProfile -File scripts/verify-all.ps1`：通过。
  - TypeScript typecheck：通过。
  - Node：54 passed，0 failed。
  - Python：30 passed，0 failed。
  - Ruff：通过。
  - default profile 同步检查：通过。
  - classified-offline、standard-online 构建、加载项清单校验和安全扫描：通过。
- `pwsh -NoProfile -File scripts/sync-host-text-golden.ps1 -Source ..\\docs\\HOST_TEXT_V1_GOLDEN.json -Check`：通过。
- 新 wheel 干净安装：通过公共 SDK 全部模型导入、`docxtool-recognize --help`、脱敏 DOCX 识别和 HostSnapshot binding。

### 构建产物与验收材料

- Wheel：`D:\\PycharmProjects\\docxtool\\tmp_wheels\\recognition-1.7-final-20260731\\docxtool-1.7-py3-none-any.whl`
- Wheel SHA-256：`8792D577BFC59CA4886CA73B8CF9300165E1868FFE53DCD833535E7C8962868C`
- classified-offline 构建目录：`D:\\PycharmProjects\\docxtool\\wps\\apps\\classified-offline\\dist`
- Build ID：`20260731071239-bfadf7f2905b`
- Asset SHA-256：`bfadf7f2905b5a214fe0c6388679e4f36a3de4a40f59066bc01bcf39984f8052`
- 验收 DOCX：`D:\\PycharmProjects\\docxtool\\test_docx\\wps_validation\\01-basic-heading-body.docx` 至 `08-section-page-format.docx`。
- 用户清单：[USER_WPS_VALIDATION.md](../docs/USER_WPS_VALIDATION.md)
- 用户记录模板：[USER_WPS_VALIDATION_RESULT.md](../docs/USER_WPS_VALIDATION_RESULT.md)

### 用户验收顺序

1. 加载上述 classified-offline `dist` 构建产物，确认 build ID。
2. 仅打开 `test_docx/wps_validation` 下的脱敏 fixture，不要使用真实文件。
3. 依次点击 `功能检测`、`预览排版`、`一键排版`，按用户清单记录批注、跳过项、用户批注保护与一次撤销结果。
4. 将结果填写到记录模板；若失败，只记录 build ID、wheel 版本、稳定错误码、fixture 编号和截图编号。

### Git 与未解决事项

- 本 WPS 仓库：本轮未执行 commit、push、reset、clean、restore 或 checkout。
- Docxtool 根仓库的 `1.7` 已按单独发布流程推送；本 WPS 工作区的既有用户改动、`.idea`、真实 DOCX、运行目录和构建产物均未纳入提交。
- 下一步只能是用户进行真实 WPS 验收；用户未明确反馈全部通过前，不得把状态改为 `SUCCESS`。

---

## 15. 2026-08-04：DocxTool recognition wheel 4.0 安装与适配

### 当前结论

- DocxTool 4.0 wheel 已复制到 WPS 独立仓库并安装到本项目 `.venv`。
- local-agent 已适配 4.0 的正式 `host-snapshot-v1` 输入校验；识别 wheel 仍只负责识别和宿主文本绑定，格式命令继续由现有 command-service 与 WPS API 执行。
- 自动验证状态为 `PASS`；本轮没有启动真实 WPS、没有执行真实 Range 写入和保存后 OOXML 验收，因此真实 WPS 状态保持 `REAL_WPS_E2E_NOT_RUN`。

### 实际改动

- Wheel：`D:\PycharmProjects\wps\docxtool-4.0-py3-none-any.whl`。
- Wheel SHA-256：`22193B2E7BC440BA8CEDD8807EA896ED9611D2F63AF424CAF5C05B519D73A3FF`。
- `.venv` 已从 `docxtool 1.7` 升级为 `docxtool 4.0`；导入路径为本项目 `.venv\Lib\site-packages\docxtool`。
- `local-agent/pyproject.toml` 的依赖约束同步为 `docxtool==4.0`。
- 新增 `local-agent/src/docxtool_local_agent/snapshot.py`，将 WPS 简化快照统一转换为正式 `host-snapshot-v1`，补齐稳定 ID、story、文档修订和 UTF-16 坐标契约。
- local-agent handshake、正式识别和只读 E2E 均通过该唯一适配边界调用 `bind_recognition_plan()`；WPS 请求新增传递 `document_revision`。
- 默认格式同步改为从本级已安装 wheel 的 `docxtool.resources` 读取配置，不再依赖上级源码目录；生成 manifest 中 wheel 版本更新为 4.0。

### 自动验证

- wheel 与来源 SHA-256 一致。
- `pip check`：通过；manifest 版本为 4.0；8 个 SDK Schema 可读取；两个 CLI help 可执行。
- local-agent 定向测试：7 passed。
- `pwsh -NoProfile -File scripts/verify-all.ps1`：`VERIFY_ALL_PASS`。
  - TypeScript typecheck：通过。
  - Node：54 passed，0 failed。
  - Python：30 passed，0 failed。
  - Ruff：通过。
  - 默认 profile 同步：通过。
  - classified-offline、standard-online 构建和 add-in 校验：通过。
- classified-offline Build ID：`20260804155619-bfadf7f2905b`。

### 真实 WPS 验收状态

- 本轮开始时 9528 没有监听进程，因此没有停止服务、轮换 session token 或重启 WPS。
- 未执行真实 WPS 识别、批注预览、一键排版、保存和 OOXML 读回，不能写为真实 WPS PASS。

### 未解决事项与下一步顺序

1. 需要真实验收时，先用现有 prepare 流程启动 3889/9528，确认 handshake 返回 package version 4.0。
2. 关闭旧 WPS WebView 后重新打开脱敏 fixture，执行功能检测、预览排版和一键排版。
3. 保存后检查 DOCX/OOXML、正文 SHA-256、批注和 Range，完成后才能更新真实 WPS 状态。

### Git 状态

- WPS 仓库开始前已有大量用户修改，本轮全部保留并在其上完成必要适配。
- 本轮未执行 commit、push、pull、merge、rebase、reset、clean、restore 或 checkout。

---

## 16. 2026-08-05：WPS 本地直连与 UI 解耦收口

### 当前结论

- 本轮已把 `classified-offline` 的主链路从“Ribbon 直接调 Host/自动开面板/依赖 9528”收口为“Ribbon 只入队，本地命令总线串行执行，TaskPane 仅做状态面板”。
- 生产入口不再加载 `ui/e2e-session.js`，改为加载 `ui/local-runtime-config.js`；Host 运行时从 `%APPDATA%\Docxtool\runtime\current.json` 或内联本地配置读取本地识别 runtime，而不是端点/token。
- `HostCommandRouter` 不再在 `preview_document/format_document/health_check` 前自动 `panes.show()`，失败也不再自动打开任务窗格。
- `TaskPaneManager` 仍使用官方 `CreateTaskPane/GetTaskPane/Visible` 生命周期，但只由 `panel/toggle_taskpane` 控制。
- 健康检查已去服务器化，改查 `Application.FileSystem`、本地识别 exe、`OAAssist.ShellExecute` 和 WPS 能力，不再探测 9528、recognition service、command service 或 session token。

### 实际改动

- 新增 `apps/classified-offline/src/local-command-bus.ts`：本地命令总线，Ribbon/TaskPane 入队立即返回，串行 drain，支持重复命令拒绝。
- 重写 `apps/classified-offline/src/composition-root.ts`：生产 composition 改用 `LocalProcessRecognitionTransport` + `LocalFormatCommandGenerator`，去掉 `HttpLocalRecognitionTransport`、`HttpCommandServiceClient`、`LocalEndpointProvider` 和 endpoint/token。
- 扩展 `packages/recognition-client/src/index.ts`：新增 `LocalProcessRecognitionTransport`，通过临时 JSON + `ShellExecute` 调用本地识别 exe，带超时、request_id 校验、结果大小上限和清理。
- 扩展 `packages/local-format-engine/src/index.ts`：本地格式命令生成器直接从 frozen profile 生成 `FormattingCommandSet`。
- `packages/wps-adapter/src/official-host.ts`：WPS 格式执行增加 `yieldEvery`，批量应用时周期性让出事件循环，并在批次间检查活动文档是否切换。
- `apps/classified-offline/src/host-runtime.ts`：Host 只暴露 `DocxtoolHostEnqueue`；任务窗格请求通过同一队列消费；不再使用 9528 上报 E2E；诊断改成本地内存队列。
- `apps/classified-offline/js/ribbon.js`、`apps/classified-offline/js/ribbon-production.js`：Ribbon 改为只调用 `DocxtoolHostEnqueue`，并新增 `panel` 按钮与 `GetActionEnabled`。
- `apps/classified-offline/ribbon.xml`：按钮改为 `预览排版 / 一键排版 / 状态面板 / 本机检测`。
- `apps/classified-offline/ui/taskpane.html`、`src/taskpane-workflow.ts`：任务窗格增加“返回文档”，去掉 HTTP 诊断 logger 依赖。
- `apps/classified-offline/main.production.js`、`main.js`、`vite.config.js`：改为加载并拷贝 `ui/local-runtime-config.js`。
- 新增 `scripts/build-local-recognition-runtime.ps1`、`install-local-runtime.ps1`、`remove-local-runtime.ps1`、`local-direct.ps1`、`verify-local-direct.ps1`。
- `scripts/verify-addin.mjs`：classified offline 校验改为 local-runtime-config + 四按钮顺序。
- `tests/wps-phase-one.test.mjs`：同步更新 Ribbon、Host、health check、production 入口和 local-direct 校验断言。
- `问题清单Agent.md`：新增 P024，记录 Ribbon/TaskPane 解耦与本地直连队列的防复发规则。

### 自动验证

- `npm run typecheck`：PASS。
- `npm test`：66 项 PASS。
- `npm run verify:local-direct`：PASS。
- `npm run build:classified`：PASS。
- `npm run verify:addin -- classified-offline`：PASS。

### 真实 WPS 验收

- 本轮未启动或点击真实 WPS，未写真实 WPS PASS。
- 真实验收前需要本地存在 `dist\local-runtime\win-x64\docxtool-recognize.exe` 或等价安装产物；当前仓库中的 build/runtime 脚本已改为明确门禁，找不到 exe 时会直接报中文稳定错误码，不再回退到 9528。

### 未解决问题

1. 本仓库当前没有现成的 `docxtool-recognize.exe`，`build-local-recognition-runtime.ps1` 只能做存在性门禁，无法凭空生成 runtime。
2. `formal-e2e-driver.ts`、`formal-e2e-usecase.ts` 等旧 9528 E2E 文件仍保留在仓库中，属于旧实现；本轮 production 路径已不再引用。
3. 还需在真实 WPS 宿主里验证：Ribbon 立即返回、TaskPane 不自动弹出、预览/排版由同一队列消费、保存后 OOXML 检查闭环。

### 下一步严格顺序

1. 先补齐或安装本地识别 runtime exe，并写入 `%APPDATA%\Docxtool\runtime\current.json`。
2. 重新打开 WPS，让最新 build 加载 `ui/local-runtime-config.js` 与 `host-runtime.js`。
3. 先点“本机检测”，确认本地 runtime 与 ShellExecute 可用。
4. 再点“预览排版”和“一键排版”，确认 Ribbon 只入队、TaskPane 只做状态显示。
5. 保存后再做 DOCX/OOXML 检查，最后才算真实 WPS 验收完成。

---

## 17. 2026-08-05：本地规范与工作目录核实

### 当前结论

- 当前真实仓库根目录已确认为 `D:\PycharmProjects\wps`。
- 后续默认只在该目录工作；旧的 `D:\PycharmProjects\docxtool` 只能作为用户明确要求时的外部路径，不再作为本项目事实源。
- 本级 `.venv` 中已安装 `docxtool 4.0`，导入路径为 `D:\PycharmProjects\wps\.venv\Lib\site-packages\docxtool\__init__.py`，未从旧源码目录导入。
- 本地直连生产链路与旧 9528 E2E/诊断链路已在规范中分开：生产链路缺少 `docxtool-recognize.exe` 时必须明确失败，不得回退到 9528。

### 实际改动

- `AGENTS.md`：补充工作目录约束；明确所有命令、读写、测试和 Git 操作默认以 `D:\PycharmProjects\wps` 为根；跨目录前必须说明目标和原因。
- `AGENTS.md`：把本地链路拆分为“本地直连生产链路”和“旧 E2E/诊断链路”，避免把 9528 写成默认主链路。
- `问题清单Agent.md`：修正 P015 的旧路径表述，新增 P025，防止再次把仓库误判为 `D:\PycharmProjects\docxtool\wps`。
- `README.md`：修正常用验证命令，删除 `../.venv` 依赖，明确当前仓库根目录。
- `docs/REPOSITORY_SYNC.md`：把事实源从旧嵌套路径修正为 `D:\PycharmProjects\wps`，默认格式说明改为从本级已安装 wheel 读取。

### 自动验证

- `git rev-parse --show-toplevel`：返回 `D:/PycharmProjects/wps`。
- `D:\PycharmProjects\wps\.venv\Scripts\python.exe -m pip show docxtool`：版本 `4.0`，位置 `d:\pycharmprojects\wps\.venv\lib\site-packages`。
- Python 导入检查：`docxtool.__file__` 位于本级 `.venv\Lib\site-packages`。
- `npm test`：66 项 PASS。

### 真实 WPS 验收状态

- 本轮只核实和修正规范，未启动真实 WPS，未执行预览、排版、保存或 OOXML 读回。
- 真实 WPS 状态未变化，不能写为新的真实 PASS。

### 未解决问题

1. 历史交接记录中仍保留旧路径，这是历史事实记录；本轮没有批量改写历史段落。
2. 旧 9528 E2E/诊断文件仍存在，属于保留兼容与历史测试链路；生产本地直连链路不得依赖它。
3. `dist\local-runtime\win-x64\docxtool-recognize.exe` 仍是本地直连真实验收门槛。

### 下一步严格顺序

1. 继续只在 `D:\PycharmProjects\wps` 实施。
2. 先补齐本地识别 runtime exe。
3. 再运行本地直连安装与校验。
4. 最后进入真实 WPS 预览/排版/保存后 OOXML 验收。

### commit/push 状态

- 本轮未执行 commit、push、pull、merge、rebase、reset、clean、restore 或 checkout。

---

## 18. 2026-08-05：本地直连版本提交到 GitHub

### 当前结论

- 已按用户要求只处理 `D:\PycharmProjects\wps` 仓库。
- 已提交并推送本地直连与规范收口版本到 GitHub：`7654321x/wps--plugin.git` 的 `main` 分支。

### 实际改动

- 本次提交包含此前已完成的 local-direct runtime workflow、Ribbon 入队、TaskPane 状态面板、本地 runtime 配置、local format engine、相关脚本、测试与本地规范修正。
- 未提交 `.idea/`、`.reasonix/`、日志、根目录真实 DOCX 或测试工作副本 DOCX。

### 自动验证

- 提交前已执行 `npm test`：66 项 PASS。
- 提交前已确认 Git 根目录为 `D:/PycharmProjects/wps`。
- 提交前已确认本级 `.venv` 中 `docxtool 4.0` 来自 `D:\PycharmProjects\wps\.venv\Lib\site-packages`。

### 真实 WPS 验收状态

- 本轮只执行本地代码提交与推送，没有启动真实 WPS，也没有执行保存后 OOXML 验收。
- 真实 WPS 状态未变化，不能写为新的真实 PASS。

### 未解决问题

1. `dist\local-runtime\win-x64\docxtool-recognize.exe` 仍是本地直连真实验收门槛。
2. 旧 E2E/诊断链路文件仍保留；生产本地直连路径不得回退到 9528。

### commit/push 状态

- 代码提交：`c9e96ea feat: local direct runtime workflow`。
- GitHub push：PASS，已推送到 `origin/main`。

---

## 19. 2026-08-05：本地直连 runtime 收口

### 审计基线

- 工作目录：当前 WPS 仓库根目录
- 当前分支：`codex/1.0`
- 当前 HEAD：`4de89a4`

### 已完成

- 新增官方 FileSystem 适配器 `packages/wps-adapter/src/local-filesystem.ts`。
- `LocalProcessRecognitionTransport` 已改为本地 exe + 两参数 `ShellExecute`。
- 新增本地识别入口 `local-runtime/recognize_entry.py` 与合同 `local-runtime/contract.py`。
- `build-local-recognition-runtime.ps1` 已产出 `dist/local-runtime/win-x64/docxtool-recognize.exe` 与 `runtime-manifest.json`。
- `install-local-runtime.ps1` 已写入 `%APPDATA%\Docxtool\runtime\current.json`。
- `local-direct.ps1` 已支持 `prepare / status / stop / reset`。
- `verify-local-direct.ps1` 已升级为生产入口、runtime、安装指针和端口检查。
- `health-check.ts`、`host-runtime.ts`、`error-messages.ts` 已同步新合同。

### 验证

- `npm run typecheck`：PASS
- `npm run build`：PASS
- `node --test tests/wps-phase-one.test.mjs`：PASS
- `python -m pytest local-runtime/tests -q`：PASS
- `scripts/build-local-recognition-runtime.ps1`：PASS
- `scripts/install-local-runtime.ps1`：PASS
- `scripts/verify-local-direct.ps1`：PASS
- `scripts/local-direct.ps1 status`：PASS
- `dist/local-runtime/win-x64/docxtool-recognize.exe --help`：PASS

### 当前状态

- 本地识别 runtime 已真实构建并安装。
- 生产链路仍保持：
  `Ribbon -> LocalCommandBus -> HostCommandRouter -> WpsDocumentReader -> LocalProcessRecognitionTransport -> docxtool-recognize.exe -> LocalFormatCommandGenerator -> WPS`

### 下一步

- 如果继续做真实 WPS 验收，先确认 `scripts/local-direct.ps1 status` 为 `READY`，再打开 WPS 走预览/排版/保存后验证。

---

## 20. 2026-08-05：根目录 main 启动入口

### 已完成

- 根目录入口已改为：`main.py`。
- 默认命令：`python main.py start`。
- 支持动作：`start / prepare / status / stop / reset / verify`。
- `start` 会构建本地识别组件、安装 runtime、构建 WPS 插件、检查插件结构、注册 WPS 插件项目、执行本地直连功能检测，随后输出中文状态。
- 控制台输出改为简明中文，例如“本地识别组件构建完成”“注册项目已加载”“WPS 功能检测已完成”。
- 不启动 `local-agent`、不启动 `command-service`、不开放 `9528`。
- `package.json` 新增：
  - `npm start`
  - `npm run start:status`
  - `npm run start:verify`

### 验证

- `python main.py status`：PASS
- `npm run start:status`：PASS
- `python main.py verify`：PASS
- `python -m py_compile main.py`：PASS
- `package.json` JSON 解析：PASS

### 使用方式

```powershell
python main.py start
```

或：

```powershell
npm start
```

---

## 21. 2026-08-05：PyCharm main 运行配置修复

### 问题

- PyCharm 运行 `main` 报错：找不到迁移前旧嵌套目录中的虚拟环境解释器。

### 根因

- 项目已迁到当前仓库根目录，但 PyCharm 的 `main` 运行配置仍通过旧模块 SDK 解析到历史目录。

### 修复

- 已修改 `.idea/workspace.xml` 中 `main` 运行配置：
  - `SDK_HOME` 改为 `$PROJECT_DIR$/.venv/Scripts/python.exe`
  - `IS_MODULE_SDK` 改为 `false`
  - 最近路径改为当前仓库根目录

### 验证

- 旧路径扫描：未发现迁移前虚拟环境路径残留。
- 当前解释器运行：

```powershell
.\.venv\Scripts\python.exe main.py status
```

结果：PASS，控制台输出中文状态。

### 二次修复

- PyCharm 仍提示“找不到此运行配置的 Python 解释器”时，说明临时配置没有绑定到有效解释器。
- 已将 `.idea/workspace.xml` 的 `SDK_HOME` 改为绝对路径：

```text
$PROJECT_ROOT\.venv\Scripts\python.exe
```

- 已新增固定运行配置：

```text
.idea/runConfigurations/main.xml
```

- 验证：
  - 当前解释器直接运行 `main.py status`：PASS
  - `.idea/workspace.xml` 与 `.idea/runConfigurations/main.xml` XML 解析：PASS

## 2026-08-05 根入口卡住修复

### 问题与根因

- `main.py start` 在“注册 WPS 插件项目”阶段长时间不返回。
- `wpsjs debug -s` 是常驻静态资源服务，不是执行完即退出的注册命令；原入口同步等待该进程，因此后续检测永远无法执行。
- PyCharm 同时保留固定与临时两个同名 `main` 配置，可能继续选中临时缓存项。

### 修复

- `main.py` 改为后台启动 `wpsjs debug -s`，以本机 3889 端口就绪作为“注册项目已加载”的判断。
- 端口已经就绪时直接复用，避免重复启动。
- 清除 `.idea/workspace.xml` 中冲突的临时 `main`，保留 `.idea/runConfigurations/main.xml` 固定入口。

### 验证

- `main.py start`：PASS，完整链路约 24 秒正常返回。
- 本地识别组件构建与安装：PASS。
- WPS 插件构建、结构检查与注册：PASS。
- 本地直连功能检测：PASS。
- Python 编译、TypeScript 类型检查、PyCharm XML 配置解析：PASS。

## 2026-08-05 PyCharm 全局解释器登记修复

- 截图中的错误发生在脚本启动前，根因不是 `main.py`，而是 PyCharm 全局 SDK 表内的 `Python 3.8 (wps)` 仍指向已失效的旧仓库虚拟环境。
- 已将该 SDK 的解释器、虚拟环境根、site-packages 和关联项目全部改为当前仓库。
- `.idea/misc.xml` 已显式绑定项目 SDK；固定 `main` 配置改为使用模块 SDK，避免运行配置自行保存另一份失效路径。
- 验证：全局 SDK 当前解释器存在且为 Python 3.8.10；项目 XML 均可解析；旧嵌套目录引用扫描为 0；`main.py status` PASS。

## 2026-08-05 重构虚拟环境依赖恢复

- 新 `.venv` 初始只有 `pip`、`setuptools`、`wheel`，导致本地识别程序无法通过 PyInstaller 构建。
- 已从项目根目录本地 wheel 安装 `docxtool 4.0` 及固定运行依赖。
- 已安装 Python 3.8 兼容的 `PyInstaller 6.21.0`、`pytest 8.3.5` 和 `ruff 0.11.0`。
- 验证：本地 runtime 构建 PASS；runtime 单元测试 3 项 PASS；生成的 exe 帮助入口 PASS；TypeScript 类型检查 PASS；完整 `main.py start` 约 23 秒返回 0。

## 2026-08-05 Ribbon 无响应与本地日志收口

- 现场确认 3889 正常监听、WPS 注册 URL 正确，但旧根日志没有当前 Ribbon 事件，说明现有 WPS 上下文没有安装新版 Host 队列。
- 修复 WPS 旧版内嵌浏览器可能缺少 `crypto.randomUUID()` 导致 Host 安装中断的问题；Host、识别请求、预览批注和任务窗格请求均使用兼容随机 ID。
- 新增本地直连持久化日志：仓库根目录 `wps-plugin-debug.log`，不依赖 9528。
- `main.py start` 默认常驻并把新增 WPS JSON 日志翻译为中文控制台信息；`--once` 用于只启动后返回。
- 验证：类型检查 PASS；相关 Node 测试 60 项 PASS；classified 构建和加载项结构检查 PASS。

### 日志路径调整

- 按用户要求，Host 写入、`main.py` 实时监视和 `scripts/wps-diagnostic-log.ps1` 已统一使用仓库根目录 `wps-plugin-debug.log`。
- 日志命令 `path` 已验证返回当前仓库根日志，重新构建与 60 项测试通过。

## 2026-08-05 Ribbon Bootstrap Looper R0–R4

- 基线：`704a3f5`；WPS `12.1.0.28043`；wpsjs `2.2.3`；注册模式为 `wpsjs debug`。
- R0 证明开发注册原先服务源码目录，而验证只检查 dist：源码 main 指向 `dist/host-runtime.js`，dist main 指向 `host-runtime.js`，两套 main/ribbon 内容不同，Host 均使用 ES Module。
- R1 删除独立的 production main/ribbon 业务副本，构建只复制 canonical `main.js` 与 `js/ribbon.js`；构建后 source/dist 两者分别一致。
- R2 新增经典脚本 `js/bootstrap-probe.js`，最先执行，捕获 window error/unhandled rejection，并在 WPS FileSystem 可用后写早期日志。
- R3 Host Runtime 改为独立 Vite IIFE 单文件构建；产物无静态 import、动态 import、外部 chunk 或 module loader。
- R4 Ribbon 回调显式赋给 `window.OnAddinLoad`、`window.OnAction`、`window.GetActionEnabled`，并增加脚本加载及 onLoad 生命周期日志。
- 自动门禁：typecheck PASS；verify-addin PASS；Node 60/60 PASS；runtime pytest 3/3 PASS；构建 PASS。
- 当前真实状态：`READY_FOR_NEXT_LOOP_R5`。当前 3889 未监听，loaded build 未知，尚未进行真实 WPS 点击，因此不标记真实 PASS。
- 下一步严格顺序：R5 唯一 debug-package → R6 wpsjs 进程日志 → R7 HTTP/Build 探针 → R8 注册检查。

## 2026-08-05 WPS 单一本地直调链收口

- 用户确认目标：WPS 直接调用底层识别和排版逻辑，不经过任何业务服务器或 Host 命令中转。
- 已删除 `LocalCommandBus`；`HostCommandRouter` 改为唯一 `LocalApplicationRuntime`；Ribbon 不再使用 `DocxtoolHostEnqueue`，改为直接调用 `DocxtoolRunLocalCommand`。
- 唯一生产链路：`Ribbon → LocalApplicationRuntime → 正式 Application Use Case → LocalProcessRecognitionTransport → docxtool-recognize.exe/wheel → LocalFormatCommandGenerator → 正式 WPS executor`。
- 预览、排版、本机检测和面板操作全部进入同一个本地 Runtime；只保留执行中互斥锁防止重复点击，不存在命令队列、HTTP 请求或服务端转发。
- TaskPane 请求仍通过 WPS `PluginStorage` 交给同一个 `DocxtoolRunLocalCommand`，没有第二套执行器。
- 自动门禁：TypeScript build/typecheck PASS；Node 60/60 PASS；classified build PASS；verify-addin PASS；verify-local-direct PASS。
- 生产扫描禁止：`DocxtoolHostEnqueue`、`DocxtoolHostDispatch`、`HostCommandRouter`、`LocalCommandBus`、HTTP recognition/command endpoints 和 9528。
- 旧 E2E 开发文件仍有历史 9528 文本，但不被 canonical main、Ribbon、Vite production build 或本地直连验证目标加载；后续可单独做开发遗留清理，不影响当前唯一生产链。

## 2026-08-05 隐藏资源进程与现有 WPS 复用

- `main.py` 不再执行 `wpsjs debug -s` 启动资源，因此不会弹出 CMD，也不会额外拉起 WPS。
- 3889 改为 `CREATE_NO_WINDOW` 启动的隐藏 Python 静态资源进程；它只提供插件 JS/CSS，不参与识别或排版业务。
- 启动目录固定为 `.runtime/wps-debug-package`；HTTP 探针验证 10 个资源、Build ID 和关键 SHA-256 后才显示 `DEBUG_SERVER_READY`。
- 注册信息通过 XML parser 验证 publish.xml，当前 URL 和插件类型匹配后才显示 `REGISTRATION_READY`。
- `main.py` 控制台同时监视资源访问日志、APPDATA 早期日志和仓库根交互日志；新增 `diagnose` 显示 bootstrap、Ribbon onLoad、本地 Runtime 和最近错误。
- 重复完整启动验证：后台服务复用成功，新增 WPS 进程数为 0；运行中的 debug-package 可覆盖更新，不再因工作目录锁定失败。
- 当前已有 WPS 会话启动早于新入口，诊断为 `bootstrap page: WAITING`。WPS JS 加载项页面无法由外部进程安全热注入；需保存文档后做一次完整 WPS 重载。完成一次新入口装载后，后续启动不会再额外打开 WPS。

## 2026-08-05 WPS 无响应诊断通道收口

### 当前结论

- 已确认 WPS 在启动时下载了当前 `host-runtime.js`；此前“资源服务无请求”的判断受 Python HTTP 访问日志缓冲影响，不能作为加载失败证据。
- 现有 WPS 会话仍未产生本次构建的 `bootstrap.probe.loaded`、`ribbon.addin.load.success` 或 `application.install.success`，所以三个按钮无反应时尚不能归因到识别、排版或 WPS 格式 API。
- 依据本地 `wpsjs 2.2.3` 源码和 `docs/WPS_OFFICIAL_REFERENCES.md`，已登记的运行中 WPS 需要重启或刷新加载项才会读取新页面；当前有用户可见文档，未强制结束 WPS。

### 实际改动

- 新增 `scripts/wps_debug_server.py`：仍只占用 3889，提供插件静态资源、`publish.xml` 空响应和热更新探针；增加诊断专用 `POST /__docxtool_log`，不承载识别或排版业务。
- `bootstrap-probe.js` 在最早阶段通过同源诊断端点回传加载、脚本错误和 Promise 错误；Host/Ribbon 事件继续走同一通道。
- `main.py` 改为启动隐藏诊断资源服务，自动替换旧 `http.server`；根目录 `wps-plugin-debug.log` 是唯一用户可见日志位置，默认常驻模式实时翻译为中文控制台日志。
- `main.py diagnose` 只统计当前 `build_id`，不会被历史 local-agent 日志污染；启动时检测到 WPS 已运行但未加载当前 build 会明确提示需要安全重载。
- `问题清单Agent.md` 新增 P037，记录 WPS WebView 注册/缓存与宿主重载门槛。

### 自动验证

- Python 3.8 编译：PASS。
- `npm test`：70/70 PASS。
- `npm run verify:addin -- classified-offline`：PASS。
- `npm run verify:local-direct`：PASS。
- 3889 `POST /__docxtool_log`：HTTP 204，事件已写入 `wps-plugin-debug.log`。
- 资源探针和注册检查：PASS。
- 真实 WPS 按钮点击：尚未重新加载本次构建，不能标记 PASS。

### 生成和运行文件

- 交互日志：`wps-plugin-debug.log`
- 当前调试包：`.runtime/wps-debug-package/`
- 资源进程元数据：`.runtime/wpsjs-debug-process.json`
- 资源探针：`.runtime/resource-probe.json`
- 隐藏资源服务：`scripts/wps_debug_server.py`
- 最新调试包 build id：`20260805135536-bf337a7e1e46`；HTTP 资源探针当前为 PASS。

### 下一步严格顺序

1. 先正常保存并关闭当前脱敏测试文档。
2. 完全退出残留 WPS 宿主后重新打开 WPS 和该测试文档；不要继续点击旧功能区。
3. 运行 `main.py diagnose`，必须先看到当前 build 的 `bootstrap.probe.loaded`、`ribbon.addin.load.success`、`application.install.success`。
4. 在同一会话中点击“本机检测”，确认控制台出现 `ribbon.action.received` 和 `application.runtime.run.received`，再点击预览排版、一键排版。
5. 只有事件链完整后，才继续分析真实 WPS 写入、即时读回和保存后 OOXML 结果。

### Git 状态

- 本轮未 commit、未 push。

## 2026-08-06 WPS 线程解耦 T8–T17 收口停止点

### 当前结论

- 当前 HEAD 仍为 68ca3bd，所有 T0–T11 改动保留在工作区，未 commit、未 push。
- P0、T8、T9、T10 已完成自动实现和测试；T11 正式预览源码已切到 Worker 且没有同步 fallback。
- 真实 WPS 在 host.launch_recognition 首次正式验证触发 S5，第二次独立启动探针再次失败并触发 S7。最终状态为 BLOCKED，不是完整 PASS。
- 按停止条件没有进入 T12–T17，也没有测试正式排版。

### 本轮实际改动

- 正式线程预览增加默认关闭门禁；未显式开启时稳定返回 THREADED_PREVIEW_RECOGNITION_LAUNCH_BLOCKED。
- PyInstaller 构建增加 docxtool 包资源收集，解决 EXE 缺少默认格式配置和 SDK schemas 导致的 RecognitionSdkError。
- WPS 识别启动器增加 ShellExecute 调用前、返回和失败事件。
- 新增 localhost 限定的 recognition_launch_probe，复用正式 Pipeline Worker 和 WpsHostBridge，只验证识别启动链，不写批注、不排版。
- 移除普通 WPS 加载时自动执行 snapshot shadow 的行为；开发影子快照只能显式启动。
- 修复全量测试无条件重写 4 个受管理 DOCX fixture 的副作用；只读网格检查不再运行无关生成器，本轮生成的二进制差异已清除。
- 开发探针标记已从当前 debug package 移除，线程预览默认配置仍为关闭。

### 自动验证

- npm run typecheck：PASS。
- npm test：94/94 PASS。
- local-runtime pytest：3/3 PASS。
- npm run build:classified：PASS。
- npm run verify:addin -- classified-offline：PASS。
- npm run verify:local-direct：PASS。
- scripts/verify-all.ps1：PASS；Node 94/94、Python 34/34，Ruff 全部通过。
- git diff --check：PASS。
- 修复后 docxtool-recognize.exe 独立执行：退出码 0，20 个识别块、20 个 binding，不生成 error 文件。
- 当前安装 runtime：docxtool-4.0；EXE SHA-256 为 134c22581b7068fffc3f30774e305d5103897a74a909dda7d3193e94a0958ea8。

### P0 性能前后

| 指标 | 修改前 | 修改后 |
|---|---:|---:|
| 1000 段批次 | 983 | 113 |
| 平均批次 | 约 1.02 | 8.85 |
| Worker 总耗时 | 12268 ms | 1988 ms |
| Host P95 | 17.2 ms | 55.67 ms |
| 主线程最大漂移 | 1218 ms | 205.9 ms |

### 真实 WPS 结果

- 真实 build：20260806012054-2398230cafe9。
- 最终自动门禁与当前 3889 调试包 build：20260806013615-2398230cafe9；无开发 E2E 标记，线程预览默认关闭。
- 脱敏 20 段 fixture 已打开，Worker probe、snapshot 和 pipeline.recognition.launch 均正常。
- OAAssist.ShellExecute 调用前事件已记录；37513.7 ms 后才返回。
- 调用期间和返回后均未观察到 docxtool-recognize 进程，也未生成 result.json/error.json；Worker 先以 HOST_RPC_TIMEOUT 失败。
- WPS 最终 Responding=True；没有写批注、没有格式写入、没有保存文档。
- 测试后活动窗口已切换为用户文档，因此未关闭或结束该 WPS 进程；没有对用户文档执行识别、预览或排版。

### 未解决问题

- 当前 WPS 12.1.0.28043 的 OAAssist.ShellExecute(file, params) 不是可用的微型异步启动边界：同步阻塞且没有实际启动本地 EXE。
- 官方 wps-jsapi 1.0.5 只声明两参数 void 接口，没有异步回调或完成合同；在确认官方可用替代机制前，不能重新开启正式线程预览。
- format_document 仍为旧正式链路，T12 分批事务、T13 分批回滚、T14 正式排版切换及之后门禁均未开始。

### 下一步严格顺序

1. 只研究并验证 WPS 官方允许的非阻塞本地进程启动机制或明确的加载项权限配置；不得增加 timeout，不得猜测非官方 ShellExecute 参数。
2. 用脱敏 fixture 做单一启动探针，要求 Host 快速返回且实际出现进程或 result/error 文件。
3. 只有启动探针 PASS 后才临时开启 threadedPreviewEnabled，并重新做正式预览。
4. 正式预览真实 PASS 后，才允许进入 T12–T17。

### 生成文件

- 诊断摘要：.runtime/diagnosis/threaded-preview-s5-20260806.md
- 脱敏完整日志：wps-plugin-debug.log
- 脱敏测试 DOCX：.runtime/thread-shadow-fixtures/snapshot-shadow-20.docx
- 安装 runtime 指针：%APPDATA%/Docxtool/runtime/current.json

### Git

- commit：未执行。
- push：未执行。

## 当前交接入口（最新）

本文件当前最新任务结论见文末“2026-08-06 Local Job Broker 修复（v1.3.0）”。Broker 自动门禁通过，真实 WPS 20/200/1000 识别与正式预览仍待验收；线程预览保持默认关闭。产品发布提交为 `00184ae`，交接文档收口提交将在本轮完成。

## 2026-08-06 Worker-Orchestrated Host Bridge 第一模块

### 结论

- 基线提交：`68ca3bd`。
- 真实 WPS classic Worker：支持；同源探针资源 `pipeline-worker-probe.js`；往返 13 ms。
- 当前状态：`READY_FOR_NEXT_LOOP_T4`，不是线程解耦 PASS。正式 Ribbon 仍调用原 PreviewDocumentUseCase/FormatDocumentUseCase，本轮没有提前替换生产链。

### 已完成微任务

- T0：确认快照全文文本扫描、全文 formatting revision 和执行器重复 activeRevision 三个主线程热路径；加入每 20 段一次的脱敏阶段计时事件。
- T1：实现 classic Worker 能力探针，构建为独立同源单文件并纳入调试包资源/hash 门禁；真实 WPS 探针通过。
- T2：建立纯数据线程协议和运行时校验，拒绝函数、宿主对象形态、未知 RPC 和非法标识。
- T3：实现只读 WpsHostBridge 初始边界：`host.capture_document_descriptor`、`host.read_paragraph_batch`。

### 当前线程架构

```text
WPS Host（探针与只读微 RPC 已具备）
  → classic Worker 探针（真实 PASS）
  → WpsHostBridge
       → 文档描述
       → 段落分批读取（每批最多 10）
```

尚未接入的正式目标架构：

```text
Ribbon → PipelineWorkerClient → Dedicated Worker
       → WpsHostBridge micro RPC → docxtool-recognize.exe
```

### 自动验证

- 基线：typecheck PASS；Node 72/72 PASS；classified build 和 verify add-in PASS。
- 当前：typecheck PASS；Node 76/76 PASS；线程协议和 Host Bridge 针对性测试 6/6 PASS；verify local direct PASS。
- Host Bridge 测试证明只访问指定段落，不读取 Font/ParagraphFormat，批次超过 10 拒绝，document token 变化拒绝。
- 真实 WPS build：`20260805153634-5b734c0c52be`；Worker probe PASS；roundtrip 13 ms。

### 下一步严格顺序

1. T4 实现 PipelineWorkerClient，但不在 Client 中调用正式 use case、全文读取或识别轮询。
2. T5 建立 Worker 状态机和 RPC pending/cancel/late-result 规则。
3. T6 用 Host Bridge 分批读取构建等价快照；通过 0/20/200/1000 段测试后，才切换预览生产入口。
4. T7 删除生产热路径 formattingRevision 和批次间 activeRevision 全文扫描。

### Git

- 本轮未 commit、未 push。
- `.runtime/looper-worker-host-bridge.md` 为忽略的本机状态文件。

## 2026-08-06 Worker-Orchestrated Host Bridge 第二模块（T4–T7）

### 结论

- 基线和当前 HEAD：`68ca3bd`；工作区改动尚未提交或推送。
- 当前状态：`READY_FOR_NEXT_LOOP_T8`，不是完整线程解耦 PASS。
- 正式 `preview_document` 和 `format_document` 仍调用原 `PreviewDocumentUseCase` / `FormatDocumentUseCase`，本轮没有切换生产入口。

### 已完成

- T4：实现 `PipelineWorkerClient`，负责 classic Worker 生命周期、单活动任务、Host RPC 转发、build/job 隔离、崩溃恢复、取消和释放。
- T5：实现仅支持 `snapshot_shadow` 的 Worker 状态机；descriptor 超时 10 秒，段落 RPC 超时 15 秒，批次自适应范围 1—10。
- T6：冻结 `worker-snapshot-v1`，新增 `textRevision`；Worker 内完成段落组合和 SHA-256，不读取或伪造 `formattingRevision`。
- T7：本地调试宿主在 Worker 探针通过后自动执行一次只读影子快照；不调用识别、不写批注、不修改格式、不保存文档。
- 新增生产边界回归测试，锁定正式用例、影子入口和 Worker/WPS API 隔离。
- 新增 `scripts/generate-thread-shadow-fixtures.py`，可重复生成 20/200/1000 段脱敏样本；运行产物位于忽略目录 `.runtime/thread-shadow-fixtures/`。

### 自动测试

- `npm run typecheck`：PASS。
- `npm test`：83/83 PASS。
- `npm run build:classified`：PASS。
- `npm run verify:addin -- classified-offline`：PASS。
- `npm run verify:local-direct`：PASS。
- `git diff --check`：PASS。
- 0/20/200/1000 段快照等价、1000 段多批次且单批不超过 10、cancel、RPC timeout、late result：PASS。
- 三份 DOCX 段落数回读一致并成功渲染：20 段 1 页、200 段 8 页、1000 段 38 页。

### 真实 WPS 影子验证

- WPS 版本：12.1.0.28043；build：`20260805161128-928a6f1f0ca0`；classic Worker probe PASS。
- 20 段：PASS；11 批；Worker 356.5 ms；Host RPC P95 120.1 ms；主线程最大漂移 61.5 ms。
- 200 段：PASS；104 批；Worker 1029 ms；Host RPC P95 9.8 ms；主线程最大漂移 165.3 ms。
- 1000 段：PASS；983 批；Worker 12268 ms；Host RPC P95 17.2 ms；主线程最大漂移 1218 ms。
- 三次完成事件均来自当前 build，完成后 WPS 进程均为 `Responding=True`；未触发“批次降为 1 后重试”的停止分支。
- 1000 段最大单次 Host RPC 1293.4 ms，虽未造成未响应，仍作为 T8 前的尾延迟观察项保留。

### 下一步严格边界

从 T8 开始处理 recognition launcher/probe、Worker recognition coordinator、Worker 内命令生成和预览批次。只有完整 Worker 预览链具备并通过真实 WPS 验证后，才允许切换正式 `preview_document`。

## 2026-08-06 同段标题正文识别信号核对

### 结论

- 指定工作副本中已保存的批注是旧结果：目标物理段只有一个覆盖整段的“主标题续行”批注。
- 使用当前已安装 wheel/SDK 对同一文件重新只读识别后，该段返回两个定位已确认的块：一级标题范围 `[0, 7]`、正文范围 `[7, 457]`；`segment_count_total=2`、定位数和确认数均为 2。
- 文首粘连段当前也已拆为主标题、主标题续行和职务姓名三个已验证子范围；旧 DOCX 批注仍只有覆盖整段的主标题批注。
- 因此当前 wheel 分类与子范围输出正确；截图反映的是旧批注尚未清理/当前预览尚未重新执行，不是 WPS 收到正确子范围后显示错误。

### 后续验证点

- 重新执行预览时应先清理旧 Docxtool 批注，再按当前子 Range 写入新批注。
- 同段标题和正文只能预览并提示拆段；正式排版不得把两套段落级格式直接写入同一个物理段落。
- 本轮仅诊断并更新文档，未修改业务代码，未 commit、未 push。

## 2026-08-06 GitHub 快速发布 v1.1.0

- 根项目与涉密离线插件版本统一升级为 `1.1.0`，构建信息由插件包版本自动生成。
- 发布范围只包含当前仓库的源码、脚本、测试和项目文档；IDE 配置、分析工具目录、真实 DOCX、测试工作副本、日志和运行产物均排除。
- `.gitignore` 已补充上述本地文件规则，后续快速发布可直接提交受管理代码，不会误带用户文档。
- 发布前门禁：`npm test` 72/72 PASS；`npm run verify:local-direct` PASS；Python 编译 PASS；资源服务脚本 Ruff PASS；`git diff --check` PASS。
- 目标远端：`7654321x/wps--plugin`；目标分支：`main`；发布标签：`v1.1.0`。

## 2026-08-06 WPS runtime 路径错误分析与修复计划

### 日志结论

- WPS 已执行 `main.js`、Ribbon 和 `host-runtime.js`，并成功调用 `OnAddinLoad`；因此本轮不是功能区回调未注册。
- Host 在每次安装尝试中报：`读取本地运行时配置失败；错误：path cannot contains '/' or '\\'`。
- 安装条件中的 `runtime_config_available` 始终为 false，随后每 250ms 重试；本地 Runtime 没有安装，预览排版、一键排版和本机检测自然不会进入正式 Use Case。
- 当前 `current.json` 和本地 exe 均存在，问题发生在 WPS `Application.FileSystem` 的路径参数语义，不是 runtime 构建或 wheel 安装失败。

### 修复计划（本轮只分析，未改业务代码）

1. **确认宿主路径语义**：在 WPS Host 内新增一次性脱敏探针，只记录路径类型、分隔符形态、是否为绝对路径和每个 API 的稳定错误码，不记录完整路径；分别验证 `GetAppDataPath`、`Exists`、`ReadFileString`、`Mkdir`、`Remove`。
2. **统一路径适配边界**：在 WPS 本地文件系统适配层实现唯一规范化和能力探测，runtime 清单、exe、临时请求/结果文件、日志文件全部经过同一入口；不在各业务模块自行替换斜杠。
3. **修复 runtime 配置读取**：让 `runtimeConfig()` 使用已验证的路径形式读取 `current.json`，并对清单内容、exe 路径和 SHA-256 做完整校验；失败时返回明确中文错误，不再把异常伪装成无限等待。
4. **收敛重试日志**：安装失败进入稳定状态并限频，避免每 250ms 重复刷 WARN；Ribbon 仍可显示失败原因，但不能调用未安装的正式用例。
5. **回归验证**：先运行 TypeScript/Node 测试和路径适配器测试；再完整重载 WPS，确认 `application.install.success`；随后按“本机检测 → 预览排版 → 一键排版”顺序验收，并检查日志和保存后的 DOCX。

### 当前状态

- 资源服务、插件构建、注册和 WPS 回调：已自动确认。
- 本地应用 Runtime 安装：失败，错误为路径参数格式异常。
- 真实格式写入：尚未开始，不能标记 PASS。
- 本轮未 commit、未 push。

## 2026-08-06 runtime 路径与日志刷屏修复

### 已完成

- 新增 `normalizeWpsPath()`，在 WPS `FileSystem` 边界统一处理混合 Windows 路径分隔符。
- runtime 清单读取、诊断日志路径、临时识别请求/结果文件统一使用规范化路径。
- Host 安装失败日志改为首次记录、之后最多每 5 秒记录一次；安装条件检查每 20 次记录一次，避免 250ms 刷屏。
- `main.py` 实时控制台只监视根目录 `wps-plugin-debug.log`，不再把旧 `.runtime/wpsjs-debug.log` 的乱码和重复内容混入 WPS 交互日志。
- 控制台输出已统一 UTF-8 中文。

### 自动验证

- `npm test`：71/71 PASS。
- `npm run verify:local-direct`：PASS。
- Python 编译：PASS。
- `ruff check scripts/wps_debug_server.py`：PASS。
- WPS 调试包已重新生成，资源探针：PASS。

### 真实 WPS 状态

- 当前旧 WPS 会话仍未重新加载本次构建，因此尚未验证 `application.install.success`。
- 重新加载后必须检查：`application.install.success` → `本机检测` → `预览排版` → `一键排版`。
- 未完成真实 WPS 重载前，不把路径修复标记为真实宿主 PASS。

## 2026-08-06 官方 FileSystem 读取与中文日志最终修复

### 问题与根因

- 混合斜杠规范化后的新构建仍报 `path cannot contains "/" or "\\"`，说明根因不是分隔符形式。
- 已安装的官方 `wps-jsapi 1.0.5` 声明读取方法为 `Application.FileSystem.ReadFile(path)`；项目此前优先调用非官方兼容方法 `ReadFileString`，真实 WPS 会把该参数按不允许包含目录分隔符的形式校验。
- 早期日志还会每 250ms 尝试使用 WPS FileSystem 建立文件日志，失败时持续累积相同事件；Python 子进程输出固定按 UTF-8 解码，遇到 GBK/GB18030 输出会显示乱码。

### 已完成修复

- `WpsLocalFileSystem.readText()` 优先使用官方 `ReadFile`，兼容方法仅作旧宿主回退；`WriteFile` 作为宿主能力回退加入适配层。
- runtime 清单、exe 检测和健康检查统一经过 `WpsLocalFileSystem`，不再各自直接调用不同字段。
- 官方单参数 `AppendFile(path)` 不再被误作 `AppendFile(path, content)`；只有明确暴露两参数实现时才走该兼容路径。
- `bootstrap-probe.js` 移除 250ms FileSystem 文件日志轮询；早期、Ribbon 和 Host 中文事件统一通过 3889 诊断端点写入根目录 `wps-plugin-debug.log`。这不改变本地识别/排版直连链路。
- `main.py` 设置 Windows 控制台 UTF-8，并对捕获的子进程字节按 UTF-8、GB18030 顺序严格解码；连续相同 WPS 事件只显示一次，后续以省略计数汇总。
- `main.py diagnose/status` 不再只依赖 build-id 尚未加载时产生的首个 bootstrap 事件；当前构建出现 Ribbon 或 Host 模块事件也会正确显示“插件页面已加载”。
- 路径拒绝错误增加稳定错误码 `WPS_FILESYSTEM_PATH_REJECTED` 和中文说明。

### 自动验证

- `npm test`：72/72 PASS。
- `npm run verify:local-direct`：PASS。
- TypeScript build/typecheck：PASS。
- Python `py_compile`：PASS。
- `ruff check scripts/wps_debug_server.py`：PASS。
- `git diff --check`：PASS。
- 资源探针与 WPS 注册检查：PASS。

### 真实 WPS 验证

- 已确认所有旧 WPS 进程均无可见窗口和活动文档后，仅关闭后台残留并打开脱敏测试副本。
- 当前真实加载 build：`20260805144325-b0b446021681`。
- 当前 build 事件统计：`application.install.attempt=1`、`application.install.success=1`、`application.install.config.failed=0`。
- Ribbon `OnAddinLoad` 成功；路径拒绝和安装轮询刷屏已停止。
- 本轮未通过坐标脚本点击 Ribbon。下一步只需在当前 WPS 中触发“本机检测”，观察同一正式回调的 `ribbon.action.received` 与 `application.runtime.run.received`；若写入链仍有问题，再按新的稳定中文错误继续定位。

### Git 状态

- 本轮未 commit、未 push。

## 当前交接入口（2026-08-06 T8–T17）

历史结论已由文末“Local Job Broker 修复（v1.3.0）”更新：生产识别启动已切换为无端口文件队列 Broker；真实 WPS 20/200/1000 识别与正式预览仍待验收，线程预览保持默认关闭。产品代码已由 `00184ae` 推送到 `main`。

## 2026-08-06 自动发布规则

- 用户已授权：以后每次大功能、协议/架构或影响用户体验的 WPS 修复完成后，自动更新版本并推送 GitHub。
- 普通修复递增补丁号；新功能、协议或架构改动递增次版本号；大版本号需用户确认。
- 根项目与 apps/classified-offline 版本号保持一致；发布前执行完整自动门禁，确认远程为 7654321x/wps--plugin，推送到 main，并记录 commit/push 结果。

## 2026-08-06 WPS Native Launch Boundary L1（v1.2.0）

### 当前结论

- 已严格停在 L1：实现单参数 `OAAssist.ShellExecute(executablePath)` 最小探针；未实现队列 launcher、未替换 Worker 识别链、未开启线程预览、未启动旧 HTTP 服务、未进入 COM。
- 单独 `docxtool-launch-probe.exe` 已构建、安装并启动成功，写入 `process-started.json`，`schema_version=1`、`argv_count=1`。
- 真实 WPS Host 尚未记录 PASS：当前 WPS 仍有可见用户文档旧会话，未强制关闭或重启以加载本轮 build；因此没有伪造 `returned_in_ms` 或真实 WPS 进程启动结论。`threadedPreviewEnabled` 继续为 `false`。

### 实际改动

- 新增 `local-runtime/launch_probe.py`：只写原子启动证明文件，不读取 DOCX、不读取请求、不启动识别。
- 新增 `scripts/build-launch-probe.ps1` 与 `scripts/install-launch-probe.ps1`，对应 npm 脚本 `build:launch-probe`、`install:launch-probe`。
- 新增 debug-only Host RPC `host.probe_shell_execute_one_argument`，并在开发任务窗格提供“本地启动边界探针”入口；生产识别双参数链仍未改变，正式预览仍受门禁保护。
- 新增问题清单 P049，防止探针再次带入 Params 参数。
- 版本同步为 `1.2.0`，构建信息由构建脚本生成。

### 自动验证

- `.venv` Python：`local-runtime/tests` 5 passed。
- Node：`npm test` 95 passed。
- `npm run verify:all`：PASS。
- `npm run verify:local-direct`：PASS。
- `python -m ruff check local-runtime scripts tests`：PASS。
- L1 Node 探针测试确认 `ShellExecute` 参数数组长度严格为 1；EXE 构建 SHA-256 与安装副本一致。

### 下一步严格顺序

1. 在已重载当前 build 的脱敏 WPS 会话中触发“本地启动边界探针”，记录 Host 返回耗时及 5 秒内 marker 文件。
2. 只有 L1 真实 PASS 才进入 L2 零参数队列 launcher；若仍阻塞，停止 L 路线并进入 C0 COM 能力探针。
3. L2 真实 PASS 后才接入 Worker 识别（20/200/1000），识别 PASS 后才临时开启线程预览。

### 文件与发布状态

- 状态文件：`.runtime/looper-wps-native-launch-boundary.md`（本地产物，已被忽略，不上传）。
- 本轮版本：`1.2.0`；发布 commit：`b22704c`（`release: v1.2.0 add native launch probe boundary`）。
- 已推送到 `origin` 的 `main`；远程 `refs/heads/main` 已核对为 `b22704cd30c7863e350de8b015f44055b277ffd2`，工作树保持干净。

## 2026-08-06 自动发布规则落地 v1.1.1

- 已将自动版本与 GitHub 发布规则写入 `AGENTS.md`，后续大改完成后按规则自动执行版本同步、门禁和发布。
- 本次文档/流程变更版本为 `1.1.1`；根项目、`apps/classified-offline`、锁文件和构建信息已同步。
- 自动验证：`npm run typecheck`、`npm test`（94 项通过）、`npm run verify:all`、`npm run verify:local-direct`、`git diff --check` 均通过。
- 真实 WPS 仍沿用既有 `BLOCKED` 结论：ShellExecute 同步阻塞问题未伪报为通过。

## 2026-08-06 Local Job Broker 修复（v1.3.0）

### 当前结论

- baseline_head：`815b6debd3c0bf8b5f437b96a53a2daf61ffca8e`。
- 工作区目标：移除生产识别链对 WPS `OAAssist.ShellExecute(exe, params)` 的依赖，接入无端口文件队列 Broker；Worker、分批快照、识别映射、命令生成和既有预览批次未重写。
- 当前 Broker：`docxtool-job-broker.exe`，版本 `1.0`，状态 `READY`，PID `6296`；通信方式为 `%APPDATA%\Docxtool\jobs\<uuid-v4>` 文件队列，无 HTTP/WebSocket/TCP 端口。
- 线程预览：`threadedPreviewEnabled=false`。真实 WPS 20/200/1000 识别、正式预览和 1000 段写入复验尚未执行，不能写成 PASS。

### 实际改动

- 新增 `local-runtime/job_broker.py`：固定 AppData 队列、原子 claim、状态/心跳、runtime/exe/hash 校验、单任务启动、取消和失败结果。
- 扩展 runtime 构建/安装清单，同时生成并安装 `docxtool-recognize.exe` 与 `docxtool-job-broker.exe`；安装时只按旧 `current.json` 精确路径停止被锁定的旧 Broker。
- `main.py` 增加 Broker healthy reuse、stale 精确重启、实际 PID 记录、SHA-256 校验、状态输出和 `stop` 管理；不结束未知进程。
- `WpsRecognitionJobService` 与 `LocalProcessRecognitionTransport` 改为只写 request/queued 并快速返回，Worker 按 queued/claimed/launched/result/error 状态探测；生产识别启动不再调用 ShellExecute。
- runtime 配置、健康检查、稳定中文错误码和 `RecognitionJobHandle` 合同已同步；保留单参数 ShellExecute debug 探针作为历史隔离测试，但它不属于生产识别链。
- 新增 Broker Python 测试与安装 recognizer smoke 脚本；版本同步为 `1.3.0`，构建信息由构建脚本生成。

### 自动验证

- `.venv` Python `local-runtime/tests`：8 passed。
- `npm test`：95/95 passed。
- `npm run typecheck`：PASS。
- `npm run build:local-runtime`：PASS，双 PyInstaller EXE、Broker `--help`/`--once` 门槛通过。
- `npm run install:local-runtime`：PASS，recognizer/Broker 安装副本 SHA-256 与清单一致。
- `npm run verify:local-direct`：PASS；包含实际 Broker → recognizer → result smoke：`tests/fixtures/wps-e2e-baseline.docx`，32 段、31 blocks、31 binding。
- `npm run build:classified`、`npm run verify:addin -- classified-offline`：PASS。
- `git diff --check`：PASS。

### 真实 WPS 验收状态

- 本轮未把当前新构建写入真实 WPS 验收 PASS；尚未完成当前 build 的完整宿主重载和按钮点击。
- 当前调试包 build：`20260806030230-212ee48af0a1`；`main.py start --once` 已按正式顺序完成构建、安装、Broker 启动/复用、调试包和校验。
- B9：20/200/1000 Broker 识别，未验收。
- B10：20/200/1000 正式线程预览，未验收。
- B11：1000 段真实 WPS 分批写入、取消、文档切换，未验收。
- 已知自动 smoke 中，`005班子对照检查材料_正确格式.docx` 返回识别器自身 `InvalidRecognitionPlanError`；这不是 Broker 启动失败，也没有被计为通过。受管理脱敏 fixture smoke 已通过。

### 未解决问题

- 真实 WPS 尚未验证 Host queue RPC 小于 200ms、Broker 1 秒内 claim、recognizer 2 秒内启动、WPS Responding=True，以及 20/200/1000 识别结果。
- 由于 B9–B11 未完成，不能开启 `threadedPreviewEnabled`，不能进入 T12–T17。
- 需要在真实 WPS 中观察 `pipeline.recognition.launch → host.recognition.job.queued → broker.job.claimed → broker.recognizer.started → recognition.result.ready → pipeline.recognition.complete`，并保存脱敏指标。

### 下一步严格顺序

1. 运行 `main.py start --once` 生成并服务当前 v1.3.0 调试包，按规则重载真实 WPS 当前脱敏文档。
2. 先只做 20、200、1000 段“线程识别自测”，不写批注；记录队列耗时、claim、启动、result/error、blocks/binding、WPS Responding。
3. 三个样本均 PASS 后，临时开启线程预览并按 20、200、1000 段顺序验收正式预览。
4. 正式预览真实 PASS 后再复验既有 1000 段批注/写入、取消和文档切换；通过后才进入 T12–T17。

### 生成文件路径

- 源码：`local-runtime/job_broker.py`、`scripts/verify-local-recognizer-smoke.py`。
- 安装清单：`dist/local-runtime/win-x64/runtime-manifest.json`（本地生成产物）。
- 安装指针：`%APPDATA%\Docxtool\runtime\current.json`。
- Broker 状态：`%APPDATA%\Docxtool\broker\status.json`；日志：`.runtime\logs\local-job-broker.log`。
- Looper 状态：`.runtime\looper-local-job-broker.md`。

### Git

- commit：`00184ae`（`release: v1.3.0 add port-free local job broker`）；交接文档收口提交：`c19629b`（`docs: record v1.3.0 broker publication`）。
- push：两个提交均已推送到 `ssh://git@ssh.github.com:443/7654321x/wps--plugin.git` 的 `main`。
- 发布前门禁已完成：`npm test`、`npm run verify:all`、`npm run verify:local-direct`、`git diff --check` 均 PASS；真实 WPS 未验收状态已如实记录。

## 2026-08-06 WPS 插件补丁发布（v1.3.1）

### 当前结论

- 本次发布目标仅为 `D:\PycharmProjects\wps` WPS 插件仓库，对应 GitHub `7654321x/wps--plugin`；未修改 `D:\PycharmProjects\docxtool`。
- 根项目、`apps/classified-offline` 和锁文件版本已同步从 `1.3.0` 升至 `1.3.1`。
- 构建信息由 `build:classified` 自动重新生成，当前 build：`20260806033948-212ee48af0a1`。
- `threadedPreviewEnabled=false` 保持不变；真实 WPS 20/200/1000 识别、正式预览和 1000 段写入仍沿用未验收结论，不能写成 PASS。

### 实际改动

- `package.json`：版本 `1.3.1`。
- `package-lock.json`：根包版本同步为 `1.3.1`。
- `apps/classified-offline/package.json`：插件版本 `1.3.1`。
- `apps/classified-offline/ui/build-info.js`：由构建脚本自动生成。
- `问题清单Agent.md` 已复核，本轮未发现新的可复发问题，未新增条目。

### 自动验证

- `npm run build:classified`：PASS。
- `npm test`：PASS，95/95。
- `npm run verify:all`：PASS，TypeScript、Node 95 项、Python 34 项、构建和加载项校验均通过。
- `npm run verify:local-direct`：PASS，本地直连 smoke 为 32 段、31 blocks、31 bindings。
- `git diff --check`：PASS；Git 仅报告 Windows 换行转换提示，无 whitespace 错误。

### Git

- release commit：`ef6bc98`（`release: v1.3.1 WPS plugin patch release`）。
- push：PASS；`ef6bc98` 已推送到 `ssh://git@ssh.github.com:443/7654321x/wps--plugin.git` 的 `main`。

## 2026-08-06 固定 GitHub 上传流程与 v1.3.1 审阅补丁

### 当前结论

- 已将固定 GitHub 上传流程写入 `AGENTS.md`：唯一仓库、允许/禁止文件、版本与交接文档同步、独立门禁、显式暂存、快速发布、远程核对和审阅 patch 上传方式均已固化。
- 本轮只增加流程规则和审阅证据附件，不修改产品源码、协议或运行链，产品版本继续为 `1.3.1`。
- `threadedPreviewEnabled=false` 保持不变；真实 WPS 20/200/1000 识别、正式预览和 1000 段写入仍未验收，不能写成 PASS。

### 审阅补丁

- 文件：`agent/reviews/wps-plugin-v1.3.1-review.patch`（原根目录文件已整理到 Agent 审阅目录）。
- 来源：`git show 1be9b8f --stat --patch --find-renames`。
- SHA-256：`053A0909F33E67256BAE75839D86BE8C41B2A260B51977F7AFE5E85DDCD65220`。
- 扫描：未发现 `.venv`、`node_modules`、`.runtime`、日志、DOCX、`local_recycle`、token 或 session 内容。

### Git

- 流程规则与审阅附件提交：`def6e62`（`docs: upload v1.3.1 review patch and fix GitHub flow`）。
- push：PASS；`def6e62` 已推送到 `ssh://git@ssh.github.com:443/7654321x/wps--plugin.git` 的 `main`。

## 2026-08-06 WPS 插件 v1.4.1 交互闭环修复

### 当前结论

- 已在 `D:\PycharmProjects\wps` 完成 v1.4.1 最小交互闭环修复并同步到 GitHub `7654321x/wps--plugin`。
- 真实确认的两个根因：Ribbon 直接对 Host 返回值调用 `.then()`，同步返回值会在回调中被当作异常；Host `tryInstall()` 在安装异常后错误地设置完成标记并清理重试定时器，导致后续注入 `Application` 也无法恢复。
- 开发与生产仍使用唯一 `main.js`、`js/ribbon.js` 和生产构建生成的 `host-runtime.js`，没有新增第二套入口；`DocxtoolHostEnqueue` 未重新引入。
- 构建信息：`20260806113251-95f45401f9f5`；`threadedPreviewEnabled=false` 保持不变。

### 实际改动

- `apps/classified-offline/js/ribbon.js`：Ribbon 继续按按钮 ID 映射到 `window.DocxtoolRunLocalCommand`，改用 `Promise.resolve(...)` 同时处理同步结果、异步结果和 rejected Promise。
- `apps/classified-offline/src/host-runtime.ts`：以单一 `InstallState` 表达 `waiting/installing/ready/retryable_failed/fatal`；可恢复安装异常继续使用同一个幂等定时器重试，成功或明确配置致命错误才停止；成功初始安装不会再被安装后的定时器重新创建。
- `tests/wps-phase-one.test.mjs`：新增同步 Host 返回、rejected Promise、安装失败重试/停止条件和真实 classified `dist` 产物入口门禁。
- `package.json`、`package-lock.json`、`apps/classified-offline/package.json`：版本同步为 `1.4.1`。
- `apps/classified-offline/ui/build-info.js`：由 `npm run build:classified` 自动生成，未手工伪造 build id。
- `问题清单Agent.md`：已按要求复核；本问题已有 P020/P031/P034/P036 相关约束，本轮未新增重复条目。

### 调用链变化

修改前：

`Ribbon → DocxtoolRunLocalCommand(...).then(...) → 同步返回时抛错；Host 安装异常 → installDone=true → 清理 timer → 永久停止重试`

修改后：

`Ribbon → Promise.resolve(DocxtoolRunLocalCommand) → LocalApplicationRuntime → command dispatcher → HostResultStore 状态/错误回写`

`Application/BuildInfo/配置未就绪 → waiting → 同一个 retry timer；可恢复安装异常 → retryable_failed → 继续重试；安装成功 → ready → 停止 timer；明确配置错误 → fatal → 停止 timer并写入错误`

### 自动验证

| 验证项 | 命令 | 结果 | 证据 |
| --- | --- | --- | --- |
| 完整 Node 测试 | `npm test` | PASS | 103/103 |
| TypeScript、Node、Python、构建和加载项总门禁 | `npm run verify:all` | PASS | `VERIFY_ALL_PASS`；Python 40 passed |
| classified 生产构建 | `npm run build:classified` | PASS | `host-runtime.js`、`js/ribbon.js`、pipeline 产物生成 |
| 本地直连验证 | `npm run verify:local-direct` | PASS | 32 段、31 blocks、31 bindings；`VERIFY_LOCAL_DIRECT_PASS` |
| 加载项生产产物校验 | `npm run verify:addin -- classified-offline` | PASS | `ADDIN_VERIFY_PASS docxtool-classified-offline` |
| 工作树差异检查 | `git diff --check`、`git diff --cached --check` | PASS | 无 whitespace 错误 |

### 真实 WPS 验证

- 代码和自动化构建已验证：PASS。
- 真实 WPS 本轮未启动、未实际点击 Ribbon 按钮：NOT RUN；不能据此声称 Host Ready、按钮响应或安装失败后真实恢复已通过。
- 手工验收仍需按“完全退出 WPS → 清理/确认插件缓存 → 启动 WPS → 打开本地 DOCX → 确认 Host Runtime Ready → 依次点击预览/一键排版/状态面板/本机检测 → 模拟 Application 延迟注入并观察恢复”的顺序执行。

### 未解决问题

- 真实 WPS 中的按钮点击、Runtime Ready、可恢复安装失败后的自动恢复、重复点击和重复 Worker/监听器仍待验收。
- 20/200/1000 段真实识别、正式预览、批注读回和 1000 段写入不属于本轮交互闭环自动门禁，仍不能写成 PASS。
- PluginStorage 单槽位请求覆盖风险、真实 WPS Worker 启动和正式预览仍按后续任务处理，本轮未扩大协议或业务范围。

### 下一步严格顺序

1. 完全关闭全部 WPS 进程，确认加载的是 build `20260806113251-95f45401f9f5`。
2. 打开脱敏本地 DOCX，确认 `host.module.loaded`、`application.install.success` 和 Host Runtime Ready 状态。
3. 依次点击四个 Ribbon 按钮，核对 `control_id`、`command_name`、`request_source`、`runtime_version`、`build_id`、`result` 和 `error_code`。
4. 通过延迟 Application 注入模拟可恢复安装失败，确认无需重启 WPS 即进入 ready；再做连续点击和双按钮快速点击检查。
5. 真实 WPS 交互闭环通过后，才继续 20/200/1000 识别、正式预览和写入验收。

### 生成文件路径

- 构建信息：`apps/classified-offline/ui/build-info.js`。
- 生产构建目录：`apps/classified-offline/dist/`（按规则忽略，不提交）。
- 本轮未生成审阅 patch、日志、DOCX 或运行时产物。

### Git

- release commit：`b4336f2ad73ac42e3278b38b38977adb99aa643d`（`release: v1.4.1 restore Ribbon Host command bridge`）。
- push：PASS；本地 HEAD 与 `origin/refs/heads/main` 均为 `b4336f2ad73ac42e3278b38b38977adb99aa643d`。

## 2026-08-06 WPS 插件 v1.5.0 中文统一日志与错误暴露重构

### 当前结论

- v1.5.0 已完成统一运行日志改造，业务识别、预览、排版、Worker、Broker 和 Control Server 合同未改变。
- 自动门禁已完成；v1.5.0 release 提交和第一次 GitHub push 已完成，交接文档回写正在追加。

### 实际改动

- 新增根目录 `wps_logging.py`，把结构化事件统一格式化为 UTF-8 中文文本并写入唯一运行日志 `wps-plugin.log`。
- 错误日志统一输出阶段、原因、处理建议和稳定错误码；连续相同事件抑制，状态变化或恢复事件重新记录。
- 统一执行敏感信息、Bearer、绝对 Windows 路径、URL 和完整异常堆栈的脱敏；不输出 DOCX 正文、请求对象或 JSONL 行。
- 文件超过上限时仅裁剪历史完整行，不生成 `.1`、`.2` 等轮转日志；Broker 识别进程 stdout/stderr 不再生成 `jobs/<id>.log`，统一事件写入 `wps-plugin.log`。
- `main.py`、WPS 资源服务、local-agent、任务窗格、Ribbon、启动探针和诊断脚本均收敛到上述日志边界；`status/diagnose` 使用中文摘要。
- 删除废弃的 `LoopbackDiagnosticLogger` 和 WPS FileSystem `BoundedDiagnosticFileBuffer` 旧日志链；旧 `wps-plugin-debug.log` 仅保留本机现有文件，不再跟踪。
- `.gitignore` 忽略 `wps-plugin.log`、`wps-plugin-debug.log` 和 `.runtime/logs/`；运行日志不作为发布文件上传。
- 新增 `tests/test_unified_logging.py`，更新 local-agent、Broker 和 WPS 阶段测试，加入统一日志门禁。
- 版本同步为 `package.json`、`package-lock.json`、`apps/classified-offline/package.json` 的 `1.5.0`；构建信息由 `npm run build:classified` 生成。

### 自动验证

| 验证项 | 命令 | 结果 | 证据 |
| --- | --- | --- | --- |
| TypeScript 类型检查 | `npm run typecheck` | PASS | `tsc -p tsconfig.base.json --noEmit` |
| Node 测试 | `npm test` | PASS | 96/96 |
| 完整总门禁 | `npm run verify:all` | PASS | Node 96/96、Python 59 passed、构建与加载项校验通过 |
| 本地直连门禁 | `npm run verify:local-direct` | PASS | 32 段、31 blocks、31 bindings；`VERIFY_LOCAL_DIRECT_PASS` |
| 本地 runtime/Broker 构建 | `npm run build:local-runtime` | PASS | PyInstaller 生成识别器和 Broker；Broker 包含 `wps_logging` |
| Python 编译 | `python -m py_compile main.py scripts/wps_debug_server.py` | PASS | 无编译错误 |
| Ruff | `python -m ruff check control-server command-service local-agent local-runtime scripts tests wps_logging.py` | PASS | All checks passed |
| 工作树差异检查 | `git diff --check -- . ':(exclude)*.patch'` | PASS | 无 whitespace 错误 |

### 真实 WPS 验证

- 真实 WPS 中文日志验收：未运行，未标记通过（`NOT RUN`）。
- 因此不能据此声称当前 build 已完成 Host 装载、按钮点击、真实识别、预览批注、保存后 OOXML 复查或 20/200/1000 段验收。

### 未解决问题

- 需要在完整门禁通过后，按“完全退出 WPS → 重载当前 build → 检查 `wps-plugin.log` → 依次执行本机检测、预览排版、一键排版 → 保存后复查 DOCX/OOXML”的严格顺序进行真实 WPS 验收。
- 本地直连门禁已在新 runtime/Broker hash 和当前状态一致后通过；真实 WPS 仍需单独重载当前 build，不能用本机 Broker 门禁替代宿主验收。

### 下一步严格顺序

1. 运行版本构建、类型检查、Node/Python 测试和两个固定门禁。
2. 审阅 `git diff`，确认只保留源码、测试、规则和交接文档，运行日志、DOCX、`.runtime` 和本机产物不进入暂存区。
3. `release: v1.5.0 unify Chinese runtime logging` 已提交并推送 `origin HEAD:main`。
4. 已核对本地 HEAD 与远程 `refs/heads/main`；交接回写使用独立 `docs:` 提交同步。
5. 完成真实 WPS 验收；未通过前继续保持 `NOT RUN` 或明确失败，不写 `PASS`。

### 生成文件路径

- 统一日志实现：`wps_logging.py`。
- 统一日志测试：`tests/test_unified_logging.py`。
- classified 构建信息：`apps/classified-offline/ui/build-info.js`（由构建脚本生成）。
- 运行期日志：`wps-plugin.log`（本机产物，忽略，不提交）。

### Git

- classified 构建信息：`20260806135945-c1ef25d8d2e9`，插件版本 `1.5.0`。
- release commit：`d945b3bf9991f64ec040967bb93c86f38a36b452`（`release: v1.5.0 unify Chinese runtime logging`）。
- push：PASS；release 提交和后续交接 `docs:` 提交均已推送，最后一次 push 后已核对本地 HEAD 与 `origin/refs/heads/main` 一致；目标远程为 `ssh://git@ssh.github.com:443/7654321x/wps--plugin.git` 的 `main`。

## 2026-08-06 WPS 插件 v1.5.1 Broker 启动超时与精准诊断修复

### 当前结论

- v1.5.1 已修复本地任务 Broker 启动等待被昂贵进程查询拖长的问题；真实 `main.py start --once` 已完成，Broker 在约 1 秒内进入 READY，完整启动继续执行，未再出现约 53 秒的笼统超时。
- `status` 和 `diagnose` 已复用同一 readiness 检查器，并能返回具体阶段、原因、建议、错误码和必要的期望/实际摘要。
- 真实 WPS 宿主尚未重新加载当前构建，仍记为 `NOT RUN`；不能把插件页面、按钮、真实 DOCX 保存或 20/200/1000 段验收写成通过。

### 实际根因

- 原 `main.py broker_healthy()` 只返回 `True/False`，状态文件不存在、状态不完整、心跳过期、版本/hash/合同不一致和进程身份不一致均被隐藏为同一个结果。
- 启动循环每 100ms 调用一次 `broker_healthy()`；该函数又调用 `Get-Process` 与 `Get-CimInstance`。单次进程查询实测约 0.95 秒，50 次轮询把名义上的 5 秒循环拖到约 52–53 秒。
- 真实 PyInstaller one-file 运行时存在启动父 PID 与状态文件实际工作 PID 不同的父子进程关系；本次复现中 Popen PID 为 `13644`，状态 PID 为 `10652`，两者都指向当前 Broker 路径。启动期间应使用本次 `Popen.poll()` 判断退出，READY 后再对状态 PID 做一次身份校验。
- 另发现 WMI 创建时间带 7 位小数、Broker 状态时间带 6 位小数；Python 3.8 直接解析前者会误报时间不一致，已在 UTC 边界统一截断到 6 位并加入回归测试。

### 实际改动

- `main.py`：新增严格 `read_broker_status()`；拆分快速 readiness 和单次进程身份检查，分别覆盖状态文件、状态、PID、心跳、Broker/runtime 版本与 SHA-256、queue/合同、可执行文件、命令行和进程创建时间错误。
- `main.py`：启动等待使用 `time.monotonic()` 的 10 秒预算；等待期间只使用本次 `Popen.poll()`，快速检查通过后进程身份查询最多一次；相同原因不刷屏，原因变化只记录一次，最终保留最后一个明确错误。
- `main.py`：`status`、`diagnose` 输出 Broker 失败阶段、具体原因、错误码、建议和摘要；顶层失败日志和命令行不再覆盖底层真实错误。
- `local-runtime/job_broker.py`：Broker 尽早写入唯一中文日志中的进程启动、runtime 清单读取/校验、队列初始化、状态文件创建、READY 和初始化失败阶段；启动清单不满足时返回真实错误并以失败退出。
- `scripts/build-local-recognition-runtime.ps1`：先生成构建 manifest 和 smoke 用 `current.json`，再执行严格 Broker `once` 检查，修复构建检查顺序错误。
- `wps_logging.py`：补充 Broker readiness 错误目录，保证错误码能够映射到稳定中文阶段/原因/建议。
- `tests/test_broker_readiness.py`：新增状态损坏/缺失、心跳、版本/hash/合同、PID、进程身份、单次查询、10 秒单调时钟预算、WMI 7 位小数和 Broker 启动日志测试。
- `docs/问题清单Agent.md`：新增 P061，固定 Broker 启动轮询和精准诊断的防复发门槛。
- `package.json`、`package-lock.json`、`apps/classified-offline/package.json`、`README.md` 和生成的 `apps/classified-offline/ui/build-info.js`：版本同步为 `1.5.1`。

### 自动验证

| 验证项 | 命令 | 结果 | 证据 |
| --- | --- | --- | --- |
| Broker readiness 快速测试 | `python -m pytest tests/test_broker_readiness.py local-runtime/tests/test_job_broker.py -q` | PASS | 31 passed |
| Python 编译 | `python -m py_compile main.py local-runtime/job_broker.py` | PASS | 无编译错误 |
| TypeScript/Node 测试 | `npm test` | PASS | Node 96/96 |
| 完整总门禁 | `npm run verify:all` | PASS | Python 59 passed、Node 96/96、构建和加载项校验通过 |
| 本地直连门禁 | `npm run verify:local-direct` | PASS | 32 段、31 blocks、31 bindings；`VERIFY_LOCAL_DIRECT_PASS` |
| runtime/Broker 构建 | `npm run build:local-runtime` | PASS | PyInstaller 识别器和 Broker 构建、`--help` 与清单后的 `--once` smoke 通过 |
| classified 构建 | `npm run build:classified` | PASS | 生成版本化构建信息 |
| 工作树差异检查 | `git diff --check -- . ':(exclude)*.patch'` | PASS | 无 whitespace 错误 |
| Ruff | `python -m ruff check main.py local-runtime tests/test_broker_readiness.py` | WARN | 本轮新增告警已清除；`main.py` 原有单行语句 E701/E702 仍存在，未做无关格式重构 |

### 真实启动与状态事实

- `python main.py start --once`：PASS；构建、安装、Broker、插件构建、结构检查、Control Server、资源服务和本地直连功能检测均完成。
- Broker 启动日志已出现：`本地任务代理进程已启动`、`运行时清单校验通过`、`状态文件已创建`、`本地任务代理已就绪`。
- 最新 `status`：本地任务 Broker 已就绪；Broker PID 有效，Broker 版本 `1.3.2`，Control Server 使用 `127.0.0.1` 随机端口 `55950`，旧 `9528`、`local-agent`、`command-service` 均未使用。
- 最新状态文件：`%APPDATA%\Docxtool\broker\status.json` 为 `READY`，`last_error_code` 为空；`current.json`、status 和 `.runtime\local-job-broker-process.json` 的 Broker/runtime 哈希、版本、合同、PID 和创建时间一致。
- 最新 classified 构建信息：`apps/classified-offline/ui/build-info.js`，build id `20260806144203-c1ef25d8d2e9`，插件版本 `1.5.1`。
- 真实 WPS 验收：`NOT RUN`。当前诊断显示 WPS 插件页面仍等待重新加载；未进行真实文档点击、批注预览、正式排版、保存后 DOCX/OOXML 复查。

### 未解决问题

- 需要在保存并完全退出 WPS 后重载 v1.5.1 当前构建，再按顺序执行：本机检测 → 任务窗格/功能区检查 → diagnostic 识别/预览入口 → 取消和文档切换 → 20、200、1000 段真实 WPS 验收。
- 在上述真实 WPS 和保存后 OOXML 证据完成前，`threadedPreviewEnabled` 不得开启，不能把本地门禁结果当作宿主验收结果。

### 下一步严格顺序

1. 保存用户文档并完全关闭全部 WPS 进程，重新打开后确认 build id 为 `20260806144203-c1ef25d8d2e9`。
2. 运行“本机检测”，核对 `wps-plugin.log` 中 Host → Worker → Broker 的事件链。
3. 仅运行 diagnostic 预览/识别，确认不写正式批注、不执行正式排版，并核对任务 result/error 合同。
4. 依次验收取消、文档切换、20/200/1000 段识别；最后才做正式预览、排版和保存后 DOCX/OOXML 检查。

### 生成文件路径

- 构建信息：`apps/classified-offline/ui/build-info.js`（脚本生成并受 Git 管理）。
- Broker readiness 测试：`tests/test_broker_readiness.py`。
- 唯一运行日志：`wps-plugin.log`（本机产物，已忽略，不提交）。
- runtime EXE、manifest、AppData status 和 `.runtime` 进程元数据：均为本机运行产物，已忽略，不提交。

### Git（本轮发布后）

- 版本：`1.5.1`。
- 目标远程：`ssh://git@ssh.github.com:443/7654321x/wps--plugin.git`，目标分支 `main`。
- release commit：`2c4187989bf51b02101e53520ef4909f881d0b96`（`fix: v1.5.1 精准诊断并修复 Broker 启动就绪判定`）。
- push：PASS；已使用 `git push origin HEAD:main`，推送后 `git rev-parse HEAD` 与 `git ls-remote origin refs/heads/main` 均为上述提交。
- 交接回写：本条目随后的 `docs: finalize v1.5.1 handoff` 提交同步；完成后再次核对远程 main 和工作树。

## 2026-08-06 WPS 插件 v1.5.2 主资源原始字节修复

### 当前结论

- 本轮只修复 WPS 主资源加载链，不修改识别规则、Broker 合同、Control Server 业务协议、排版命令、Worker 线程模型、任务窗格业务或顶部功能栏命令定义。
- 已确认原始根因：旧的 `scripts/wps_debug_server.py` 在 HTTP 响应阶段读取 `index.html` 后解码为文本，插入 `<script src="/hot-update-inject.js"></script>`，再重新编码返回。旧响应为 188 字节，构建文件为 143 字节；旧响应 SHA-256 前 12 位为 `807df864047a`，构建 SHA-256 前 12 位为 `c920eda7dae8`。
- 注入发生在资源响应阶段，不发生在 Vite 构建、调试包复制或 WPS 注册阶段；`apps/classified-offline/dist/index.html`、源码首页和重新生成的调试包首页均不含该脚本。
- 资源服务现在只从显式传入的 `.runtime/wps-debug-package` 读取当前构建调试包，该目录由 `scripts/prepare-wps-debug-package.ps1` 从 `apps/classified-offline/dist` 生成；首页和关键 JavaScript 均按原始字节返回，不进行文本替换或重新编码。

### 实际改动

- `scripts/wps_debug_server.py`：删除响应阶段热更新注入和热更新首页依赖；静态文件使用 `read_bytes()`，设置实际 `Content-Length`、Content-Type、`Cache-Control` 和 `Pragma`；不存在的资源直接 404，`.js` 不回退首页；启动时记录资源服务、构建目录和热更新关闭事件，首页请求/原样返回只写统一日志。
- `main.py`：资源探针将 `index.html`、`main.js`、`host-runtime.js`、`js/ribbon.js` 与 `apps/classified-offline/dist` 逐项比较状态码、Content-Type、Content-Length、字节长度、SHA-256 和完整字节；首页不一致时返回 `WPS_INDEX_RESPONSE_MISMATCH` 并记录前 12 位摘要，不记录完整 HTML。
- `scripts/prepare-wps-debug-package.ps1`：把 `index.html`、`main.js`、`host-runtime.js`、`js/ribbon.js` 纳入调试包关键资源摘要。
- `wps_logging.py`：增加首页一致性错误码和资源文件大小/摘要等中文字段；仍只有根目录 `wps-plugin.log` 一个运行日志。
- `tests/test_wps_debug_server.py`：新增原始字节、BOM/中文/CRLF、长度、哈希、404、日志和探针不一致回归测试；`scripts/verify-all.ps1` 纳入该测试。
- `docs/问题清单Agent.md`：新增 P062，固定禁止首页动态注入和响应文本重编码。
- 根项目、`package-lock.json`、classified 应用和 README 版本同步为 `1.5.2`；构建信息由脚本生成，当前 build id 为 `20260806153026-c1ef25d8d2e9`。

### 自动验证

- `py_compile main.py scripts/wps_debug_server.py wps_logging.py tests/test_wps_debug_server.py`：PASS。
- 资源与统一日志快速测试：9 PASS。
- `npm run typecheck`：PASS。
- `npm test`：96 PASS。
- `npm run verify:local-direct`：PASS；脱敏 fixture 32 段、31 blocks、31 bindings。
- `npm run verify:all`：PASS；Python 63 PASS、Node 96 PASS、classified/online build、Add-in 校验和安全扫描均通过。
- `ruff check scripts/wps_debug_server.py wps_logging.py tests/test_wps_debug_server.py`：PASS。`ruff check main.py scripts tests` 仍会报告 main.py 原有的 E701/E702 紧凑写法，本轮未做无关全文件格式化；固定全量门禁使用的 Ruff 范围已 PASS。
- PowerShell 7 实际资源验证：服务器长度 `143`，构建长度 `143`；服务器摘要和构建摘要均为 `C920EDA7DAE8DDFB0DD236EF16C69588303668EC7BE5F8E1A4CC7E8A8DA4E8C1`；输出 `WPS 首页响应与构建文件完全一致`。
- 实际 3889 资源边界：`/missing.js` 返回 `404`、`text/plain; charset=utf-8`；`/main.js` 返回 `200`、`application/javascript; charset=utf-8`。
- `main.py status`：调试资源服务已验证、WPS 注册已登记；当前 WPS 插件页面仍等待重新加载。现有 Broker 身份诊断为 `LOCAL_JOB_BROKER_EXECUTABLE_IDENTITY_MISMATCH`，属于本轮明确未修改的旧运行时状态，不影响资源一致性验证。

### 真实 WPS 验收状态

- 真实 WPS 主资源验收：`NOT RUN`。本轮没有声称 JS 调试器错误已消失、`main.js` 已在真实 WPS 执行、Ribbon 点击已恢复或真实 DOCX 功能通过；资源服务与构建文件的一致性已经通过本机 HTTP 验证。
- 顶部功能栏和侧边栏不属于本轮实现/验收目标；本轮没有改变现有 Ribbon 直接调用 `DocxtoolRunLocalCommand` 的命令定义。

### 下一步严格顺序

1. 保存并关闭测试文档，完全退出全部 WPS 进程。
2. 保持当前 3889 资源服务运行，重新打开 WPS 并观察 `wps-plugin.log` 中 `wps.resource.index.requested` 和 `wps.resource.index.served`。
3. 确认 JS 调试器不再报告 `Main resource content verification failed`，再点击顶部“本机检测”。
4. 如果响应仍与构建文件完全一致但 WPS 继续报告主资源校验失败，立即停止修改，按 P062 记录注册配置或 WPS 调试器缓存事实，不继续猜测式改动。

### 生成文件路径与 Git

- 受 Git 管理的构建信息：`apps/classified-offline/ui/build-info.js`。
- 本机调试包、资源探针和 PowerShell 下载副本：`.runtime/wps-debug-package/`、`.runtime/resource-probe.json`、`.runtime/served-index.html`，均不提交。
- 唯一运行日志：`wps-plugin.log`，本机产物，不提交。
- 产品代码提交：`404f337`（`fix: 修复 WPS 首页动态注入导致的主资源校验失败`）。
- GitHub push：PASS；已执行 `git push origin HEAD:main`，远程 `main` 已更新到 `404f337`。
- 交接文档回写提交：`d215746`（`docs: finalize v1.5.2 handoff`），已随后的 push 一并同步到远程 `main`。

## 2026-08-06 WPS 插件 v1.5.3 启动顺序与资源服务 build_id 同步修复

### 当前结论

- 用户提供的启动日志证明 v1.5.2 的首页字节修复有效：`index.html` 仍为 143 字节，资源一致性校验通过。
- 同一日志又暴露出新的可复发问题：资源服务日志使用旧 build `20260806152628-c1ef25d8d2e9`，而启动过程中调试包/`dist` 已被后续验证构建更新。原因是启动流程先注册资源服务，`verify:local-direct` 随后再次生成 classified build，旧资源服务进程继续运行并缓存旧 build_id。
- Broker 的 `LOCAL_JOB_BROKER_EXECUTABLE_HASH_MISMATCH` 只出现在初次旧进程校验，随后新 Broker 在约 0.8 秒内进入 READY，并完成识别 smoke；本轮没有修改 Broker 合同或识别协议。
- 新版 `main.py --once` 已实际完成启动：资源服务重启后日志中的 build_id 与最终构建一致，WPS 功能检测成功。

### 实际改动

- `main.py`：`start` 改为先完成会重建产物的 `verify`，再同步最终调试包、启动/复用 Control Server，并以 `force_restart=True` 重启受管理的 WPS 资源服务；未知占用端口仍直接失败，不结束未知进程。
- `register_addin()`：增加受控资源服务强制重启路径，并记录 `debug_server.restart`；普通 `status`/独立调用仍可复用已验证服务。
- 版本同步为 `1.5.3`；README、问题清单 P063 和本交接文档同步记录“验证重建后不得复用旧资源服务 build_id”。

### 自动验证

- `py_compile`：PASS。
- 资源、统一日志、Broker readiness 和启动顺序快速测试：32 PASS。
- `npm run typecheck`：PASS。
- `npm test`：PASS；Node 96/96。
- `npm run verify:all`：PASS；Python 64 PASS、Node 96 PASS，门禁末尾 classified 构建生成 build id `20260806155248-c1ef25d8d2e9`。
- `npm run verify:local-direct`：PASS；本地直连 fixture 为 32 段、31 blocks、31 bindings，末尾 classified 构建生成 build id `20260806155314-c1ef25d8d2e9`。
- 新版 `main.py --once`：PASS；最终 build id `20260806155420-c1ef25d8d2e9` 同时存在于 `apps/classified-offline/ui/build-info.js`、`.runtime/wps-debug-package/debug-package.json` 和资源服务日志；首页 143 字节、SHA-256 前 12 位 `c920eda7dae8` 一致。

### 真实 WPS 验收状态

- `NOT RUN`。本轮已完成新版启动器和本机 HTTP/日志验收，但尚未让真实 WPS 宿主重新加载本次构建；仍需完全重启 WPS 后验证主资源加载和顶部“本机检测”。

### 下一步严格顺序

1. 保存并关闭脱敏测试文档，完全退出全部 WPS 进程。
2. 重新打开 WPS，确认主资源加载日志使用 `20260806155021-c1ef25d8d2e9`，再点击顶部“本机检测”。
3. 若 WPS 仍报主资源校验失败，停止扩大修改，保留当前响应和注册配置证据。

### Git

- 产品代码提交：`c4269f8`（`release: v1.5.3 修复资源服务 build_id 同步`）。
- GitHub push：PASS；已执行 `git push origin HEAD:main`，远程 `main` 已更新到 `c4269f8`。
- 本文档已在产品提交后回写最终 build_id、验证结果和真实 WPS `NOT RUN` 状态；回写内容将通过后续 `docs:` 提交同步。

## 2026-08-07 WPS 插件 v1.5.4 runtime 复用启动流程

### 当前结论

- `main.py start` 不再每次重建和安装本地识别 runtime；当已安装 `current.json` 与 runtime 构建清单的版本、合同、识别/Broker SHA-256、文件名和安装路径一致时，直接复用 runtime。
- 显式 `main.py prepare` 仍强制重建 runtime。清单缺失或不一致时，普通 `start` 自动沿用原有构建安装流程。
- 真实启动已确认 `broker.runtime.reused`、`broker.reused` 和本地直连功能检测成功，未出现“开始构建本地识别组件”。本机 Codex 会话无法停止由更高权限启动的旧 3889 资源服务，因此完整启动在资源服务重启阶段按新错误码停止；这不是 runtime/Broker 失败。

### 实际改动

- `main.py`：新增 runtime 构建清单与已安装 runtime 的一致性判断；`start` 使用复用路径，`prepare` 保持强制重建。
- `main.py`：兼容 PyInstaller one-file 子进程 WMI 返回空路径/命令行的事实，以受信路径哈希、直接同名父子树和创建时间联合校验 Broker 与资源服务；未知进程仍不停止。
- `main.py`：统一时间解析兼容 `+0800` 与 `+08:00`；资源服务停止权限失败返回 `WPSJS_MANAGED_PROCESS_STOP_FAILED`，不再静默变成泛化端口占用。
- 版本同步为 `1.5.4`；README、问题清单 P064 与本交接文档同步更新。
- `docs/AGENTS.md`：新增“功能优先与不过度审核”，限制无关审计、假设性测试和猜测性防御；保留发布固定门禁与真实故障证明必要的校验。

### 自动验证

- `python -m pytest tests/test_broker_readiness.py tests/test_wps_debug_server.py -q`：PASS，31 项。
- `python -m py_compile main.py tests/test_broker_readiness.py tests/test_wps_debug_server.py`：PASS。
- `npm test`：PASS；Node 96/96。
- `npm run verify:all`：PASS；Python 65 PASS、Node 96 PASS，classified 构建生成 build id `20260806163438-c1ef25d8d2e9`。
- `npm run verify:local-direct`：PASS；本地直连 fixture 为 32 段、31 blocks、31 bindings，最终 classified build id 为 `20260806163503-c1ef25d8d2e9`。
- `main.py --once`：runtime 和 Broker 复用、WPS 本地直连功能检测 PASS；未出现 runtime 构建步骤。资源服务原始首页校验仍为 143 字节、摘要 `c920eda7dae8`，重启阶段明确返回 `WPSJS_MANAGED_PROCESS_STOP_FAILED`。

### 真实 WPS 验收状态

- `NOT RUN`。当前 3889 旧资源服务由更高权限进程持有，Codex 无权停止；必须由启动该服务的同一权限终端关闭后，才能完成资源服务重启与真实 WPS 重新加载验收。

### 下一步严格顺序

1. 在启动旧服务的同一权限终端执行 `main.py stop`，或关闭该终端及其子服务；不要结束未知进程。
2. 重新执行 `main.py --once`，确认 `debug_server.restart` 后出现新的资源服务 build_id。
3. 完全退出并重新打开 WPS，再验证顶部“本机检测”和真实主资源加载。

### Git

- 产品代码提交：`a3c7bd7`（`release: v1.5.4 复用已安装 runtime`）。
- GitHub push：PASS；已执行 `git push origin HEAD:main`，远程 `main` 已更新到 `a3c7bd7`。
- Agent 规则与本文档最终回写将通过后续 `docs:` 提交同步，不改变产品版本。
